# Local game data
machine_id.txt
player_data.json

# Build-time HMAC secret (D-09). The real value lives only in hmac_secret.local
# (never committed); the repo ships hmac_secret.example as a placeholder. The
# generated _baked_secret.py is build output carrying the obfuscated secret.
hmac_secret.local
_baked_secret.py

# Python
__pycache__/
*.pyc
.venv/
venv/

# Test artifacts (regenerated PNG/montage/GIF per run — never commit)
tests/artifacts/

# IDE
.idea/

# PyInstaller
build/
dist/
*.spec
freesansbold.ttf
firebase-key.json
/.vscode
/.claude
