---
phase: 09-arcade-juice
plan: 05
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - assets/audio/eat_ghost.wav
  - NOTICE
autonomous: false
requirements: [FEEL-02, FEEL-03, FEEL-05]
user_setup:
  - service: "audio asset"
    why: "Eat-ghost sound (FEEL-03) — a CC0/permissive or self-authored .wav; licensing must be human-verified (D-09/D-10)"
    dashboard_config:
      - task: "Source a CC0/permissive retro eat-ghost .wav (freesound.org CC0 filter / Kenney.nl / OpenGameArt CC0) OR self-author in bfxr/jsfxr; save as assets/audio/eat_ghost.wav"
        location: "assets/audio/"
      - task: "Record source + author + license in a NOTICE/credits line"
        location: "NOTICE (repo root)"

must_haves:
  truths:
    - "Full pytest is green with NO --bless: golden state traces + frame-hash + juice firewall + determinism guard + all FEEL feature tests + the FEEL-02/05 regression guards all pass"
    - "A CC0/permissive or self-authored assets/audio/eat_ghost.wav exists with its source+author+license recorded in NOTICE"
    - "In real play (juice=True): the death wedge ends about when death.wav ends; the eat-ghost bite is audible over the paused siren; the frightened blink reads clearly in the last ~2s"
    - "The new eat_ghost.wav ships in the PyInstaller build (assets glob) and the eat sound plays in dist/pacman"
  artifacts:
    - path: "assets/audio/eat_ghost.wav"
      provides: "FEEL-03 eat-ghost sound asset (human-sourced, licensed)"
    - path: "NOTICE"
      provides: "Recorded source+author+license for the eat-ghost .wav (V14 supply-chain)"
  key_links:
    - from: "build.py --add-data=assets;assets"
      to: "dist/pacman/assets/audio/eat_ghost.wav"
      via: "PyInstaller assets-tree glob (A4 — confirmed: whole assets/ is bundled)"
      pattern: "add-data=assets"
---

<objective>
Phase sign-off: prove SC5 (no re-bless), supply the human-sourced eat-ghost .wav with its license, and playtest the three subjective dials (death cadence D-04, eat-sound feel D-10, blink readability D-07) that automated tests cannot judge. Also re-asserts the already-shipped FEEL-02 + FEEL-05 stay green.

Purpose: This is the golden gate. The deterministic suite must be green with NO `--bless` — that single fact is the SC5 success signal. The .wav is the only artifact Claude cannot legitimately acquire/license, so the user provides + verifies it here (D-09/D-10).
Output: full green suite (no --bless); `assets/audio/eat_ghost.wav` + NOTICE; signed-off playtest.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-VALIDATION.md
@.planning/phases/09-arcade-juice/09-01-SUMMARY.md
@.planning/phases/09-arcade-juice/09-02-SUMMARY.md
@.planning/phases/09-arcade-juice/09-03-SUMMARY.md
@.planning/phases/09-arcade-juice/09-04-SUMMARY.md
@build.py
@sound.py
@settings.py
@CLAUDE.md
</context>

<artifacts_this_phase>
- `assets/audio/eat_ghost.wav` (human-sourced CC0/permissive or self-authored)
- `NOTICE` line: eat_ghost.wav source + author + license (V14 supply-chain record)
- No code artifacts — this plan verifies and supplies the asset only.
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: SC5 golden gate — full suite green, no --bless</name>
  <files>(verification only — no file written)</files>
  <read_first>09-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" (the phase gate = full pytest green, no --bless; frame-hash asserts only on Linux CI). 09-RESEARCH.md "Golden-Safety Status" (the eat_freeze is already baked into the re-blessed goldens — do NOT --bless to "fix" anything).</read_first>
  <action>Run the full test suite with NO --bless flag and confirm green: golden state traces (9 scenarios), frame-hash (asserts on Linux CI; skips local Windows — note which environment ran), juice firewall, determinism guard, the three feature tests (test_death_anim/test_eat_ghost_sound/test_fright_flash), and test_feel_regression (FEEL-02 + FEEL-05). If ANY golden or frame-hash test is red, STOP — a juice=False path leaked; fix the offending feature plan rather than re-blessing. Record in the SUMMARY which interpreter ran and whether frame-hash asserted (Linux CI) or skipped (local Windows). NEVER pass --bless.</action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (no --bless anywhere in the command)
    - `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_frame_hash.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` exits 0
    - SUMMARY records the interpreter + whether frame-hash asserted (CI) or skipped (Windows), and confirms no --bless was used
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <done>Full suite green with NO re-bless — SC5 satisfied on the deterministic side.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Source + license the eat-ghost .wav (human action)</name>
  <files>assets/audio/eat_ghost.wav, NOTICE</files>
  <action>HUMAN ACTION — Claude cannot legitimately acquire or verify a CC0 license. The user supplies assets/audio/eat_ghost.wav and records its source+author+license in NOTICE per the steps in how-to-verify. D-09/D-10.</action>
  <what-built>The eat-ghost sound wiring (SoundManager.play_eat_ghost on Channel(2), fired on the bite) is complete from 09-04; the .wav slot is expected at assets/audio/eat_ghost.wav and currently degrades silently (None-guarded).</what-built>
  <how-to-verify>
    1. Obtain a distinct retro "eat-ghost" bite sound as a `.wav`. Lowest-risk: self-author in bfxr/jsfxr/sfxr (you are the author — no third-party license). Alternative: download a CC0/permissive clip (freesound.org with the CC0 license filter, Kenney.nl audio, or OpenGameArt CC0). Do NOT rip the copyrighted Namco arcade sound.
    2. Save it as `assets/audio/eat_ghost.wav` (exact filename — that is what SoundManager loads).
    3. Record a credits line in `NOTICE` at the repo root: filename, source URL (or "self-authored in <tool>"), author, and license (CC0/CC-BY/etc.). This is the V14 supply-chain record (D-09/D-10).
  </how-to-verify>
  <resume-signal>Type "added" once assets/audio/eat_ghost.wav exists and its license is recorded in NOTICE, or describe a blocker.</resume-signal>
  <done>assets/audio/eat_ghost.wav present; its source+author+license recorded in NOTICE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playtest sign-off — cadence, eat-sound feel, blink readability, exe build</name>
  <files>(verification only — settings.py dials may be tuned during playtest)</files>
  <action>HUMAN VERIFY — run the game with juice=True and judge the three subjective dials and the exe build per how-to-verify; dial settings.py constants if any read poorly. D-04/D-07/D-10/A4.</action>
  <what-built>All three FEEL visuals/audio are wired and golden-safe: the death wedge (FEEL-01, juice-gated), the frightened-end white blink (FEEL-04, juice-gated), and the eat-ghost bite sound (FEEL-03, ungated). Tunables DEATH_ANIM_FRAMES / FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL are dialable in settings.py.</what-built>
  <how-to-verify>
    1. Run the game normally (`.venv/Scripts/python.exe main.py`, which sets juice=True).
    2. DEATH CADENCE (D-04): die in-game; confirm the wedge collapse finishes about when `death.wav` ends. If it ends early/late, dial `DEATH_ANIM_FRAMES` in settings.py and re-die.
    3. EAT SOUND (D-10): eat a power pellet, then a frightened ghost; confirm the bite blip is audible over the paused siren and feels retro/distinct. Adjust the .wav or its volume if masked.
    4. BLINK READABILITY (D-07): watch the last ~2s of a power pellet; confirm the white blink clearly signals "no longer safe to chase." If it reads poorly, dial `FRIGHT_FLASH_INTERVAL` or the BLEND_RGB_ADD tint color in game.py.
    5. BUILD SHIP (A4): run `.venv/Scripts/python.exe build.py`; confirm `dist/pacman/assets/audio/eat_ghost.wav` exists (build.py globs the whole assets tree via `--add-data=assets;assets`) and the eat sound plays in the built exe.
  </how-to-verify>
  <resume-signal>Type "approved" once cadence, eat-sound feel, blink readability, and the exe build+sound all pass — or describe what to dial.</resume-signal>
  <done>Human-approved: death cadence, eat-sound feel, blink readability, and exe build+sound all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content asset → repo/build | A new third-party-or-self-authored .wav enters the repo and the shipped exe; the only real risk surface this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-AS | Repudiation / legal | assets/audio/eat_ghost.wav | mitigate | Blocking human-action checkpoint records source+author+license in NOTICE, or self-authors the sound (zero third-party provenance). A .wav is inert data, not executable code. |
| T-09-01 | Tampering | deterministic sim / golden net | mitigate | This plan writes no code; the golden gate (Task 1) proves juice=False render+sim stay byte-identical with NO --bless. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-09). The asset is the only new external artifact; handled by T-09-AS. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` → green with NO --bless (the SC5 success signal).
- `assets/audio/eat_ghost.wav` present + NOTICE license line recorded.
- Playtest sign-off: death cadence, eat-sound feel, blink readability, exe build ships the wav.
</verification>

<success_criteria>
- Full pytest green with NO re-bless (golden traces + frame-hash + firewall + determinism + all FEEL tests + FEEL-02/05 regression).
- Licensed eat_ghost.wav in assets/audio/ + NOTICE record.
- Human-approved playtest of the three subjective dials and the exe build.
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-05-SUMMARY.md` when done.
</output>
