---
phase: 09-arcade-juice
plan: 05
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified: []
autonomous: false
requirements: [FEEL-02, FEEL-05]
must_haves:
  truths:
    - "Full pytest is green with NO --bless: golden state traces + frame-hash + juice firewall + determinism guard + all remaining FEEL feature tests + the FEEL-02/05 regression guards all pass"
    - "In real play (juice=True): the death wedge ends about when death.wav ends; the frightened blink reads clearly in the last ~2s"
  artifacts: []
  key_links: []
---

<objective>
Phase sign-off: prove SC5 (no re-bless) and playtest the two remaining subjective dials (death cadence D-04, blink readability D-07) that automated tests cannot judge. Also re-asserts the already-shipped FEEL-02 + FEEL-05 stay green.

Purpose: This is the golden gate. The deterministic suite must be green with NO `--bless` — that single fact is the SC5 success signal.

NOTE — FEEL-03 (eat-ghost sound) was **cut/descoped** during Phase 9: sourcing/licensing a `.wav` was judged not worth the chore, and the 09-04 audio wiring was reverted. The former `.wav` human-action task and the eat-sound playtest dial are removed from this plan accordingly.

Output: full green suite (no --bless); signed-off playtest of death cadence + blink readability.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-VALIDATION.md
@.planning/phases/09-arcade-juice/09-01-SUMMARY.md
@.planning/phases/09-arcade-juice/09-02-SUMMARY.md
@.planning/phases/09-arcade-juice/09-03-SUMMARY.md
@.planning/phases/09-arcade-juice/09-04-SUMMARY.md
@settings.py
@CLAUDE.md
</context>

<artifacts_this_phase>
- No artifacts — this plan verifies the deterministic gate and collects the human playtest sign-off only.
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: SC5 golden gate — full suite green, no --bless</name>
  <files>(verification only — no file written)</files>
  <read_first>09-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" (the phase gate = full pytest green, no --bless; frame-hash asserts only on Linux CI). 09-RESEARCH.md "Golden-Safety Status" (do NOT --bless to "fix" anything).</read_first>
  <action>Run the full test suite with NO --bless flag and confirm green: golden state traces (9 scenarios), frame-hash (asserts on Linux CI; skips local Windows — note which environment ran), juice firewall, determinism guard, the remaining feature tests (test_death_anim/test_fright_flash), and test_feel_regression (FEEL-02 + FEEL-05). If ANY golden or frame-hash test is red, STOP — a juice=False path leaked; fix the offending feature plan rather than re-blessing. Record in the SUMMARY which interpreter ran and whether frame-hash asserted (Linux CI) or skipped (local Windows). NEVER pass --bless.</action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (no --bless anywhere in the command)
    - `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_frame_hash.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` exits 0
    - SUMMARY records the interpreter + whether frame-hash asserted (CI) or skipped (Windows), and confirms no --bless was used
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <done>Full suite green with NO re-bless — SC5 satisfied on the deterministic side.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playtest sign-off — death cadence, blink readability, exe build</name>
  <files>(verification only — settings.py dials may be tuned during playtest)</files>
  <action>HUMAN VERIFY — run the game with juice=True and judge the two remaining subjective dials and the exe build per how-to-verify; dial settings.py constants if any read poorly. D-04/D-07/A4.</action>
  <what-built>Both remaining FEEL visuals are wired and golden-safe: the death wedge (FEEL-01, juice-gated) and the frightened-end white blink (FEEL-04, juice-gated). Tunables DEATH_ANIM_FRAMES / FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL are dialable in settings.py.</what-built>
  <how-to-verify>
    1. Run the game normally (`.venv/Scripts/python.exe main.py`, which sets juice=True).
    2. DEATH CADENCE (D-04): die in-game; confirm the wedge collapse finishes about when `death.wav` ends. If it ends early/late, dial `DEATH_ANIM_FRAMES` in settings.py and re-die.
    3. BLINK READABILITY (D-07): watch the last ~2s of a power pellet; confirm the white blink clearly signals "no longer safe to chase." If it reads poorly, dial `FRIGHT_FLASH_INTERVAL` or the BLEND_RGB_ADD tint color in game.py.
    4. BUILD SHIP (A4): run `.venv/Scripts/python.exe build.py`; confirm the built exe (`dist/pacman`) launches and plays normally.
  </how-to-verify>
  <resume-signal>Type "approved" once death cadence, blink readability, and the exe build all pass — or describe what to dial.</resume-signal>
  <done>Human-approved: death cadence, blink readability, and exe build all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries this plan — FEEL-03's third-party `.wav` asset (the only prior risk surface) was cut. This plan writes no code and ships no assets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deterministic sim / golden net | mitigate | This plan writes no code; the golden gate (Task 1) proves juice=False render+sim stay byte-identical with NO --bless. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-09). No new external artifacts. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` → green with NO --bless (the SC5 success signal).
- Playtest sign-off: death cadence + blink readability + exe build launches.
</verification>

<success_criteria>
- Full pytest green with NO re-bless (golden traces + frame-hash + firewall + determinism + remaining FEEL tests + FEEL-02/05 regression).
- Human-approved playtest of the two subjective dials and the exe build.
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-05-SUMMARY.md` when done.
</output>
