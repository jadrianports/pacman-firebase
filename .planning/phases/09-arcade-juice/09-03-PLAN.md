---
phase: 09-arcade-juice
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - ghost.py
  - game.py
autonomous: true
requirements: [FEEL-04]

must_haves:
  truths:
    - "In the last ~2s of the power window (power_counter > FRIGHT_FLASH_START) with juice=True, frightened ghosts alternate blue↔white every FRIGHT_FLASH_INTERVAL frames"
    - "Under juice=False, frightened ghosts blit the steady spooked_img — byte-identical (D-08)"
    - "Ghost.__init__ accepts blink_white/spooked_white_img as keyword-defaults appended after level — every positional caller and test_ghost_micro stay green"
    - "Golden state traces + frame-hash stay green with no --bless"
  artifacts:
    - path: "ghost.py"
      provides: "Keyword-default blink params + spooked-branch white-blit selection"
      contains: "blink_white"
    - path: "game.py"
      provides: "Pre-tinted spooked_white_img + per-frame blink_white compute threaded into all four Ghost calls"
      contains: "spooked_white_img"
  key_links:
    - from: "game.py create_ghosts"
      to: "ghost.py Ghost(... blink_white=, spooked_white_img=)"
      via: "keyword args on all four constructions"
      pattern: "blink_white="
    - from: "ghost.py draw spooked branch"
      to: "self.spooked_white_img"
      via: "select white when self.blink_white"
      pattern: "self.spooked_white_img if self.blink_white"
---

<objective>
FEEL-04: frightened ghosts blink white in the last ~2 seconds of the power window so the player knows it is no longer safe to chase — gated behind juice (D-08) so juice=False render is byte-identical (SC5).

Purpose: The ghost is "dumb" and rebuilt every frame, so all juice + threshold + cadence logic lives in `Game` (D-06) and one precomputed bool + a pre-tinted white surface are threaded into `Ghost` via KEYWORD-DEFAULT params (Pitfall 2 — positional would break test_ghost_micro and every caller). Look is Claude's discretion (D-07): a white tint of the existing spooked_img via Surface.fill + BLEND_RGB_ADD, no new sprite asset.
Output: `ghost.py` ctor kwargs + spooked-branch blit selection; `game.py` `spooked_white_img` build + `blink_white` compute.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-PATTERNS.md
@.planning/phases/09-arcade-juice/09-01-SUMMARY.md
@ghost.py
@game.py
@settings.py
@tests/test_ghost_micro.py
@CLAUDE.md
</context>

<artifacts_this_phase>
- new Ghost kwargs: `blink_white=False`, `spooked_white_img=None` (appended after `level` in ghost.py:280-281), stored before the construction-time `self.draw()` call at ghost.py:300
- modified Ghost.draw spooked branch (ghost.py:305-306): selects `self.spooked_white_img` when `self.blink_white` else `self.spooked_img`
- new Game state: `Game.spooked_white_img` (pre-tinted once in __init__ after game.py:39)
- new compute in `Game.create_ghosts` (game.py:381-393): `blink_white` bool passed as a keyword to all four Ghost calls; consumes settings.FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL
</artifacts_this_phase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ghost keyword-default params + white-tint blit selection</name>
  <files>ghost.py</files>
  <read_first>ghost.py:280-298 (ctor signature ending `..., spooked_img, dead_img, level` + the `self.spooked_img = spooked_img` store at :296). ghost.py:299-300 (CRITICAL: `self.turns, self.in_box = self.check_collisions()` then `self.rect = self.draw()` run AT CONSTRUCTION — new params must be stored BEFORE line 299). ghost.py:302-310 (draw: spooked branch at :305-306 blits spooked_img). tests/test_ghost_micro.py:41-57 (make_ghost positional ctor — must keep working). 09-RESEARCH.md Pattern 3 + 09-PATTERNS.md "ghost.py — keyword-default params".</read_first>
  <behavior>
    - `Ghost(..., level, blink_white=True, spooked_white_img=<white surf>)` blits a different pixel than the same ghost with the default (blue) spooked_img, when in the spooked state.
    - Default construction (no new kwargs) is byte-identical to today — test_ghost_micro stays green.
  </behavior>
  <action>Append `, blink_white=False, spooked_white_img=None` to the Ghost.__init__ signature AFTER `level` (ghost.py:281). Store `self.blink_white = blink_white` and `self.spooked_white_img = spooked_white_img` mirroring the `self.spooked_img = spooked_img` line (ghost.py:296) — and BEFORE the construction-time `self.draw()` at ghost.py:300 so the first blit sees them. In draw()'s spooked branch (ghost.py:305-306), select `img = self.spooked_white_img if self.blink_white else self.spooked_img` and blit `img`. Defaults guarantee that when blink_white is False the existing spooked_img path is byte-identical (D-08). Do NOT add positional params; do NOT change any other branch of draw.</action>
  <acceptance_criteria>
    - ghost.py __init__ signature ends with `blink_white=False, spooked_white_img=None`; both stored as `self.blink_white`/`self.spooked_white_img` before line 300
    - ghost.py draw spooked branch contains `self.spooked_white_img if self.blink_white else self.spooked_img`
    - `.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py -q` exits 0 (positional ctor contract intact)
    - `.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>Ghost accepts the two keyword-defaults and blits white when blink_white; test_ghost_micro + determinism green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build spooked_white_img once + compute blink_white in create_ghosts</name>
  <files>game.py</files>
  <read_first>game.py:39 (`self.spooked_img = pygame.transform.scale(... 'assets/ghosts/powerup.png', (45,45))` — build the tint right after). game.py:381-393 (create_ghosts: four Ghost(...) calls each ending `..., self.spooked_img, self.dead_img, self.level)`). game.py:548 (power_counter paused during eat_freeze → blink pauses with the freeze for free). 09-RESEARCH.md Pattern 3 (blink_white = juice and powerup and power_counter>FRIGHT_FLASH_START and (power_counter//FRIGHT_FLASH_INTERVAL)%2==0; BLEND_RGB_ADD tint). tests/test_fright_flash.py (from 09-01).</read_first>
  <behavior>
    - `self.spooked_white_img` is a one-time white-tinted copy of spooked_img (Surface.copy + fill BLEND_RGB_ADD) built in __init__.
    - create_ghosts computes a single `blink_white` bool and passes it (plus self.spooked_white_img) as keyword args to all four Ghost constructions.
    - `blink_white` is ALWAYS False when juice is False (so juice=False render is byte-identical — test_blink_off_under_juice_false).
  </behavior>
  <action>In __init__ right after game.py:39 build `self.spooked_white_img = self.spooked_img.copy()` then `self.spooked_white_img.fill((90, 90, 120, 0), special_flags=pygame.BLEND_RGB_ADD)` (BLEND_RGB_ADD exists in both pygame editions — Pitfall 5; tune the add-color per D-07 in the 09-05 playtest). In create_ghosts (game.py:381-393), compute once before the four calls: `blink_white = (self.juice and self.powerup and self.power_counter > FRIGHT_FLASH_START and (self.power_counter // FRIGHT_FLASH_INTERVAL) % 2 == 0)`. Append `blink_white=blink_white, spooked_white_img=self.spooked_white_img` as KEYWORD args to all four Ghost(...) calls (after self.level). Import FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL from settings. The `self.juice and ...` guard makes this golden-safe (D-01/D-08).</action>
  <acceptance_criteria>
    - game.py __init__ builds `self.spooked_white_img` via `.copy()` + `.fill(..., special_flags=pygame.BLEND_RGB_ADD)`
    - game.py create_ghosts computes `blink_white` with `self.juice and self.powerup and self.power_counter > FRIGHT_FLASH_START` and passes `blink_white=` + `spooked_white_img=` to all four Ghost calls
    - `.venv/Scripts/python.exe -m pytest tests/test_fright_flash.py -x` exits 0 (RED→GREEN, incl. test_blink_off_under_juice_false)
    - `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` exits 0 with NO --bless
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_fright_flash.py tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>Frightened ghosts blink white in the last window under juice; juice=False byte-identical; golden + firewall + determinism green with no re-bless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Draw-only sprite swap in a local offline game; no network/auth/input-trust/crypto/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deterministic sim / golden net (juice=False spooked render) | mitigate | `blink_white` includes `self.juice and ...`; under juice=False it is always False → identical spooked_img blit (D-08). Verified by golden_traces + frame_hash + firewall green, no --bless. |
| T-09-02 | Tampering | Ghost ctor contract (positional callers / test_ghost_micro) | mitigate | New params are keyword-defaults appended after `level` (Pitfall 2); test_ghost_micro.py asserted green. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_fright_flash.py tests/test_ghost_micro.py tests/test_golden_traces.py tests/test_frame_hash.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` → green (frame-hash asserts on Linux CI; skips local Windows).
- NO `--bless` anywhere.
</verification>

<success_criteria>
- Frightened ghosts alternate blue↔white in the last ~2s under juice=True.
- juice=False spooked render byte-identical (D-08).
- Ghost keyword-default ctor keeps every positional caller + test_ghost_micro green.
- Golden state traces + frame-hash + firewall green with NO re-bless (SC5).
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-03-SUMMARY.md` when done.
</output>
