---
phase: 09-arcade-juice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
  - tests/test_death_anim.py
  - tests/test_eat_ghost_sound.py
  - tests/test_fright_flash.py
  - tests/test_feel_regression.py
autonomous: true
requirements: [FEEL-01, FEEL-02, FEEL-03, FEEL-04, FEEL-05]

must_haves:
  truths:
    - "settings.py exposes DEATH_ANIM_FRAMES, FRIGHT_FLASH_START, FRIGHT_FLASH_INTERVAL as pure ints"
    - "Three new-feature tests exist and fail RED on the not-yet-built symbols (death_anim_frame/_draw_death, play_eat_ghost, blink_white)"
    - "FEEL-02 and FEEL-05 regression guards exist and are GREEN, pinning the already-shipped eat-freeze popup and READY! beat"
    - "Golden state traces, frame-hash, juice firewall, and determinism guard stay green with no --bless (the new constants are inert pure ints)"
  artifacts:
    - path: "settings.py"
      provides: "FEEL tunables DEATH_ANIM_FRAMES / FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL"
      contains: "DEATH_ANIM_FRAMES"
    - path: "tests/test_death_anim.py"
      provides: "Failing FEEL-01 wedge tests (RED until 09-02)"
    - path: "tests/test_eat_ghost_sound.py"
      provides: "Failing FEEL-03 eat-sound tests (RED until 09-04)"
    - path: "tests/test_fright_flash.py"
      provides: "Failing FEEL-04 blink tests (RED until 09-03)"
    - path: "tests/test_feel_regression.py"
      provides: "GREEN FEEL-02 + FEEL-05 characterization guards"
  key_links:
    - from: "tests/test_*.py"
      to: "harness.headless / harness.replay"
      via: "init_headless + install_frame_driven_sound"
      pattern: "install_frame_driven_sound"
    - from: "tests/test_death_anim.py + test_fright_flash.py"
      to: "settings constants"
      via: "import DEATH_ANIM_FRAMES / FRIGHT_FLASH_START / FRIGHT_FLASH_INTERVAL"
      pattern: "FRIGHT_FLASH_START"
---

<objective>
Stand up the phase's failing-test-first net (the Phase-8 pattern) plus the centralized tunables, and lock the two already-shipped FEEL behaviors with GREEN regression guards.

Purpose: Every new visual must be provably golden-safe (SC5). This plan creates the RED tests that 09-02/03/04 turn GREEN, adds the named `settings.py` dials those features consume, and pins FEEL-02 (eat popup + 45-frame freeze) and FEEL-05 ("READY!" beat) so the new juice work cannot silently regress them — without re-implementing either.
Output: `settings.py` constants; `tests/test_death_anim.py`, `tests/test_eat_ghost_sound.py`, `tests/test_fright_flash.py` (RED); `tests/test_feel_regression.py` (GREEN).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-PATTERNS.md
@.planning/phases/09-arcade-juice/09-VALIDATION.md
@settings.py
@tests/test_juice_firewall.py
@tests/test_ghost_micro.py
@CLAUDE.md
</context>

<artifacts_this_phase>
This plan produces (phase-wide inventory; this plan owns the starred rows):
- settings constants*: `DEATH_ANIM_FRAMES` (75, FEEL-01/D-04 dial), `FRIGHT_FLASH_START` (480, FEEL-04/D-06), `FRIGHT_FLASH_INTERVAL` (8, FEEL-04/D-07)
- new test files*: `tests/test_death_anim.py`, `tests/test_eat_ghost_sound.py`, `tests/test_fright_flash.py`, `tests/test_feel_regression.py`
- (built later) state field `death_anim_frame` + method `_draw_death` (09-02); `SoundManager.play_eat_ghost` + `eat_ghost_sound` + `_eat_channel` (09-04); Ghost kwargs `blink_white`/`spooked_white_img` + `Game.spooked_white_img` (09-03); asset `assets/audio/eat_ghost.wav` + license NOTICE (09-05)
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Add FEEL tunables to settings.py</name>
  <files>settings.py</files>
  <read_first>settings.py:111-119 (Phase-8 fairness tunables block — the exact pure-int, one-line-rationale style to mirror; GHOST_CATCH_DISTANCE etc.). 09-RESEARCH.md Pattern 1 (DEATH_ANIM_FRAMES rationale) + Pattern 3 (FRIGHT_FLASH_START/INTERVAL). 09-PATTERNS.md "settings.py — new tunables".</read_first>
  <action>Append a new "Phase 9 - Arcade Juice tunables (FEEL-01/FEEL-04; D-04/D-06/D-07)" block directly after the Phase-8 block at settings.py:119, matching its style exactly (pure ints, no new import, one-line rationale comment per dial so test_determinism_guard stays green). Add three names: DEATH_ANIM_FRAMES = 75 (FEEL-01/D-04 — wedge collapse frame budget; ~1.25s at 60 FPS; provisional, dialed to death.wav length during the 09-05 playtest); FRIGHT_FLASH_START = 480 (FEEL-04/D-06 — blink when power_counter > 480, i.e. the last 120 of the 600-frame power window ≈ 2s); FRIGHT_FLASH_INTERVAL = 8 (FEEL-04/D-07 — frames per blink half-cycle). Do NOT touch any existing constant. Do NOT add floats or imports.</action>
  <acceptance_criteria>
    - `settings.py` contains `DEATH_ANIM_FRAMES`, `FRIGHT_FLASH_START`, and `FRIGHT_FLASH_INTERVAL` assigned to integer literals
    - `.venv/Scripts/python.exe -c "import settings; assert isinstance(settings.DEATH_ANIM_FRAMES,int) and isinstance(settings.FRIGHT_FLASH_START,int) and isinstance(settings.FRIGHT_FLASH_INTERVAL,int)"` exits 0
    - `.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q` exits 0 (no float/forbidden token introduced)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>The three FEEL tunables exist as pure ints; determinism guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing-test net for FEEL-01 / FEEL-03 / FEEL-04</name>
  <files>tests/test_death_anim.py, tests/test_eat_ghost_sound.py, tests/test_fright_flash.py</files>
  <read_first>tests/test_juice_firewall.py:1-42 (headless-Game harness: SDL dummy env, init_headless, Game(surface, clock), install_frame_driven_sound, _frame_hash two-run equality). tests/test_ghost_micro.py:41-57 (make_ghost positional Ghost ctor — the contract test_fright_flash must NOT break). 09-VALIDATION.md "Phase Requirements → Test Map" + "Wave 0 Requirements". 09-RESEARCH.md Patterns 1-3 for the exact target symbols.</read_first>
  <behavior>
    - test_death_anim.py: with g.juice=True and g forced into `dying`, ticking once drives `g.death_anim_frame` upward and calls `_draw_death` without raising (player_circle stays safe — Pitfall 4); a companion test asserts that two juice=False games still render byte-identical `dying` frames (reuse the _frame_hash two-run equality). These FAIL now because `death_anim_frame`/`_draw_death` do not exist yet.
    - test_eat_ghost_sound.py: `g.sound.play_eat_ghost()` exists, uses a dedicated channel, and no-raises headless; a spy on `play_eat_ghost` is called when the eat branch fires. FAILS now (AttributeError on play_eat_ghost).
    - test_fright_flash.py: a Ghost built with blink_white=True blits a different pixel than one built with the steady spooked_img; and create_ghosts yields blink_white=False whenever juice=False (test_blink_off_under_juice_false). FAILS now (Ghost has no blink_white kwarg).
  </behavior>
  <action>Create the three test files using the test_juice_firewall headless harness (import init_headless, build Game on its own off-screen pygame.Surface, install_frame_driven_sound). Target the EXACT symbols 09-02/03/04 will build: `Game.death_anim_frame` + `Game._draw_death`; `SoundManager.play_eat_ghost` on Channel(2); `Ghost(..., blink_white=..., spooked_white_img=...)` keyword params + the spooked-branch blit; `Game.create_ghosts` computing `blink_white`. Build ghosts in test_fright_flash via the positional make_ghost shape from test_ghost_micro.py:41-57 plus the two new keyword args (NEVER positional). Use only dual-edition-safe primitives in assertions (screen.get_at, draw.polygon/circle, Surface.fill+BLEND_RGB_ADD) — NO gaussian_blur (Pitfall 5). Tests MUST import cleanly (Game/Ghost/init_headless all exist) and fail on the missing attribute/method (RED), not on a collection/import error. Name the determinism-safe blink-off test exactly `test_blink_off_under_juice_false` and the dying no-raise test exactly `test_dying_juice_frame_ok` (09-VALIDATION references these IDs).</action>
  <acceptance_criteria>
    - `tests/test_death_anim.py`, `tests/test_eat_ghost_sound.py`, `tests/test_fright_flash.py` all exist and are collectible (no import/collection error)
    - `.venv/Scripts/python.exe -m pytest tests/test_death_anim.py tests/test_eat_ghost_sound.py tests/test_fright_flash.py -q` exits NON-zero (RED on the missing symbols — proves they target the right not-yet-built API)
    - `tests/test_fright_flash.py` defines `test_blink_off_under_juice_false`; `tests/test_death_anim.py` defines `test_dying_juice_frame_ok`
    - Failure messages reference the missing symbol (AttributeError/TypeError on death_anim_frame/_draw_death/play_eat_ghost/blink_white), NOT a NameError at import time
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_death_anim.py tests/test_eat_ghost_sound.py tests/test_fright_flash.py -q; test $? -ne 0</automated>
  </verify>
  <done>Three feature tests exist, collect cleanly, and fail RED on the exact symbols the implementation plans will create.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN regression guards for FEEL-02 + FEEL-05 (already shipped — assert, do not re-implement)</name>
  <files>tests/test_feel_regression.py</files>
  <read_first>game.py:464-476 (eat branch sets eat_freeze=True, eat_freeze_timer=45, eat_freeze_score=points 200/400/800/1600). game.py:541-545 (eat_freeze tick-down + unpause_powerup at 0). game.py:591-604 (eat-popup render, juice=False = black rect + cyan score). game.py:198-202 (draw_ready: blits yellow 'READY!' at center (WIDTH//2, 540) while self.starting). tests/test_juice_firewall.py:1-42 harness; tests/test_ghost_micro.py:41-57 for placing a ghost on the player.</read_first>
  <action>Create characterization tests that PIN the current shipped behavior (capture-the-current-behavior, not prescribe new). FEEL-02: build a headless Game, set g.powerup=True, create_ghosts with a ghost positioned at the player so the eat branch fires, call check_ghost_collisions(player_circle); assert g.eat_freeze becomes True, g.eat_freeze_timer == 45, and g.eat_freeze_score in {200,400,800,1600}; then assert the timer decrements toward 0 across ticks and eat_freeze clears (mirrors game.py:541-545). FEEL-05: build a fresh headless Game (starts starting=True), run one juice=False tick, and assert the READY! beat renders during the starting phase — a non-black (yellow) pixel exists near the READY text region (center x=WIDTH//2, y≈540) while g.starting is True. These MUST be GREEN now (both features already shipped). Do NOT modify game.py. Use only dual-edition-safe primitives.</action>
  <acceptance_criteria>
    - `tests/test_feel_regression.py` exists with at least one FEEL-02 test and one FEEL-05 test
    - `.venv/Scripts/python.exe -m pytest tests/test_feel_regression.py -q` exits 0 (GREEN — pins the shipped behavior)
    - Golden subset + firewall stay green: `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_feel_regression.py tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>FEEL-02 and FEEL-05 are locked GREEN by characterization tests; golden traces + firewall + determinism unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local single-player offline game-feel phase — no network, auth, input-trust, crypto, or persistence surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deterministic sim / golden net (juice=False render + state trace) | mitigate | This plan adds only pure-int settings + test files; no game.py/ghost.py behavior change. Verified by golden_traces + firewall + determinism guard staying green with no --bless. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (D-09 rejects numpy/sndarray). RESEARCH "Package Legitimacy Audit" = N/A; nothing to verify on PyPI. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_feel_regression.py tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` → green
- `.venv/Scripts/python.exe -m pytest tests/test_death_anim.py tests/test_eat_ghost_sound.py tests/test_fright_flash.py -q` → RED (expected; turned green by 09-02/03/04)
- No `--bless` is run anywhere in this plan.
</verification>

<success_criteria>
- Three FEEL tunables present as pure ints (determinism guard green).
- Three new-feature tests collect cleanly and fail RED on the exact missing symbols.
- FEEL-02 + FEEL-05 regression guards GREEN.
- Golden state traces + firewall + determinism guard green with NO re-bless (SC5).
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-01-SUMMARY.md` when done.
</output>
