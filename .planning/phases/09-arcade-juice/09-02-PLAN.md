---
phase: 09-arcade-juice
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - game.py
autonomous: true
requirements: [FEEL-01]

must_haves:
  truths:
    - "Under juice=True during the dying phase, Pac-Man renders a wedge that collapses to nothing across DEATH_ANIM_FRAMES"
    - "Under juice=False, a dying frame renders exactly as today (player sprite held) — byte-identical"
    - "A dying frame under juice=True raises no exception (player_circle is never consulted during dying)"
    - "The death golden state trace + frame-hash manifest stay green with no --bless"
  artifacts:
    - path: "game.py"
      provides: "Death-wedge overlay method and juice-gated dying frame counter"
      contains: "_draw_death"
  key_links:
    - from: "game.py tick() render block"
      to: "game.py _draw_death"
      via: "if self.dying and self.juice branch"
      pattern: "self.dying and self.juice"
    - from: "game.py _draw_death"
      to: "self.death_anim_frame + math + pygame.draw.polygon"
      via: "frame-counter-driven wedge geometry"
      pattern: "pygame.draw.polygon"
---

<objective>
FEEL-01: replace the held player sprite with a classic arcade wedge-spin collapse during the dying phase — but only when juice is on (D-05), so the death golden/frame-hash terminal is untouched (SC5).

Purpose: Death should "play out" in sync with death.wav (D-04) instead of just freezing. The effect is a draw-only overlay gated by `self.juice`, driven by a juice-gated frame counter and `math` (no random/wall-clock — D-03).
Output: `game.py` gains `self.death_anim_frame` state + `_draw_death()` + the gated render branch.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-PATTERNS.md
@.planning/phases/09-arcade-juice/09-01-SUMMARY.md
@game.py
@settings.py
@CLAUDE.md
</context>

<artifacts_this_phase>
- new state field: `Game.death_anim_frame` (int, juice-gated, reset to 0 in __init__/start_dying/reset_after_death)
- new method: `Game._draw_death(anim_frame)` (wedge overlay; pygame.draw.polygon + math; consumes settings.DEATH_ANIM_FRAMES)
- modified render branch: game.py:586-587 player-draw site, now `if self.dying and self.juice: ... else: player_circle = self.player.draw(self.counter)`
</artifacts_this_phase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add death_anim_frame state + _draw_death wedge method</name>
  <files>game.py</files>
  <read_first>game.py:87-92 (__init__ dying/dying_delay/eat_freeze state — add death_anim_frame beside dying_delay). game.py:140-150 (reset_after_death). game.py:419-424 (start_dying — the reset-on-death-start template). game.py:152-202 (draw_board/draw_ready: existing pygame.draw usage + math.sin already in file → math is imported). 09-RESEARCH.md Pattern 1 (copy-ready _draw_death geometry: filled circle minus a growing mouth, half-mouth 0→pi, facing map by direction, 24 steps, color (255,222,0); returns early once fully collapsed). 09-PATTERNS.md "game.py — death wedge".</read_first>
  <behavior>
    - `Game.death_anim_frame` initializes to 0 and is reset to 0 in both start_dying and reset_after_death.
    - `_draw_death(anim_frame)` draws a wedge whose mouth half-angle grows from 0 to pi as anim_frame/DEATH_ANIM_FRAMES goes 0→1, returning without drawing once fully collapsed (vanished). Uses only `pygame.draw.polygon`, `math`, and `self.player.center_x/center_y/direction`.
    - No `random`, no `time`, no `get_ticks` (Pitfall 3 / determinism guard).
  </behavior>
  <action>In __init__ add `self.death_anim_frame = 0` adjacent to `self.dying_delay` (game.py:88). In start_dying (game.py:419-424) and reset_after_death (game.py:140-150) set `self.death_anim_frame = 0`. Add a `_draw_death(self, anim_frame)` method implementing the 09-RESEARCH Pattern 1 wedge: compute p = min(1.0, anim_frame / DEATH_ANIM_FRAMES); mouth = p * math.pi; return early if mouth >= math.pi; map self.player.direction to a facing angle; build the polygon point list (center + arc points from facing+mouth to facing+(2*pi-mouth), ~24 steps); pygame.draw.polygon to self.screen in Pac-Man yellow. Import DEATH_ANIM_FRAMES from settings (extend the existing settings import line — math is already imported). Use radius matching the current player sprite (~21). Do NOT wire the render branch yet (Task 2). Do NOT use draw.pie (does not exist) or gaussian_blur (CI vanilla pygame lacks it — Pitfall 5).</action>
  <acceptance_criteria>
    - `game.py` contains `def _draw_death(` and three assignments of `self.death_anim_frame = 0` (init, start_dying, reset_after_death) plus the init `self.death_anim_frame`
    - `_draw_death` body references `DEATH_ANIM_FRAMES`, `math.`, and `pygame.draw.polygon`; contains no `random`/`time`/`get_ticks`
    - `.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>State field + wedge method exist; determinism guard green; render not yet wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the juice-gated dying render branch</name>
  <files>game.py</files>
  <read_first>game.py:586-588 (the player-draw call site: `if not self.eat_freeze: player_circle = self.player.draw(self.counter)` then create_ghosts). game.py:616-618 (the collision guard `if not self.dying and not self.eat_freeze and not self.starting:` — the ONLY reader of player_circle, never runs during dying → Pitfall 4 safe). 09-RESEARCH.md Pattern 1 render-branch snippet (D-05). tests/test_death_anim.py (from 09-01 — the RED tests this turns GREEN).</read_first>
  <behavior>
    - When `not self.eat_freeze` and `self.dying and self.juice`: increment self.death_anim_frame and call self._draw_death(self.death_anim_frame) INSTEAD of player.draw.
    - Otherwise (the existing else): `player_circle = self.player.draw(self.counter)` exactly as today.
    - Under juice=False the branch is never taken → byte-identical dying render (D-05).
  </behavior>
  <action>At game.py:586-587, inside the existing `if not self.eat_freeze:` block, split into: `if self.dying and self.juice:` increment `self.death_anim_frame` then call `self._draw_death(self.death_anim_frame)`; `else:` keep the unchanged `player_circle = self.player.draw(self.counter)`. The death_anim_frame increment lives ONLY inside this juice branch (so juice=False sim is untouched and the counter never enters captured state — SC5). Do NOT reference player_circle outside the existing collision guard at game.py:616-618. Keep the branch strictly within `if not self.eat_freeze:`.</action>
  <acceptance_criteria>
    - `game.py` render block contains `if self.dying and self.juice:` with a `self.death_anim_frame += 1` and `self._draw_death(` call, and an `else:` retaining `player_circle = self.player.draw(self.counter)`
    - `.venv/Scripts/python.exe -m pytest tests/test_death_anim.py -x` exits 0 (RED→GREEN, including test_dying_juice_frame_ok)
    - `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` exits 0 with NO --bless (death trace + firewall byte-identical)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_death_anim.py tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>Wedge renders under dying+juice; juice=False dying byte-identical; death golden + frame-hash + firewall green with no re-bless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Draw-only overlay in a local offline game; no network/auth/input-trust/crypto/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deterministic sim / golden net (death trace + frame-hash) | mitigate | Wedge + frame counter are strictly inside `if self.dying and self.juice:`; juice=False dying path unchanged; death_anim_frame is not a captured-state field. Verified by golden_traces + frame_hash + firewall green, no --bless. |
| T-09-03 | Denial (crash) | dying frame under juice (player_circle unbound) | mitigate | Wedge branch lives inside `if not self.eat_freeze:` and never references player_circle; collision guard (game.py:616-618) is the only reader and never runs during dying. test_dying_juice_frame_ok asserts no raise. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_death_anim.py tests/test_golden_traces.py tests/test_frame_hash.py tests/test_juice_firewall.py tests/test_determinism_guard.py -q` → green (frame-hash asserts on Linux CI; skips on local Windows — CI is the authority).
- NO `--bless` anywhere.
</verification>

<success_criteria>
- Wedge collapse plays during dying when juice=True; vanishes by DEATH_ANIM_FRAMES.
- juice=False dying renders byte-identical (D-05).
- No exception on a dying juice frame.
- death golden state trace + frame-hash + firewall green with NO re-bless (SC5).
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-02-SUMMARY.md` when done.
</output>
