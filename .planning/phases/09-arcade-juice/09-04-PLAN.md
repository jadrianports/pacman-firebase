---
phase: 09-arcade-juice
plan: 04
type: execute
wave: 4
depends_on: ["09-01", "09-03"]
files_modified:
  - sound.py
  - game.py
autonomous: true
requirements: [FEEL-03]

must_haves:
  truths:
    - "On a ghost bite, SoundManager.play_eat_ghost() fires on a dedicated pygame.mixer.Channel(2)"
    - "play_eat_ghost() no-raises headless and when the .wav is absent (None-guarded degraded path)"
    - "The eat-ghost sound is ungated (D-02) — it plays under juice=False too, beside stop_waka/pause_powerup"
    - "Golden state traces + frame-hash stay green with no --bless (audio never touches the hashed pixel path)"
  artifacts:
    - path: "sound.py"
      provides: "eat-ghost sound loader, dedicated channel, and one-shot play method"
      contains: "def play_eat_ghost("
    - path: "game.py"
      provides: "Eat-ghost sound trigger in the check_ghost_collisions bite branch"
      contains: "self.sound.play_eat_ghost()"
  key_links:
    - from: "game.py check_ghost_collisions eat branch"
      to: "sound.py play_eat_ghost"
      via: "call after pause_powerup() on the bite"
      pattern: "self.sound.play_eat_ghost\\(\\)"
    - from: "sound.py play_eat_ghost"
      to: "pygame.mixer.Channel(2)"
      via: "one-shot channel play, None-guarded"
      pattern: "_eat_channel"
---

<objective>
FEEL-03: play a distinct retro eat-ghost sound on the bite. Audio is exempt from the firewall (D-02) — it never touches determinism or the hashed pixel path, so it ships ungated and the goldens stay green (SC5).

Purpose: Eating a ghost should reward with a distinct bite blip over the paused power siren. Wire a new `SoundManager.play_eat_ghost()` on a dedicated `pygame.mixer.Channel(2)` (Pitfall 6 — guaranteed audible, never stolen) and fire it where the bite is already detected (D-10). The actual .wav asset + licensing is sourced and verified by the user in 09-05 (D-09); the `_load_sound` None-guard means this code ships and degrades silently until then.
Output: `sound.py` loader + channel + `play_eat_ghost()`; one ungated call in `game.py` check_ghost_collisions.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-arcade-juice/09-CONTEXT.md
@.planning/phases/09-arcade-juice/09-RESEARCH.md
@.planning/phases/09-arcade-juice/09-PATTERNS.md
@.planning/phases/09-arcade-juice/09-01-SUMMARY.md
@sound.py
@game.py
@CLAUDE.md
</context>

<artifacts_this_phase>
- new SoundManager members: `self.eat_ghost_sound` (loaded from 'eat_ghost.wav'), `self._eat_channel = pygame.mixer.Channel(2)`, method `play_eat_ghost()` (one-shot, None-guarded)
- modified game.py:474-476: `self.sound.play_eat_ghost()` added after `self.sound.pause_powerup()` in the eat branch — UNGATED (D-02)
- (09-05 provides the actual `assets/audio/eat_ghost.wav` + license NOTICE)
</artifacts_this_phase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SoundManager loader + Channel(2) + play_eat_ghost()</name>
  <files>sound.py</files>
  <read_first>sound.py:10-18 (the four `_load_sound(...)` loads + `_waka_channel = Channel(0)` / `_powerup_channel = Channel(1)`). sound.py:19-25 (`_load_sound` already returns None if the file is missing → free degraded path). sound.py:43-45 (play_powerup shape: None-guard + channel.play with loops=-1 — the new method mirrors this but ONE-SHOT, no loops). 09-RESEARCH.md Pattern 2 + 09-PATTERNS.md "sound.py — play_eat_ghost".</read_first>
  <behavior>
    - `play_eat_ghost()` exists, plays `self.eat_ghost_sound` once on `self._eat_channel` (Channel 2), and no-raises when the sound is None (missing .wav) or headless (SDL dummy audio).
  </behavior>
  <action>In SoundManager.__init__ add `self.eat_ghost_sound = self._load_sound('eat_ghost.wav', volume=0.5)` beside the death_sound load (sound.py:13), and `self._eat_channel = pygame.mixer.Channel(2)` beside the other dedicated channels (sound.py:16-17). Add a `play_eat_ghost(self)` method mirroring play_powerup (sound.py:43-45) but as a ONE-SHOT (no loops=-1) on `self._eat_channel`, guarded by `if self.eat_ghost_sound:`. Default mixer has 8 channels so Channel(2) needs no set_num_channels call (Pitfall 6). Do NOT gate on juice (D-02). The 'eat_ghost.wav' file does not exist yet — _load_sound returns None and the method degrades silently (the file lands in 09-05).</action>
  <acceptance_criteria>
    - sound.py contains `def play_eat_ghost(`, `self.eat_ghost_sound`, and `self._eat_channel = pygame.mixer.Channel(2)`
    - play_eat_ghost is None-guarded (`if self.eat_ghost_sound:`) and uses no `loops=-1`
    - `.venv/Scripts/python.exe -c "import os; os.environ.setdefault('SDL_AUDIODRIVER','dummy'); import pygame; pygame.init(); pygame.mixer.init(); from sound import SoundManager; SoundManager().play_eat_ghost()"` exits 0 (no raise, degraded)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_eat_ghost_sound.py -k "channel or no_raise or play_eat_ghost" -q</automated>
  </verify>
  <done>play_eat_ghost exists on Channel(2), one-shot, None-guarded, no-raise headless.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fire play_eat_ghost on the bite (ungated)</name>
  <files>game.py</files>
  <read_first>game.py:464-476 (the eat branch: sets eat_freeze/timer/score/pos, then `self.sound.stop_waka()` + `self.sound.pause_powerup()` + `break`). 09-PATTERNS.md "game.py — eat-ghost sound trigger" (insert right after pause_powerup, ungated). tests/test_eat_ghost_sound.py spy test (from 09-01).</read_first>
  <behavior>
    - When the eat branch fires, `self.sound.play_eat_ghost()` is called exactly once on the bite (a spy/monkeypatch on play_eat_ghost is invoked when driving the eat branch).
    - The call is NOT behind `self.juice` (D-02) — audio is exempt and never hashed.
  </behavior>
  <action>In check_ghost_collisions, inside the eat branch, add `self.sound.play_eat_ghost()` immediately after `self.sound.pause_powerup()` (game.py:475) and before `break`. Do NOT gate on self.juice (D-02). Do NOT alter eat_freeze/timer/score/pos (those are the already-shipped FEEL-02 — leave byte-identical). This is the only game.py change in this plan.</action>
  <acceptance_criteria>
    - game.py eat branch contains `self.sound.play_eat_ghost()` after `self.sound.pause_powerup()`, not behind any `if self.juice`
    - `.venv/Scripts/python.exe -m pytest tests/test_eat_ghost_sound.py -x` exits 0 (RED→GREEN, incl. the spy that the bite calls it)
    - `.venv/Scripts/python.exe -m pytest tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py tests/test_feel_regression.py -q` exits 0 with NO --bless (FEEL-02 eat-freeze unchanged; audio never hashed)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_eat_ghost_sound.py tests/test_golden_traces.py tests/test_juice_firewall.py tests/test_determinism_guard.py tests/test_feel_regression.py -q</automated>
  </verify>
  <done>The bite fires play_eat_ghost ungated; FEEL-02 freeze + golden traces + firewall + determinism green with no re-bless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Audio playback in a local offline game; no network/auth/input-trust/crypto/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deterministic sim / golden net | mitigate | The eat sound is audio-only on Channel(2); it touches no captured state and no hashed pixel (SDL dummy audio headless). FEEL-02 eat-freeze state left byte-identical. Verified by golden_traces + feel_regression + firewall green, no --bless. |
| T-09-AS | Repudiation / legal | new eat_ghost.wav content asset | mitigate (deferred to 09-05) | This plan ships only the loader/trigger code; the actual CC0/self-authored .wav + license NOTICE is sourced and human-verified in 09-05 (D-09/D-10). _load_sound None-guard keeps the game fully playable until then. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-09 rejects numpy/sndarray); the only artifact is inert .wav data, not code. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_eat_ghost_sound.py tests/test_golden_traces.py tests/test_frame_hash.py tests/test_juice_firewall.py tests/test_determinism_guard.py tests/test_feel_regression.py -q` → green (frame-hash asserts on Linux CI; skips local Windows).
- NO `--bless` anywhere.
</verification>

<success_criteria>
- play_eat_ghost fires on the bite, on Channel(2), ungated (D-02).
- No-raise headless / missing-wav degraded path.
- FEEL-02 eat-freeze unchanged; golden traces + frame-hash + firewall green with NO re-bless (SC5).
</success_criteria>

<output>
Create `.planning/phases/09-arcade-juice/09-04-SUMMARY.md` when done.
</output>
