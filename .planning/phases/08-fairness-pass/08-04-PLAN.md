---
phase: 08-fairness-pass
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - tests/golden/box_exit/trace.jsonl
  - tests/golden/power_chase/trace.jsonl
  - tests/golden/ghost_eat/trace.jsonl
  - tests/golden/death/trace.jsonl
  - tests/golden/win/trace.jsonl
  - tests/golden/box_edge/trace.jsonl
  - tests/golden/tunnel_wrap/trace.jsonl
  - tests/golden/eyes_return/trace.jsonl
  - tests/golden/claude_session_01/trace.jsonl
  - tests/golden/box_exit/frame_hashes.txt
  - tests/golden/power_chase/frame_hashes.txt
  - tests/golden/ghost_eat/frame_hashes.txt
  - tests/golden/death/frame_hashes.txt
  - tests/golden/win/frame_hashes.txt
  - tests/golden/box_edge/frame_hashes.txt
  - tests/golden/tunnel_wrap/frame_hashes.txt
  - tests/golden/eyes_return/frame_hashes.txt
  - tests/golden/claude_session_01/frame_hashes.txt
  - tests/golden/death/input.jsonl
  - tests/golden/ghost_eat/input.jsonl
  - settings.py
autonomous: false
requirements: [FAIR-01, FAIR-02, FAIR-03]
must_haves:
  truths:
    - "The user has playtested on Windows and signed off on the feel before any re-bless (D-10 sequencing)"
    - "The death scenario still reaches game_over and ghost_eat still registers its eat under the new constants (re-author input if not - Pitfall 2)"
    - "The golden net (9 traces + frame-hash + determinism guard + 15 micro tests) is green after ONE deliberate Linux/Docker re-bless covering all three changes together"
    - "ghost micro-tests + determinism guard are green WITHOUT the re-bless (byte-identical decision-logic proof)"
  artifacts:
    - path: "tests/golden/death/trace.jsonl"
      provides: "re-blessed death trace under the new fairness constants"
      contains: ""
    - path: "tests/golden/ghost_eat/trace.jsonl"
      provides: "re-blessed ghost_eat trace under the new catch radius"
      contains: ""
  key_links:
    - from: "re-bless run"
      to: "Linux/Docker python:3.12 env"
      via: "never on Windows (frame-hash font rasterization differs - Pitfall 3)"
      pattern: "python:3.12"
---

<objective>
Close Phase 8 with the locked D-10 sequence: Build (done in 08-02/08-03) -> playtest -> re-bless. The user
playtests on Windows and dials the three tunable constants until the feel is right and signs off; only THEN
do we run the SINGLE deliberate golden-net re-bless on Linux/Docker that covers all three FAIR-* changes
together (never per-change, never on Windows). Because tighter catch (FAIR-01) + slower ghosts (FAIR-02)
make Pac-Man harder to kill, we explicitly verify each terminal-dependent scenario still terminates and
re-author its recorded input if not (Pitfall 2) - the re-bless is NOT purely mechanical.

Purpose: satisfy success criterion 5 (golden net green after one Linux/Docker re-bless) without re-blessing
against unsigned-off interim numbers, and prove criterion 4 (byte-identical ghost decision logic) by showing
the ghost micro-tests + determinism guard stay green WITHOUT the re-bless.

Output: regenerated `tests/golden/*/trace.jsonl` + `tests/golden/*/frame_hashes.txt` (9 scenarios) and,
if needed, re-authored `tests/golden/death/input.jsonl` and/or `tests/golden/ghost_eat/input.jsonl`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fairness-pass/08-CONTEXT.md
@.planning/phases/08-fairness-pass/08-RESEARCH.md
@.planning/phases/08-fairness-pass/08-PATTERNS.md
@tests/golden/manifest.json
@tests/test_golden_traces.py
@tests/test_frame_hash.py
@.planning/phases/08-fairness-pass/08-02-SUMMARY.md
@.planning/phases/08-fairness-pass/08-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan REGENERATES committed fixtures (not new symbols):
- `tests/golden/*/trace.jsonl` x9 - re-blessed integer-only state traces
- `tests/golden/*/frame_hashes.txt` x9 - re-blessed Linux-pinned pixel hashes
- Possibly re-authored `tests/golden/death/input.jsonl` and `tests/golden/ghost_eat/input.jsonl` (only if their scripted terminal no longer fires)
- No production-code symbols are created here. The final tunable values are the user's D-10 dials of the Plan 08-01 constants.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-10 playtest on Windows + dial constants + sign off (gate before any re-bless)</name>
  <files>settings.py</files>
  <read_first>
    - .planning/phases/08-fairness-pass/08-CONTEXT.md (D-10 workflow; D-02/D-07/D-09 ranges for the dials)
    - settings.py (the four FAIR tunables to dial)
    - CLAUDE.md (run the game: `python main.py`; .venv is the real env)
  </read_first>
  <action>
    HUMAN CHECKPOINT (D-10, blocking). Do NOT run any golden re-bless until this sign-off is given. Present the
    playtest steps below; the user launches the game on Windows and dials the four FAIR tunables in settings.py
    until the feel is right, then reports the FINAL values plus the explicit feel confirmation. Commit any dialed
    settings.py values before Task 2 runs so the re-bless reflects the signed-off numbers, not interim ones. This
    is human-judgment only (game feel) and cannot be automated.
  </action>
  <what-built>
    FAIR-01 (center-distance catch, GHOST_CATCH_DISTANCE), FAIR-02 (chase ghosts ~1.85 via the integer
    accumulator, GHOST_CHASE_SPEED_NUM/DEN), and FAIR-03 (pre-turn window, PLAYER_TURN_WINDOW_MARGIN) are
    implemented behind named, trivially-editable constants in settings.py. The game is fully playable on
    Windows. No golden re-bless has run yet (locked by D-10 until your sign-off).
  </what-built>
  <how-to-verify>
    1. Launch the game on Windows: `.venv/Scripts/python.exe main.py` (or `python main.py`).
    2. FAIR-01: walk Pac-Man past a ghost rounding a corner - a diagonal corner-kiss should read SAFE; only an essentially-same-tile overlap should kill you. Dial `GHOST_CATCH_DISTANCE` (D-02 range ~14-16) if catches feel too loose/tight.
    3. FAIR-02: run a straightaway with a ghost trailing - you should gradually pull away over a corridor (subtle, not instant). Dial `GHOST_CHASE_SPEED_NUM` (+1 ~= +0.05 px/frame faster ghosts; lower it to widen your escape edge). Confirm escape is always possible (never an unbeatable chase).
    4. FAIR-03: input a turn a few pixels before a junction - it should round smoothly, not overshoot, and not feel "auto-steered." Dial `PLAYER_TURN_WINDOW_MARGIN` (D-09 range ~4-6).
    5. Edit values directly in settings.py between runs; re-launch to feel the change. When it feels right, record the FINAL three values.
  </how-to-verify>
  <acceptance_criteria>
    - The user reports the FINAL chosen values of GHOST_CATCH_DISTANCE, GHOST_CHASE_SPEED_NUM, GHOST_CHASE_SPEED_DEN, and PLAYER_TURN_WINDOW_MARGIN.
    - The user explicitly confirms: corner-kiss is safe, straightaway escape works, cornering is snappy-not-auto-steered.
    - Any dialed values are committed to settings.py before the re-bless task runs (the re-bless must reflect the signed-off numbers, not interim ones).
  </acceptance_criteria>
  <verify>
    <human-check>User confirms the feel (corner-kiss safe, escape works, cornering snappy) and supplies the final four constant values, committed to settings.py.</human-check>
  </verify>
  <resume-signal>Reply "approved" with the final constant values, or describe what still feels off (and the dialed values to try).</resume-signal>
  <done>User has signed off on the feel and the final dialed constant values are committed to settings.py; no re-bless has run yet.</done>
</task>

<task type="auto">
  <name>Task 2: Single Linux/Docker re-bless + terminal-scenario verification</name>
  <files>tests/golden/*/trace.jsonl, tests/golden/*/frame_hashes.txt, tests/golden/death/input.jsonl, tests/golden/ghost_eat/input.jsonl</files>
  <read_first>
    - tests/golden/manifest.json (9 scenarios; `death` is the only `terminal: game_over`, frame_cap 4200; `ghost_eat` is fixed_frames and must register its scripted eat)
    - tests/test_golden_traces.py (the `--bless` path; the terminal assertions assert_invariants lines 159-179: "death did not reach game_over" / fixed_frames length check)
    - tests/test_frame_hash.py (lines 18-31 - Linux-pinned platform contract, pygame 2.6.1)
    - tests/conftest.py (lines 59-66 - `--bless` flag registration)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Pitfall 2 terminal re-authoring; Pitfall 3 Linux-only frame-hash bless; Open Question 1)
    - project memory golden-rebless-linux-docker.md (re-bless on Linux via python:3.12 Docker, never Windows)
    - requirements.txt (runtime deps to install in the container)
  </read_first>
  <action>
    Run the SINGLE re-bless in a Linux python:3.12 Docker container from the repo root - NEVER on the Windows
    playtest machine (frame-hash font rasterization differs - Pitfall 3). Mount the repo, install pytest plus
    the project runtime deps (at minimum pygame==2.6.1 to match the pinned CI hash contract; use requirements.txt
    if it covers them), set `SDL_VIDEODRIVER=dummy` and `SDL_AUDIODRIVER=dummy`, then run `python -m pytest --bless`
    to regenerate all 9 `tests/golden/*/trace.jsonl` and `tests/golden/*/frame_hashes.txt` under the signed-off
    constants. Then run the FULL suite WITHOUT `--bless` in the SAME container and check the terminal-dependent
    scenarios (Pitfall 2): if `death` no longer reaches `game_over` (assert_invariants raises "death did not reach
    game_over") or `ghost_eat` no longer registers its eat under the tighter 15px radius (its trace shows no
    `dead=True` flip / no eat_freeze), re-author the affected `tests/golden/<scenario>/input.jsonl` by headless
    re-capture so the scripted outcome fires again, then re-run `--bless` and re-verify. Repeat until the full
    suite is green in the container. Do all of this in one container session - this is the one and only re-bless
    for all three FAIR-* changes (D-10, never per-change).
  </action>
  <acceptance_criteria>
    - The bless ran in a Linux python:3.12 container (e.g. `docker run --rm -v "$PWD":/app -w /app -e SDL_VIDEODRIVER=dummy -e SDL_AUDIODRIVER=dummy python:3.12 bash -c "pip install -q pygame==2.6.1 pytest && python -m pytest --bless -q"`), NOT on Windows.
    - All 9 `tests/golden/*/trace.jsonl` and `tests/golden/*/frame_hashes.txt` are regenerated (git shows them modified).
    - In the SAME container, the full non-bless suite is green: `python -m pytest -q` exits 0 - including the `death` game_over terminal assertion and the `ghost_eat` eat registration.
    - If a terminal scenario failed to terminate, its `tests/golden/<scenario>/input.jsonl` was re-authored and re-blessed until green (git shows it modified); otherwise input.jsonl files are unchanged.
    - `grep -rlE '[0-9]+\.[0-9]+' tests/golden/*/trace.jsonl || echo CLEAN` prints CLEAN (no fractional float leaked into any trace; positions stayed integer).
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v "$PWD":/app -w /app -e SDL_VIDEODRIVER=dummy -e SDL_AUDIODRIVER=dummy python:3.12 bash -c "pip install -q pygame==2.6.1 pytest && python -m pytest -q"</automated>
  </verify>
  <done>One Linux/Docker re-bless regenerated all 9 golden traces + frame-hash manifests under the signed-off constants; every terminal-dependent scenario reaches its scripted outcome (input re-authored where needed); the full suite is green in the pinned env; no float leaked into any trace.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the byte-identical guard without re-bless, then commit fixtures</name>
  <files>tests/golden/*/trace.jsonl, tests/golden/*/frame_hashes.txt, tests/golden/death/input.jsonl, tests/golden/ghost_eat/input.jsonl</files>
  <read_first>
    - tests/test_ghost_micro.py (the 15 ghost characterization tests - the byte-identical proof artifact)
    - tests/test_determinism_guard.py (forbidden-token static guard)
    - git status output (to confirm ghost.py is unmodified across the whole phase)
  </read_first>
  <action>
    Confirm criterion 4 (byte-identical ghost decision logic) holds INDEPENDENT of the re-bless: the ghost
    micro-tests and the determinism guard must pass WITHOUT any re-blessed fixture, because ghost.py was never
    touched. Verify `git diff --stat ghost.py` shows zero changes across Plans 08-02 and 08-03. Then stage and
    commit the regenerated golden fixtures (all 9 trace.jsonl + frame_hashes.txt, plus any re-authored
    input.jsonl from Task 2) as the single deliberate re-bless, with a message documenting that all three FAIR-*
    changes were blessed together on Linux/Docker after D-10 sign-off (cite the final constant values). Ensure
    any plan/doc files committed are LF (CRLF breaks downstream must_haves parsing).
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q` exits 0 on Windows WITHOUT any re-bless (proof the guard held).
    - `git diff --stat ghost.py` shows ZERO lines changed in ghost.py for the entire phase.
    - The regenerated `tests/golden/*/trace.jsonl` + `tests/golden/*/frame_hashes.txt` (and any re-authored input.jsonl) are committed in one re-bless commit.
    - The commit message names the single Linux/Docker re-bless covering FAIR-01 + FAIR-02 + FAIR-03 and records the final dialed constant values.
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q</automated>
  </verify>
  <done>Ghost micro-tests + determinism guard green without re-bless (byte-identical proof); ghost.py shows zero diff; the single re-bless of all 9 fixtures (+ any re-authored input) is committed with a documenting message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local keyboard -> game sim | Only untrusted input is local keystrokes (already handled). This plan re-records committed test fixtures and runs the game locally; no new boundary. |
| committed golden fixtures -> CI merge gate | The re-blessed traces/hashes become the merge truth. Risk is an off-platform or unsigned-off bless, not an external attacker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | golden fixtures / sim outcomes | accept | Offline single-player local fixtures; no network/persistence/external input. The score-range and not-in-wall invariants are still asserted across every re-blessed trace by the golden net. |
| T-08-03 | Tampering | off-platform / premature re-bless | mitigate | Re-bless runs ONLY in the Linux python:3.12 env after D-10 sign-off (Pitfall 3); a single deliberate bless, never on Windows, never per-change. Terminal scenarios are re-verified (Pitfall 2). |
| T-08-02 | Tampering | determinism of the regression net | mitigate | Proven by `tests/test_determinism_guard.py` + integer-only traces (no float leaked); confirmed green without re-bless. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept (N/A) | The only installs are `pytest`/`pygame==2.6.1` into an ephemeral Docker container to run the bless - pinned, first-party test deps, no new app dependency. No registry/slopcheck surface in the shipped app. |

No high-severity threats. Offline, local, single-player fixture re-bless; no new attack surface.
</threat_model>

<verification>
- In the Linux python:3.12 container: full `python -m pytest -q` green AFTER the single `--bless`, including the death game_over terminal and ghost_eat eat registration.
- On Windows: `.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q` green WITHOUT re-bless (criterion 4 proof).
- `git diff --stat ghost.py` shows zero changes for the whole phase.
- No `tests/golden/*/trace.jsonl` line contains a decimal point (integer positions preserved).
- Exactly one re-bless commit; never blessed on Windows, never per-change (D-10).
</verification>

<success_criteria>
- D-10 sequencing honored: build -> Windows playtest + sign-off -> single Linux/Docker re-bless (criterion 5).
- All 9 golden traces + frame-hash manifests re-blessed together under the signed-off constants; full suite green in the pinned env; terminal scenarios still terminate (input re-authored if needed).
- Ghost micro-tests + determinism guard green without re-bless; ghost.py byte-identical (criterion 4).
</success_criteria>

<output>
Create `.planning/phases/08-fairness-pass/08-04-SUMMARY.md` when done.
</output>
