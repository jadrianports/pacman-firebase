---
phase: 08-fairness-pass
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - game.py
  - tests/test_fairness_unit.py
autonomous: true
requirements: [FAIR-01, FAIR-02]
must_haves:
  truths:
    - "A catch registers only when player and ghost centers are within GHOST_CATCH_DISTANCE - a diagonal corner-kiss is SAFE (FAIR-01)"
    - "Chasing ghosts average ~1.85 px/frame while every ghost position stays a strict integer; Pac-Man (speed 2) gradually pulls away (FAIR-02)"
    - "ghost.py is byte-identical (untouched) - ghost micro-tests and the determinism guard stay green as the proof artifact"
  artifacts:
    - path: "game.py"
      provides: "center-to-center squared-distance catch helper"
      contains: "def _catches"
    - path: "game.py"
      provides: "per-ghost integer-rational step accumulator state"
      contains: "ghost_step_acc"
    - path: "game.py"
      provides: "accumulator-driven chase step in update_ghost_speeds"
      contains: "GHOST_CHASE_SPEED_NUM"
  key_links:
    - from: "game.py check_ghost_collisions"
      to: "_catches"
      via: "all three catch groups call self._catches (no colliderect remains in the catch path)"
      pattern: "self\\._catches"
    - from: "game.py update_ghost_speeds"
      to: "self.ghost_step_acc"
      via: "integer-rational step gated on self.moving and not self.eat_freeze"
      pattern: "ghost_step_acc"
---

<objective>
Implement FAIR-01 and FAIR-02 entirely within `game.py` (+ green its unit tests). FAIR-01 replaces the
AABB `colliderect` catch with an integer center-to-center squared-distance test against `GHOST_CATCH_DISTANCE`
(D-01/D-02/D-04). FAIR-02 makes the lethal chase tier average ~1.85 px/frame via a per-ghost integer-rational
step accumulator that keeps every ghost position a strict integer (D-05/D-06/D-07) - never a fractional speed.

Purpose: corner-kisses become safe and the player can finally out-run a straightaway chase, WITHOUT touching
`ghost.py`. All collision arbitration and speed assignment already live in `Game`; ghosts only consume the
per-frame `speed` value. Leaving `ghost.py` byte-identical makes the core-value guard (success criterion 4)
trivially true and keeps the 15 ghost micro-tests + determinism guard green as the proof.

Output: `game.py` with `_catches` + the step accumulator; the FAIR-01/02 unit tests in
`tests/test_fairness_unit.py` flipped from xfail to passing.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fairness-pass/08-CONTEXT.md
@.planning/phases/08-fairness-pass/08-RESEARCH.md
@.planning/phases/08-fairness-pass/08-PATTERNS.md
@game.py
@settings.py
@.planning/phases/08-fairness-pass/08-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols (exclude from drift/source-grounding - they do not exist before Phase 8):
- `Game._catches(self, ghost)` - integer squared-distance catch helper (FAIR-01)
- `Game.ghost_step_acc` - per-ghost integer-rational accumulator list `[0,0,0,0]` (FAIR-02)
- Consumes (from Plan 08-01): `settings.GHOST_CATCH_DISTANCE`, `settings.GHOST_CHASE_SPEED_NUM`, `settings.GHOST_CHASE_SPEED_DEN`
- `ghost.py` is intentionally NOT modified by this plan (byte-identical guard)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FAIR-01 - center-to-center squared-distance catch (all three catch groups)</name>
  <files>game.py, tests/test_fairness_unit.py</files>
  <read_first>
    - game.py (check_ghost_collisions, lines 400-436 - the 3 `colliderect` groups; the call site at line 578 `self.check_ghost_collisions(player_circle)` and its `not eat_freeze and not starting` gate at 576-578)
    - player.py (center_x = x+23, center_y = y+24, lines 23-29 - always live on self.player)
    - ghost.py (center_x = x_pos+22, center_y = y_pos+22, lines 284-285 - READ ONLY, do not edit)
    - settings.py (GHOST_CATCH_DISTANCE from Plan 08-01)
    - tests/test_fairness_unit.py (the xfail FAIR-01 catch tests to flip)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Pattern 1; "Bonus correctness note" on reading centers off self.player not the stale player_circle)
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (_catches drop-in helper; D-04 symmetric replacement)
  </read_first>
  <behavior>
    - Same-tile overlap (centers coincident) -> caught.
    - Diagonal one-tile corner-kiss (centers ~41px apart) -> NOT caught (the corner-kiss-safe fix, D-02).
    - Boundary: 15px apart -> caught; 16px apart -> NOT caught.
    - Applied symmetrically to ALL existing catch checks (D-04): normal ghost-kills-player, powerup already-eaten ghost-kills-player, AND the player-eats-frightened-ghost loop.
  </behavior>
  <action>
    Import `GHOST_CATCH_DISTANCE` into game.py's `from settings import (...)` block. Add a helper
    `def _catches(self, ghost):` that returns `dx*dx + dy*dy <= GHOST_CATCH_DISTANCE * GHOST_CATCH_DISTANCE`
    where `dx = self.player.center_x - ghost.center_x` and `dy = self.player.center_y - ghost.center_y`
    (integer squared distance per D-01; NO `math.sqrt`, no float - keeps determinism guard green). In
    `check_ghost_collisions`, replace every `player_circle.colliderect(<ghost>.rect)` across all THREE groups
    (normal kill 403-406, powerup already-eaten kill 411-414, eat loop 425) with `self._catches(<ghost>)`,
    keeping the surrounding `and not <ghost>.dead` / `and self.eaten_ghost[idx]` guards exactly as they are
    (D-04 symmetric). Read centers off `self.player` (always live) rather than the passed `player_circle`
    (stale during eat_freeze) per RESEARCH; you may leave the `player_circle` parameter in the signature.
    After implementing, remove the `@pytest.mark.xfail` markers from the FAIR-01 catch tests in
    tests/test_fairness_unit.py so they report as passed (strict xfail would XPASS-fail otherwise).
    Do NOT touch ghost.py.
  </action>
  <acceptance_criteria>
    - game.py contains `def _catches(self, ghost):` and the body uses `dx * dx + dy * dy <= GHOST_CATCH_DISTANCE * GHOST_CATCH_DISTANCE`
    - `grep -v '^[[:space:]]*#' game.py | grep -c "colliderect"` is 0 inside the catch path (no AABB catch remains; all three groups call `self._catches`)
    - `grep -c "math.sqrt" game.py` does not increase (no sqrt added)
    - The FAIR-01 catch tests in tests/test_fairness_unit.py no longer carry `@pytest.mark.xfail` and report passed
    - `.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -k catch -q` exits 0 with passes (no xfail/xpass)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -k catch tests/test_determinism_guard.py -q -rxX</automated>
  </verify>
  <done>All three catch groups use `self._catches`; no `colliderect` remains in the catch path; the FAIR-01 unit tests pass with their xfail markers removed; determinism guard green; ghost.py untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FAIR-02 - integer-rational step accumulator for the lethal chase tier</name>
  <files>game.py, tests/test_fairness_unit.py</files>
  <read_first>
    - game.py (update_ghost_speeds 333-353; `self.ghost_speeds = [2,2,2,2]` init line 77; reset_after_death 133-141; the moving gate `if self.moving and not self.eat_freeze:` at 573-575; update_ghost_speeds CALL site at line 543 - note it runs every frame, before create_ghosts at 548)
    - settings.py (GHOST_CHASE_SPEED_NUM=37, GHOST_CHASE_SPEED_DEN=20 from Plan 08-01)
    - ghost.py (consumes self.speed via move_*; READ ONLY - do not edit; this is why FAIR-02 lives in game.py)
    - tests/test_fairness_unit.py (the xfail FAIR-02 accumulator test to flip)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Pattern 2 full; Pitfall 1 fractional-speed trap; Pitfall 4 accumulator-during-pauses)
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (update_ghost_speeds + accumulator code shape; gating analog; reset analog)
  </read_first>
  <behavior>
    - Over 20 consecutive moving frames (no powerup, no dead, no eaten), each chase ghost's per-frame step is in {1,2} and the 20-frame sum is exactly GHOST_CHASE_SPEED_NUM (37) -> average 1.85 px/frame (D-06).
    - Ghost positions remain strict integers at all times (never a fractional 1.85 - Pitfall 1).
    - Frightened tier (1, D-08) and eyes-return tier (4, D-08) are byte-identical - untouched.
    - The accumulator advances ONLY on moving frames (`self.moving and not self.eat_freeze`) and is reset on death (Pitfall 4) so no phantom credit banks across pauses/lives.
  </behavior>
  <action>
    Import `GHOST_CHASE_SPEED_NUM, GHOST_CHASE_SPEED_DEN` into game.py's settings import. Add
    `self.ghost_step_acc = [0, 0, 0, 0]` adjacent to `self.ghost_speeds = [2, 2, 2, 2]` (line 77). In
    `update_ghost_speeds`, AFTER the existing base-tier assignment (powerup->1, else->2, eaten->2, dead->4),
    refine ONLY the lethal-chase tier: iterate i in range(4) and for each ghost whose computed
    `self.ghost_speeds[i] == 2` (this uniquely identifies the lethal chaser in BOTH the default-chase and the
    eaten-revived-during-powerup cases per D-04/A2), advance the integer-rational accumulator -
    `self.ghost_step_acc[i] += GHOST_CHASE_SPEED_NUM`, `step = self.ghost_step_acc[i] // GHOST_CHASE_SPEED_DEN`,
    `self.ghost_step_acc[i] -= step * GHOST_CHASE_SPEED_DEN`, then `self.ghost_speeds[i] = step`. Gate this
    refinement on `self.moving and not self.eat_freeze` (read directly inside update_ghost_speeds, since it is
    called every frame at line 543 before create_ghosts at 548); on non-moving frames leave the tier at 2
    (ghosts do not move those frames, so it is inert) and do NOT advance the accumulator (Pitfall 4). Leave
    tiers 1 and 4 exactly as-is (D-08 - frightened-slow and eyes-return unchanged). Append
    `self.ghost_step_acc = [0, 0, 0, 0]` to reset_after_death (lines 133-141). All pure integer math: no float,
    no `random`/wall-clock, no new import beyond the two settings constants. Do NOT edit ghost.py.
    After implementing, remove the `@pytest.mark.xfail` marker from the FAIR-02 accumulator test in
    tests/test_fairness_unit.py.
  </action>
  <acceptance_criteria>
    - game.py `__init__` contains `self.ghost_step_acc = [0, 0, 0, 0]` and reset_after_death resets it
    - update_ghost_speeds uses `GHOST_CHASE_SPEED_NUM` and `GHOST_CHASE_SPEED_DEN` and assigns the floored `step` to `self.ghost_speeds[i]` only for the `== 2` tier, gated on `self.moving and not self.eat_freeze`
    - The `== 1` (frightened) and `== 4` (eyes) tier assignments are unchanged
    - `grep -v '^[[:space:]]*#' game.py | grep -Ec "random|time.time|get_ticks|datetime|sqrt|1\\.85"` is 0 (no fractional speed, no forbidden token)
    - The FAIR-02 accumulator test no longer carries `@pytest.mark.xfail` and reports passed
    - `.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -k accumulator tests/test_ghost_micro.py tests/test_determinism_guard.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py tests/test_ghost_micro.py tests/test_determinism_guard.py -q -rxX</automated>
  </verify>
  <done>The chase tier averages 1.85 via the integer-rational accumulator (positions stay integer); frightened/eyes tiers untouched; accumulator gated to moving frames and reset on death; FAIR-02 unit test passes with xfail removed; ghost micro-tests + determinism guard green; ghost.py byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local keyboard -> game sim | Only untrusted input is local keystrokes (already handled). This plan changes local deterministic collision + speed math; no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | catch/speed sim math in game.py | accept | Offline single-player local math; no network/persistence/external input. Score-range invariant (0 <= score <= MAX_SCORE) is unchanged and still asserted per-frame by the golden net. |
| T-08-02 | Tampering | determinism of the regression net | mitigate | FAIR-01 uses integer squared distance (no sqrt/float); FAIR-02 keeps positions strictly integer via an integer-rational accumulator (no fractional speed, no new import, no `random`/wall-clock). `tests/test_determinism_guard.py` stays green. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Phase installs no external packages; edits first-party game.py + test file only. No registry/slopcheck surface. |

No high-severity threats. Offline, local, single-player gameplay math; no new attack surface.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -q -rxX` exits 0 with FAIR-01 + FAIR-02 tests passed (no xfail/xpass remaining).
- `.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q` exits 0 - the byte-identical decision-logic + determinism proof (ghost.py untouched).
- `git diff --stat ghost.py` shows ZERO changes to ghost.py.
- No `colliderect` remains in the catch path; no fractional `1.85` speed anywhere in game.py.
</verification>

<success_criteria>
- FAIR-01: catch uses center-to-center squared distance vs GHOST_CATCH_DISTANCE across all three catch groups; corner-kiss reads SAFE; eat path uses the same model (D-04).
- FAIR-02: chase ghosts average ~1.85 px/frame via the integer-step accumulator; positions stay integer; frightened (1) and eyes (4) tiers unchanged (D-08).
- ghost.py byte-identical; ghost micro-tests + determinism guard green (criterion 4 proof).
</success_criteria>

<output>
Create `.planning/phases/08-fairness-pass/08-02-SUMMARY.md` when done.
</output>
