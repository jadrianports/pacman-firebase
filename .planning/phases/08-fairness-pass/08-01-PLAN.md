---
phase: 08-fairness-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
  - tests/test_player_micro.py
  - tests/test_fairness_unit.py
autonomous: true
requirements: [FAIR-01, FAIR-02, FAIR-03]
must_haves:
  truths:
    - "All four FAIR tunables are named constants in settings.py, trivially editable for the D-10 playtest"
    - "A fast, deterministic, non-golden test net exists for FAIR-01/02/03 (red-until-implemented via strict xfail)"
  artifacts:
    - path: "settings.py"
      provides: "FAIR tunable constants"
      contains: "GHOST_CATCH_DISTANCE"
    - path: "settings.py"
      provides: "ghost chase-speed integer-rational tunable"
      contains: "GHOST_CHASE_SPEED_NUM"
    - path: "settings.py"
      provides: "player pre-turn window tunable"
      contains: "PLAYER_TURN_WINDOW_MARGIN"
    - path: "tests/test_player_micro.py"
      provides: "FAIR-03 cornering-window characterization (baseline + widened)"
      contains: "PLAYER_TURN_WINDOW_MARGIN"
    - path: "tests/test_fairness_unit.py"
      provides: "FAIR-01 catch-helper + FAIR-02 accumulator unit tests"
      contains: "GHOST_CHASE_SPEED_NUM"
  key_links:
    - from: "tests/test_fairness_unit.py"
      to: "settings.GHOST_CATCH_DISTANCE / GHOST_CHASE_SPEED_NUM / GHOST_CHASE_SPEED_DEN"
      via: "import from settings"
      pattern: "from settings import"
    - from: "tests/test_player_micro.py"
      to: "settings.PLAYER_TURN_WINDOW_MARGIN"
      via: "import from settings"
      pattern: "PLAYER_TURN_WINDOW_MARGIN"
---

<objective>
Lay the Phase 8 foundation: add the four named, arcade-faithful tunable constants to `settings.py`
(D-03/D-07/D-09) and author the fast non-golden proof artifacts for all three fairness changes.
Tests assert the NEW (post-change) behavior and are RED today, guarded with `@pytest.mark.xfail(strict=True)`
so the suite stays green at wave merge; the Wave 2 implementation plans remove each marker as its
test turns green.

Purpose: `settings.py` is the shared dependency that lets the two Wave 2 implementation plans
(`game.py` FAIR-01/02 and `player.py` FAIR-03) run in parallel without a file conflict, and these
tests give FAIR-01/02/03 a deterministic, sub-second proof artifact independent of the golden net
(which is only re-blessed once, at the very end — D-10).

Output: 4 new constants in `settings.py`; `tests/test_player_micro.py` (NEW); `tests/test_fairness_unit.py` (NEW).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fairness-pass/08-CONTEXT.md
@.planning/phases/08-fairness-pass/08-RESEARCH.md
@.planning/phases/08-fairness-pass/08-PATTERNS.md
@settings.py
@tests/test_ghost_micro.py
@tests/conftest.py
</context>

## Artifacts this phase produces

Newly-created symbols (exclude from drift/source-grounding — they do not exist before this phase):
- `settings.GHOST_CATCH_DISTANCE` (FAIR-01 catch radius, px)
- `settings.GHOST_CHASE_SPEED_NUM`, `settings.GHOST_CHASE_SPEED_DEN` (FAIR-02 integer-rational chase step)
- `settings.PLAYER_TURN_WINDOW_MARGIN` (FAIR-03 pre-turn widening, px)
- `tests/test_player_micro.py` (NEW test file)
- `tests/test_fairness_unit.py` (NEW test file)
- `Game.ghost_step_acc` and `Game._catches` are produced in Plan 08-02 (referenced red here)

<tasks>

<task type="auto">
  <name>Task 1: Add the four FAIR tunable constants to settings.py</name>
  <files>settings.py</files>
  <read_first>
    - settings.py (append target; see existing `PLAYER_SPEED = 2` line 10 and `HALF_TILE = 15` line 21 as the analog)
    - .planning/phases/08-fairness-pass/08-CONTEXT.md (D-02/D-03/D-07/D-09 ranges)
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (settings.py new-tunables block)
  </read_first>
  <action>
    Append a documented "Phase 8 - Fairness Pass tunables" block at the end of settings.py with four
    new module-level constants, all plain integers (no float, no new import - keeps the determinism
    guard green): `GHOST_CATCH_DISTANCE = 15` (FAIR-01 center-to-center catch radius in px, HALF_TILE-ish,
    per D-02 ~14-16px), `GHOST_CHASE_SPEED_NUM = 37` and `GHOST_CHASE_SPEED_DEN = 20` (FAIR-02
    integer-rational chase step; 37/20 = 1.85 px/frame average per D-06/D-07; document that +1 to NUM
    is approximately +0.05 px/frame for the D-10 dial), and `PLAYER_TURN_WINDOW_MARGIN = 6` (FAIR-03
    pre-turn widening each edge in px, per D-09 ~4-6px early). Add a comment that each is a D-10 playtest
    dial. Do NOT change `PLAYER_SPEED = 2` (D-07 - controls feel identical in the player's hands).
  </action>
  <acceptance_criteria>
    - settings.py contains `GHOST_CATCH_DISTANCE = 15`
    - settings.py contains `GHOST_CHASE_SPEED_NUM = 37` and `GHOST_CHASE_SPEED_DEN = 20`
    - settings.py contains `PLAYER_TURN_WINDOW_MARGIN = 6`
    - settings.py still contains `PLAYER_SPEED = 2` (unchanged)
    - `python -c "import settings; print(settings.GHOST_CATCH_DISTANCE, settings.GHOST_CHASE_SPEED_NUM, settings.GHOST_CHASE_SPEED_DEN, settings.PLAYER_TURN_WINDOW_MARGIN)"` prints `15 37 20 6`
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import settings; assert (settings.GHOST_CATCH_DISTANCE, settings.GHOST_CHASE_SPEED_NUM, settings.GHOST_CHASE_SPEED_DEN, settings.PLAYER_TURN_WINDOW_MARGIN, settings.PLAYER_SPEED) == (15, 37, 20, 6, 2)"</automated>
  </verify>
  <done>The four FAIR tunables exist as integer constants in settings.py with documenting comments; PLAYER_SPEED stays 2; the import-assert above exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/test_player_micro.py - FAIR-03 cornering-window characterization</name>
  <files>tests/test_player_micro.py</files>
  <read_first>
    - tests/test_ghost_micro.py (the analog: headless SDL-dummy screen fixture, module docstring, deepcopy-board-per-case, exact-literal assertions)
    - tests/conftest.py (SDL dummy drivers set before pygame import; `--bless` registration)
    - player.py (check_position lines 43-91; the four `12 <= ... <= 18` windows at lines 65,70,76,81; centers at 23-29)
    - board.py (board.boards layout - to pick a concrete junction with an open perpendicular corridor)
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (tests/test_player_micro.py NEW section: make_player helper, assertion pattern)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Wave 0 Gaps; Phase Requirements -> Test Map FAIR-03 row)
  </read_first>
  <behavior>
    - Baseline (NO xfail, green now and after): at a junction position whose travel-axis residue is squarely inside the original window (residue 15), check_position grants the queued perpendicular turn - pins that the at-junction window still works.
    - Widened (xfail strict until FAIR-03): at the SAME junction but positioned ~6px earlier so the travel-axis residue is 8 (inside the widened `(12 - PLAYER_TURN_WINDOW_MARGIN) .. (18 + PLAYER_TURN_WINDOW_MARGIN)` = 6..24 band, but OUTSIDE the original 12..18), check_position grants the perpendicular turn. This is False under today's player.py and becomes True only after Plan 08-03 widens the window.
  </behavior>
  <action>
    Create tests/test_player_micro.py modeled on tests/test_ghost_micro.py. Reuse the module-scoped
    headless `screen` fixture pattern (pygame.display.init + set_mode((900,950))) and a `make_player(screen, x, y, direction)`
    helper that constructs `Player(screen)` and sets `x`/`y`/`direction`. Each case calls
    `p.check_position(copy.deepcopy(board.boards))` and asserts on the returned `turns_allowed`.
    Pick ONE concrete junction from board.boards that has an open perpendicular corridor (mirror how
    test_ghost_micro uses a known decisive intersection), and compute player x/y so the travel-axis
    `center % TILE` residue equals 15 for the baseline case and 8 for the widened case (8 is in the
    margin-6 widened band 6..24 but not in the legacy 12..18 band). Write `test_cornering_baseline_window`
    (no marker) asserting the perpendicular turn is allowed at residue 15, and `test_cornering_preturn_widened`
    decorated with `@pytest.mark.xfail(strict=True, reason="RED until FAIR-03 widens the player window - Plan 08-03, Wave 2")`
    asserting the perpendicular turn is allowed at residue 8. Import `PLAYER_TURN_WINDOW_MARGIN` from settings
    and reference it when documenting/deriving the residue so the intent (D-09 pre-turn forgiveness) is explicit.
    Do NOT touch ghost.py or any ghost window - this is a player-only affordance (D-15).
  </action>
  <acceptance_criteria>
    - tests/test_player_micro.py exists and imports `copy`, `pygame`, `pytest`, `board`, `Player`, and `PLAYER_TURN_WINDOW_MARGIN` from settings
    - The file defines `test_cornering_baseline_window` (no xfail) and `test_cornering_preturn_widened` (decorated `@pytest.mark.xfail(strict=True, ...)`)
    - `.venv/Scripts/python.exe -m pytest tests/test_player_micro.py -q` exits 0 with 1 passed + 1 xfailed (the widened case is xfail today, NOT xpass)
    - `grep -c "PLAYER_TURN_WINDOW_MARGIN" tests/test_player_micro.py` is >= 1
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_player_micro.py -q -rxX</automated>
  </verify>
  <done>Both player-cornering tests are collected; baseline passes, widened is reported xfailed (not xpassed) against today's player.py; the suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author tests/test_fairness_unit.py - FAIR-01 catch helper + FAIR-02 accumulator</name>
  <files>tests/test_fairness_unit.py</files>
  <read_first>
    - tests/test_ghost_micro.py (headless screen fixture analog)
    - tests/conftest.py (SDL dummy drivers; bless flag)
    - game.py (constructor `Game(screen, timer)` lines 24-25; `check_ghost_collisions` 400-436; `update_ghost_speeds` 333-353; `self.ghost_speeds` init line 77; the moving gate at 568-575)
    - player.py (center_x = x+23, center_y = y+24, lines 23-29)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Pattern 1 squared-distance form; Pattern 2 integer-rational step; Wave 0 Gaps)
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (catch helper + accumulator code shapes)
  </read_first>
  <behavior>
    - FAIR-01 catch helper (xfail strict until Plan 08-02 adds `Game._catches`):
      - Same-tile overlap (player center == fake-ghost center) -> caught (True).
      - Diagonal one-tile corner-kiss (centers offset by approx TILE_WIDTH=30 horizontally and TILE_HEIGHT=28 vertically, distance approx 41px) -> NOT caught (False) - this is the corner-kiss-safe assertion (D-02).
      - Boundary: centers exactly GHOST_CATCH_DISTANCE (15) apart -> caught (15*15 <= 15*15); 16 apart -> NOT caught.
    - FAIR-02 accumulator (xfail strict until Plan 08-02 adds the integer-rational step): driving `update_ghost_speeds` over 20 moving frames with no powerup / no dead / no eaten ghosts yields a per-frame ghost_speeds[0] sequence whose every value is in {1,2} and whose 20-frame sum is exactly GHOST_CHASE_SPEED_NUM (37) -> long-run average 1.85; today (no accumulator) the value is always 2 and the 20-sum is 40, so the test is RED.
  </behavior>
  <action>
    Create tests/test_fairness_unit.py with the headless `screen` fixture (pygame.display.init + set_mode).
    Construct a `Game(screen, clock)` headless (use a `pygame.time.Clock()` for the timer arg, matching
    tests/test_golden_traces.py). For FAIR-01: write tests that set `game.player.x`/`game.player.y` to control
    `player.center_x`/`center_y`, build a stand-in ghost with `types.SimpleNamespace(center_x=..., center_y=...)`
    (the only attributes `_catches` reads), and assert `game._catches(fake_ghost)` for the same-tile, diagonal
    corner-kiss, and 15px/16px boundary cases above; reference `GHOST_CATCH_DISTANCE` from settings to derive
    the boundary coordinates. For FAIR-02: set `game.powerup = False`, leave all `*_dead`/`eaten_ghost` False,
    set `game.moving = True` and `game.eat_freeze = False`, call `game.update_ghost_speeds()` 20 times collecting
    `game.ghost_speeds[0]` each call, and assert every step is in {1,2} and `sum(steps) == GHOST_CHASE_SPEED_NUM`.
    Decorate every NEW-behavior test with `@pytest.mark.xfail(strict=True, reason="RED until FAIR-0X - Plan 08-02, Wave 2")`
    (catch tests reference FAIR-01; the accumulator test references FAIR-02). Pure integer math only;
    no `math.sqrt`, no float, no ghost.py import.
  </action>
  <acceptance_criteria>
    - tests/test_fairness_unit.py exists and imports `types`, `pygame`, `pytest`, `Game`, and `GHOST_CATCH_DISTANCE`/`GHOST_CHASE_SPEED_NUM` from settings
    - Every NEW-behavior test is decorated `@pytest.mark.xfail(strict=True, ...)`
    - `.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -q` exits 0 with all NEW-behavior tests reported xfailed (none xpassed) against today's game.py
    - `grep -c "math.sqrt" tests/test_fairness_unit.py` is 0 (squared-distance only)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_fairness_unit.py -q -rxX</automated>
  </verify>
  <done>The catch-helper and accumulator unit tests are collected and reported xfailed (not xpassed) against today's game.py; the suite is green; no float/sqrt is used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local keyboard -> game sim | The only untrusted input is local keystrokes, already handled in `handle_events`. This plan adds no new boundary (config constants + tests only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | new settings constants / sim math | accept | Offline single-player local math; no network/persistence/external input added. No new attack surface. |
| T-08-02 | Tampering | determinism of the regression net | mitigate | New constants are plain integers, no new import / no `random`/wall-clock token; `tests/test_determinism_guard.py` stays green. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Phase installs no external packages; edits first-party `settings.py` + adds test files only. No registry/slopcheck surface. |

No high-severity threats. This is offline, local, single-player gameplay-tuning config + tests with no new attack surface.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_player_micro.py tests/test_fairness_unit.py -q -rxX` exits 0 (baselines pass; NEW-behavior tests xfailed, none xpassed).
- `.venv/Scripts/python.exe -m pytest tests/test_determinism_guard.py -q` exits 0 (settings change added no forbidden token).
- `.venv/Scripts/python.exe -c "import settings; assert settings.PLAYER_SPEED == 2"` exits 0.
</verification>

<success_criteria>
- settings.py exposes GHOST_CATCH_DISTANCE, GHOST_CHASE_SPEED_NUM, GHOST_CHASE_SPEED_DEN, PLAYER_TURN_WINDOW_MARGIN as integers; PLAYER_SPEED unchanged.
- tests/test_player_micro.py and tests/test_fairness_unit.py exist, are collected, and keep the full suite green (NEW-behavior assertions xfailed, ready to go green in Wave 2).
- Determinism guard still green; no float/sqrt/new-import introduced.
</success_criteria>

<output>
Create `.planning/phases/08-fairness-pass/08-01-SUMMARY.md` when done.
</output>
