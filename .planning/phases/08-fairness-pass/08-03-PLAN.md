---
phase: 08-fairness-pass
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - player.py
  - tests/test_player_micro.py
autonomous: true
requirements: [FAIR-03]
must_haves:
  truths:
    - "A queued player turn registers ~4-6px before the junction via the widened pre-turn window (FAIR-03)"
    - "The ghost `% TILE` windows are byte-identical - only the PLAYER path widens (D-09/D-15, core-value guard)"
  artifacts:
    - path: "player.py"
      provides: "widened pre-turn cornering windows (all four sites)"
      contains: "PLAYER_TURN_WINDOW_MARGIN"
  key_links:
    - from: "player.py check_position"
      to: "settings.PLAYER_TURN_WINDOW_MARGIN"
      via: "all four `12 <= center % TILE <= 18` windows widened to (12 - MARGIN) .. (18 + MARGIN)"
      pattern: "PLAYER_TURN_WINDOW_MARGIN"
---

<objective>
Implement FAIR-03 entirely within `player.py`: widen the four player-only `12 <= center % TILE <= 18`
turn windows in `check_position` to `(12 - PLAYER_TURN_WINDOW_MARGIN) <= center % TILE <= (18 + PLAYER_TURN_WINDOW_MARGIN)`
so a queued turn registers a few pixels early and rounds the corner smoothly instead of overshooting
(D-09, symmetric widening). Then flip the FAIR-03 cornering test from xfail to passing.

Purpose: the player can input a turn a touch early and still land it - the "snappy / arcade-faithful"
cornering feel. This is a PLAYER affordance only; the look-alike ghost windows (`ghost.py:343,352,363,372`)
stay byte-identical (D-15 + the core-value guard - they must NOT be merged or touched).

Output: `player.py` with all four widened windows; `tests/test_player_micro.py` widened-case test passing.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fairness-pass/08-CONTEXT.md
@.planning/phases/08-fairness-pass/08-RESEARCH.md
@.planning/phases/08-fairness-pass/08-PATTERNS.md
@player.py
@settings.py
@.planning/phases/08-fairness-pass/08-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols (exclude from drift/source-grounding - they do not exist before Phase 8):
- Consumes (from Plan 08-01): `settings.PLAYER_TURN_WINDOW_MARGIN`
- `ghost.py` is intentionally NOT modified by this plan (byte-identical guard)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FAIR-03 - widen the four player-only pre-turn windows</name>
  <files>player.py, tests/test_player_micro.py</files>
  <read_first>
    - player.py (check_position 43-91; the FOUR `12 <= ... <= 18` windows at lines 65, 70, 76, 81; the `from settings import (...)` block lines 3-7; the inner wall guard `level[...] < 3` on each branch)
    - settings.py (PLAYER_TURN_WINDOW_MARGIN = 6 from Plan 08-01)
    - ghost.py (the look-alike windows at lines 343, 352, 363, 372 - READ ONLY; D-15 forbids touching or merging these)
    - tests/test_player_micro.py (the xfail widened-case test to flip; the green baseline to keep green)
    - .planning/phases/08-fairness-pass/08-RESEARCH.md (Pattern 3 symmetric widening; "Do NOT factor into a shared helper with Ghost.check_collisions")
    - .planning/phases/08-fairness-pass/08-PATTERNS.md (player.py check_position widening; import pattern to extend)
  </read_first>
  <behavior>
    - At a junction position whose travel-axis residue is 8 (inside the widened band 6..24 but outside the legacy 12..18), the queued perpendicular turn becomes available - registers ~6px early (D-09).
    - The original at-junction behavior still holds (residue 15 still turns) - widening only enlarges the timing window.
    - The inner wall guard (`level[...] < 3`) still gates every turn -> no illegal turn into a wall is enabled, only the timing window widens.
    - Ghost windows are unchanged (D-15) - this is player-only.
  </behavior>
  <action>
    Add `PLAYER_TURN_WINDOW_MARGIN` to player.py's existing `from settings import (...)` block. Replace each
    of the four `12 <= centerx % TILE_WIDTH <= 18` / `12 <= centery % TILE_HEIGHT <= 18` window expressions
    (player.py lines 65, 70, 76, 81) with the symmetric widened form
    `(12 - PLAYER_TURN_WINDOW_MARGIN) <= centerx % TILE_WIDTH <= (18 + PLAYER_TURN_WINDOW_MARGIN)` (and the
    centery/TILE_HEIGHT variants) per D-09 / RESEARCH Pattern 3. Change ONLY the window bounds - leave the
    inner `level[...] < 3` wall guards and the turn-setting bodies exactly as they are. Do NOT touch ghost.py
    and do NOT extract a shared helper with Ghost.check_collisions (D-15 keeps them deliberately divergent).
    After implementing, remove the `@pytest.mark.xfail` marker from `test_cornering_preturn_widened` in
    tests/test_player_micro.py so it reports passed.
  </action>
  <acceptance_criteria>
    - player.py imports `PLAYER_TURN_WINDOW_MARGIN` from settings
    - `grep -c "PLAYER_TURN_WINDOW_MARGIN" player.py` is >= 4 (all four windows widened)
    - `grep -c "12 <= center" player.py` is 0 (no legacy hard-coded window bound remains in player.py)
    - `test_cornering_preturn_widened` no longer carries `@pytest.mark.xfail` and reports passed; the baseline case still passes
    - `.venv/Scripts/python.exe -m pytest tests/test_player_micro.py -q` exits 0 with 2 passed (no xfail/xpass)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_player_micro.py tests/test_ghost_micro.py tests/test_determinism_guard.py -q -rxX</automated>
  </verify>
  <done>All four player windows widened by PLAYER_TURN_WINDOW_MARGIN; wall guards intact; the widened cornering test passes with xfail removed; ghost windows + ghost micro-tests + determinism guard byte-identical/green; ghost.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local keyboard -> game sim | Only untrusted input is local keystrokes (already handled). This plan widens a local timing window in player movement math; no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | player turn-window math | accept | Offline single-player local math; no network/persistence/external input. The widened window still passes through the `level[...] < 3` wall guard, so no illegal movement is enabled. |
| T-08-02 | Tampering | determinism of the regression net | mitigate | Widening uses an integer constant only - no new import, no float, no `random`/wall-clock token; `tests/test_determinism_guard.py` stays green. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Phase installs no external packages; edits first-party player.py + test file only. No registry/slopcheck surface. |

No high-severity threats. Offline, local, single-player movement math; no new attack surface.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_player_micro.py -q -rxX` exits 0 with 2 passed (baseline + widened).
- `.venv/Scripts/python.exe -m pytest tests/test_ghost_micro.py tests/test_determinism_guard.py -q` exits 0 (ghost windows byte-identical; determinism intact).
- `git diff --stat ghost.py` shows ZERO changes to ghost.py.
</verification>

<success_criteria>
- FAIR-03: all four player-only turn windows widened by PLAYER_TURN_WINDOW_MARGIN; a queued turn registers ~4-6px early; wall guards intact.
- Ghost windows unchanged (D-15); ghost micro-tests + determinism guard green (criterion 4 proof).
</success_criteria>

<output>
Create `.planning/phases/08-fairness-pass/08-03-SUMMARY.md` when done.
</output>
