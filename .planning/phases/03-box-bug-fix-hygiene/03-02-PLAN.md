---
phase: 03-box-bug-fix-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - geometry.py
  - game.py
  - ghost.py
  - tests/test_box_bounds_oracle.py
  - tests/golden/box_edge/trace.jsonl
  - tests/golden/box_exit/trace.jsonl
autonomous: false
requirements: [BUG-01]
user_setup: []

must_haves:
  truths:
    - "geometry.py exposes a single GHOST_BOX_BOUNDS = (350, 550, 360, 480); the two old constants are gone"
    - "game.py:get_targets and ghost.py:check_collisions both import and use GHOST_BOX_BOUNDS (no GHOST_BOX_BOUNDS_TARGET or _COLLISION remain anywhere)"
    - "The differential oracle proves get_targets old-vs-new differs ONLY in the ring (in TARGET, not in COLLISION) and is identical everywhere else"
    - "The belt-check proves check_collisions is byte-identical old-vs-new across the enumerated state space (name-only rename)"
    - "The teeth-check confirms the oracle goes RED on an out-of-ring perturbation, then green again; oracle + belt-check are deleted in the same commit as the fix"
    - "Golden traces move ONLY at box-edge frames (re-blessed in Linux CI); the rest of every trace is byte-identical; 15 micro-tests stay green untouched"
    - "Claude adversarial playtest finds no box-exit soft-locks / pen-mouth oscillation / wall-clips / eaten-eyes glitches; human before/after GIF confirms the intended change"
  artifacts:
    - path: "geometry.py"
      provides: "Unified GHOST_BOX_BOUNDS constant + reconciled docstring"
      contains: "GHOST_BOX_BOUNDS = (350, 550, 360, 480)"
    - path: "game.py"
      provides: "get_targets repointed to GHOST_BOX_BOUNDS (the behavior-delta site)"
      contains: "GHOST_BOX_BOUNDS"
    - path: "ghost.py"
      provides: "check_collisions repointed to GHOST_BOX_BOUNDS (value unchanged)"
      contains: "GHOST_BOX_BOUNDS"
  key_links:
    - from: "game.py:get_targets (8 call sites)"
      to: "geometry.GHOST_BOX_BOUNDS"
      via: "in_box(ghost_x, ghost_y, GHOST_BOX_BOUNDS)"
      pattern: "in_box\\(.*GHOST_BOX_BOUNDS\\)"
    - from: "ghost.py:check_collisions"
      to: "geometry.GHOST_BOX_BOUNDS"
      via: "in_box(self.x_pos, self.y_pos, GHOST_BOX_BOUNDS) — value (350,550,360,480) unchanged"
      pattern: "in_box\\(self\\.x_pos, self\\.y_pos, GHOST_BOX_BOUNDS\\)"
    - from: "tests/golden/box_edge + box_exit trace.jsonl"
      to: "per-frame target tuple"
      via: "Linux-CI --bless re-record of ONLY box-edge frames"
      pattern: "target"
---

<objective>
Land BUG-01 — the ONE sanctioned behavior change of the entire milestone — as the final, isolated commit: collapse geometry.py's two box constants into a single `GHOST_BOX_BOUNDS = (350, 550, 360, 480)` (collision box wins, D-01) and repoint both importers, so `game.py:get_targets` shifts to the tighter box (the ONLY value change, D-02) while `ghost.py:check_collisions` is byte-identical by construction (name-only rename, value unchanged). Prove the change is isolated to the box ring with "maximum paranoia": a get_targets differential oracle (D-03) + check_collisions belt-check (D-04) + teeth-check then one-shot delete (D-05), golden re-bless of ONLY box-edge frames in Linux CI (D-06), a required Claude adversarial playtest (D-07), and a human before/after GIF gate (D-08).

Purpose: Unify the two inconsistent ghost-box rectangles into one constant, provably confining the behavior delta to the box region and proving the rest of the codebase is untouched. This is BUG-01 and the milestone's only sanctioned behavior change.

Output: Unified GHOST_BOX_BOUNDS constant + repointed importers; re-blessed box_edge/box_exit golden traces (box-edge frames only); a transient oracle/belt-check (created, proven, deleted in the fix commit); playtest + GIF verification.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md
@.planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md
@.planning/phases/02-safe-refactor/02-02-SUMMARY.md
@geometry.py
@tests/test_ghost_micro.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the get_targets differential oracle + check_collisions belt-check + teeth-check (D-03/D-04/D-05)</name>
  <files>tests/test_box_bounds_oracle.py</files>
  <behavior>
    - get_targets oracle: across the enumerated space, OLD (TARGET box (340,560,340,500)) and NEW (COLLISION box (350,550,360,480)) targeting outputs differ ONLY for ghost positions IN the ring (inside TARGET, outside COLLISION) and are IDENTICAL for every position outside the ring.
    - check_collisions belt-check: across the same enumerated space, OLD and NEW check_collisions in_box output is byte-identical (proving the name-only rename leaves it unchanged).
    - teeth-check: a deliberate perturbation of get_targets OUTSIDE the ring makes the oracle FAIL (RED); reverting makes it pass — proving the oracle has teeth.
  </behavior>
  <read_first>
    - tests/test_ghost_micro.py:27-57 (the module-scoped headless `screen` fixture + `make_ghost` harness — copy.deepcopy(board.boards) per case, Pitfall 4)
    - tests/test_golden_traces.py:62-66 (`_new_game()` — headless Game construction; get_targets is a Game method, not a Ghost method)
    - game.py:240-302 (get_targets: 8 `in_box(..., GHOST_BOX_BOUNDS_TARGET)` call sites across powerup + non-powerup branches, for blinky/inky/pinky/clyde)
    - ghost.py:384 (check_collisions box test using GHOST_BOX_BOUNDS_COLLISION)
    - geometry.py:18-36 (the two constants + the in_box predicate)
    - .planning/phases/02-safe-refactor/02-02-SUMMARY.md (the deleted Phase-2 oracle: frozen-legacy + itertools.product exhaustive enumeration, old-vs-new assertion, mutation-canary)
    - .planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md (test_box_bounds_oracle.py section — the highest-value map)
  </read_first>
  <action>
    Create tests/test_box_bounds_oracle.py (NEW, temporary — deleted in Task 2). Mirror the deleted Phase-2 oracle pattern (frozen-legacy + itertools.product). Construct subjects headless: a `Game` via the `_new_game()` pattern (tests/test_golden_traces.py:62-66) for get_targets, and ghosts via `make_ghost` (tests/test_ghost_micro.py:41-57) for check_collisions — always `copy.deepcopy(board.boards)` per case.

    get_targets differential oracle (D-03): Freeze the OLD targeting logic as a local copy that uses the TARGET box `(340,560,340,500)` and the NEW logic that uses the COLLISION box `(350,550,360,480)`. Enumerate with itertools.product over ghost position × board state × `dead`/`eaten_ghost`/`powerup` (D-03). The position grid MUST densely cover the ring — x near {340,350,550,560}, y near {340,360,480,500} — plus a coarse grid well inside COLLISION and well outside TARGET. For each enumerated state, compute OLD and NEW get_targets output per ghost and assert: outputs DIFFER only when the ghost position is in the ring (in TARGET, not in COLLISION), and are IDENTICAL otherwise. Player position handling: hold player at a fixed off-box position (and optionally a second position) so the "chase player" branch is distinguishable from SCATTER_EATEN_TARGET (Claude's Discretion on exact grid bounds — mirror Phase-2 D-05/D-07 construction).

    check_collisions belt-check (D-04): In the SAME file, across the same position grid, assert OLD (GHOST_BOX_BOUNDS_COLLISION) and NEW (GHOST_BOX_BOUNDS, same value) check_collisions `in_box` flag are byte-identical — mechanically proving the name-only rename (D-02 guardrail).

    teeth-check (D-05): Include a clearly-labelled mutation-canary test (or documented procedure) that perturbs get_targets OUTSIDE the ring and asserts the oracle goes RED, then reverts — confirming the oracle is not vacuously green. Reference the Phase-2 precedent (flipped `g.x_pos += g.speed` → `-= g.speed`, confirmed RED, reverted).

    NO fenced code in this action — replicate the cited analog files. Do NOT modify game.py/ghost.py/geometry.py in this task (the oracle freezes BOTH old and new logic locally so it can run before the repoint). Do NOT touch tests/test_ghost_micro.py (the measuring instrument stays untouched).
  </action>
  <verify>
    <automated>pytest -q tests/test_box_bounds_oracle.py && pytest -q tests/test_ghost_micro.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_box_bounds_oracle.py exists and `pytest tests/test_box_bounds_oracle.py` exits 0 (oracle + belt-check GREEN)
    - The oracle enumerates ring positions (x near {340,350,550,560}, y near {340,360,480,500}) — `grep -Eq '340|350|550|560' tests/test_box_bounds_oracle.py`
    - The belt-check asserts check_collisions old-vs-new byte-identical (file references both GHOST_BOX_BOUNDS values / the COLLISION value (350,550,360,480))
    - A teeth-check/mutation-canary is present and documented (file references a deliberate perturbation that makes the oracle RED)
    - `pytest tests/test_ghost_micro.py` exits 0 — the 15 micro-tests are untouched and green
    - game.py, ghost.py, geometry.py are UNMODIFIED in this task (`git status --porcelain geometry.py game.py ghost.py` returns empty)
  </acceptance_criteria>
  <done>The differential oracle (get_targets ring-only-diff) + belt-check (check_collisions byte-identical) + teeth-check are green; production code and micro-tests untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify GHOST_BOX_BOUNDS + repoint importers + prove teeth + delete oracle (BUG-01, D-01/D-02/D-05/D-06)</name>
  <files>geometry.py, game.py, ghost.py, tests/test_box_bounds_oracle.py, tests/golden/box_edge/trace.jsonl, tests/golden/box_exit/trace.jsonl</files>
  <behavior>
    - After the repoint, `pytest tests/test_box_bounds_oracle.py` is GREEN (the NEW get_targets matches the oracle's NEW-logic expectations and the belt-check holds).
    - The teeth-check is exercised once on the live code (perturb get_targets out-of-ring → oracle RED → revert → green) before deletion.
    - After re-bless, golden traces move ONLY at box-edge frames; every other frame of every trace is byte-identical; the 15 micro-tests stay green.
  </behavior>
  <read_first>
    - geometry.py:1-36 (the two constants, the module docstring at lines 7-14 that says "Do NOT merge these" / "D-15 landmine" — now contradicted by the fix, reconcile it; the in_box predicate at 33-36 is UNCHANGED)
    - game.py:18 (import `from geometry import in_box, GHOST_BOX_BOUNDS_TARGET`) + game.py:240-302 (8 call sites)
    - ghost.py:4 (import `from geometry import in_box, GHOST_BOX_BOUNDS_COLLISION`) + ghost.py:384 (single call site)
    - tests/test_box_bounds_oracle.py (the oracle from Task 1 — proven green, deleted here)
    - tests/test_golden_traces.py:188-212 (the `--bless` branch — re-records trace.jsonl and prints the diff; do NOT assert in bless mode)
    - tests/golden/box_edge/trace.jsonl, tests/golden/box_exit/trace.jsonl (the box scenarios whose per-frame `target` tuple shifts)
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-01 collision box wins; D-02 game.py is the only value change; D-05 teeth-then-delete; D-06 Linux-CI re-bless box-edge frames only)
  </read_first>
  <action>
    This is the ONE isolated, trace-touching commit (D-09). Perform in order:

    (1) geometry.py: Collapse the two constants into a single `GHOST_BOX_BOUNDS = (350, 550, 360, 480)` (the COLLISION value wins, D-01) — remove both `GHOST_BOX_BOUNDS_COLLISION` and `GHOST_BOX_BOUNDS_TARGET`. Leave the `in_box(x, y, bounds)` predicate (lines 33-36) UNCHANGED — it already takes bounds as a parameter. Reconcile the module docstring (lines 7-14): the current prose ("intentionally kept as TWO DISTINCT named constants ... Do NOT merge these" + the "D-15 landmine" line) now contradicts the fix — rewrite it to describe the single unified constant. Per Claude's Discretion, add a one-line comment at GHOST_BOX_BOUNDS noting the historical divergence (was TARGET (340,560,340,500) vs COLLISION (350,550,360,480); unified onto collision per BUG-01/D-01) to guard against a future accidental re-split.

    (2) game.py: Repoint import at line 18 `from geometry import in_box, GHOST_BOX_BOUNDS_TARGET` → `from geometry import in_box, GHOST_BOX_BOUNDS`. Replace every `GHOST_BOX_BOUNDS_TARGET` in get_targets (the 8 call sites, game.py:240-302) with `GHOST_BOX_BOUNDS`. This is THE behavior-delta site (D-02): the tighter box means an eaten/returning ghost in the ring switches from "aim at gate (SCATTER_EATEN_TARGET)" to "chase player" a touch sooner.

    (3) ghost.py: Repoint import at line 4 `from geometry import in_box, GHOST_BOX_BOUNDS_COLLISION` → `from geometry import in_box, GHOST_BOX_BOUNDS`. Rename the single call site at ghost.py:384 from `GHOST_BOX_BOUNDS_COLLISION` to `GHOST_BOX_BOUNDS`. Value is identical (350,550,360,480) → name-only rename → in_box/movement/box-exit byte-identical by construction.

    (4) Teeth-check (D-05): Run the oracle against the live repointed code (`pytest tests/test_box_bounds_oracle.py` → green). Then exercise the teeth-check ONCE: perturb get_targets OUTSIDE the ring, confirm the oracle goes RED, revert, confirm green, confirm `git diff` is empty.

    (5) One-shot delete (D-05/D-06): Delete tests/test_box_bounds_oracle.py — the re-blessed golden traces + 15 micro-tests are the permanent guard. The oracle is transient by design (Phase-2 one-shot-then-delete lifecycle).

    (6) Re-bless (D-06): Run the `--bless` flow for ONLY the `box_edge` and `box_exit` scenarios to re-record their trace.jsonl. CRITICAL: re-bless ONLY in Linux CI — NEVER bless on Windows (platform pixel/float differences; the frame-hash manifest is a Windows placeholder). Inspect the bless diff and confirm it shows ONLY box-edge frames change (the per-frame `target` tuple shifts); every non-box-edge frame and every other scenario stays byte-identical.

    Commit everything (geometry + game + ghost + oracle deletion + re-blessed traces) as the SINGLE final isolated commit (suggested message `fix(03): unify ghost-box bounds onto GHOST_BOX_BOUNDS, isolated to box region (BUG-01)`). This is the only Phase-3 commit that moves golden traces.
  </action>
  <verify>
    <automated>grep -Eq 'GHOST_BOX_BOUNDS = \(350, 550, 360, 480\)' geometry.py && [ "$(grep -rc 'GHOST_BOX_BOUNDS_TARGET\|GHOST_BOX_BOUNDS_COLLISION' geometry.py game.py ghost.py | grep -v ':0' | wc -l)" -eq 0 ] && test ! -f tests/test_box_bounds_oracle.py && pytest -q tests/test_ghost_micro.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GHOST_BOX_BOUNDS = (350, 550, 360, 480)' geometry.py` returns 1 (unified constant present)
    - `grep -rc 'GHOST_BOX_BOUNDS_TARGET' geometry.py game.py ghost.py` returns 0 everywhere (old TARGET name gone)
    - `grep -rc 'GHOST_BOX_BOUNDS_COLLISION' geometry.py game.py ghost.py` returns 0 everywhere (old COLLISION name gone)
    - game.py imports and uses `GHOST_BOX_BOUNDS` (8 call sites) — `grep -c 'GHOST_BOX_BOUNDS' game.py` >= 9 (import + 8 sites)
    - ghost.py:384 uses `in_box(self.x_pos, self.y_pos, GHOST_BOX_BOUNDS)` — value unchanged
    - geometry.py docstring no longer says "Do NOT merge these" — `grep -c 'Do NOT merge these' geometry.py` returns 0
    - `test ! -f tests/test_box_bounds_oracle.py` succeeds (oracle deleted in this commit, D-06 lifecycle)
    - `pytest tests/test_ghost_micro.py` exits 0 — 15 micro-tests stay green untouched (D-02 measuring instrument)
    - Bless diff (Linux CI) shows ONLY box_edge/box_exit box-edge frames change; all other scenarios byte-identical (`pytest tests/test_golden_traces.py` green after re-bless)
  </acceptance_criteria>
  <done>GHOST_BOX_BOUNDS unified; both importers repointed; get_targets shifted (8 sites), check_collisions byte-identical; oracle teeth-checked then deleted; box_edge/box_exit re-blessed in Linux CI (box-edge frames only); 15 micro-tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Claude adversarial playtest + human before/after GIF gate (D-07/D-08)</name>
  <files>tests/artifacts/ (montages + GIF, regenerated — not committed)</files>
  <read_first>
    - .planning/phases/01-test-safety-net/01-CONTEXT.md (HRN-03 frame/montage/GIF capture; HRN-04 Claude observe→decide→act play-loop)
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-07 required playtest; D-08 human GIF gate)
    - tests/test_golden_traces.py (the box_edge/box_exit scenarios re-blessed in Task 2 — source frames for the montage)
  </read_first>
  <what-built>
    Task 2 unified GHOST_BOX_BOUNDS (BUG-01) and re-blessed ONLY the box-edge frames of box_edge/box_exit. The oracle proved the get_targets delta is confined to the ring and check_collisions is byte-identical; the rest of every golden trace is unchanged. This checkpoint confirms the EMERGENT play feel the oracle/traces cannot model: (a) Claude runs the HRN-04 adversarial playtest, (b) the human watches a before/after GIF.
  </what-built>
  <action>
    AUTOMATED portion (before the human gate): Run the required Claude adversarial playtest (D-07) — drive the game via the HRN-04 observe→decide→act play-loop in a live session, specifically hunting near the box: box-exit soft-locks, ghosts oscillating at the pen mouth, wall-clips near the box, and eaten-eyes targeting glitches (the emergent dynamics a real intentional edge change newly makes possible, which Phase 2 could skip because behavior was provably identical there). Capture before/after montages (HRN-03) of the box region for Claude's own vision review, and assemble a before/after canonical-playthrough GIF for the human (reuse the Phase-1 HRN-03 montage/GIF path). Claude reviews the montages with its own vision and confirms the box change looks intended and nothing else moved. THEN pause for the human gate.
  </action>
  <how-to-verify>
    Human (D-08, human_verify_mode = end-of-phase):
    1. Watch the before/after canonical-playthrough GIF produced above.
    2. Confirm the ONLY visible difference is the box-region behavior (an eaten/returning ghost in the ring switches from "aim at gate" to "chase player" a touch sooner) — i.e. the intended BUG-01 change.
    3. Confirm NOTHING ELSE moved — ghost personalities, Pac-Man, scoring, and non-box behavior all look identical to before.
    4. Confirm no soft-lock, oscillation, wall-clip, or eaten-eyes glitch is visible near the box.
  </how-to-verify>
  <verify>
    <human-check>Claude playtest reports no box-exit soft-lock / pen-mouth oscillation / wall-clip / eaten-eyes glitch; human confirms the before/after GIF shows ONLY the intended box-region change and nothing else moved</human-check>
  </verify>
  <acceptance_criteria>
    - Claude adversarial playtest (HRN-04) completes a full session near the box and reports NO box-exit soft-lock, pen-mouth oscillation, wall-clip, or eaten-eyes glitch
    - Before/after box-region montages exist for Claude vision review; Claude confirms the change is confined to the box and intended
    - Before/after canonical-playthrough GIF exists for the human
    - Human confirms the GIF shows ONLY the intended box-region behavior change and nothing else moved (D-08)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the playtest is clean and the GIF confirms the intended box-only change, or describe any soft-lock / unintended movement / glitch observed.</resume-signal>
  <done>Claude adversarial playtest finds no box-region glitches; human-verified before/after GIF confirms BUG-01 is the only visible change.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols and files this plan introduces (for the source-grounding drift-exclusion pass):
- **`GHOST_BOX_BOUNDS = (350, 550, 360, 480)`** in geometry.py — NEW unified constant (replaces the deleted `GHOST_BOX_BOUNDS_COLLISION` and `GHOST_BOX_BOUNDS_TARGET`). Both are REMOVED by this plan; do not flag their disappearance as drift.
- **`tests/test_box_bounds_oracle.py`** — NEW, TRANSIENT test file containing the get_targets differential oracle, the check_collisions belt-check, and the teeth-check/mutation-canary. Created in Task 1, proven green and DELETED in Task 2 (D-05/D-06 one-shot-then-delete lifecycle). It does NOT exist at end of phase by design — do not flag its absence as missing.
- Re-blessed `tests/golden/box_edge/trace.jsonl` and `tests/golden/box_exit/trace.jsonl` — box-edge frames re-recorded (Linux CI only).
- Regenerated `tests/artifacts/` montages + GIF (not committed — gitignored per Phase-1 layout).
Imports repointed (not new symbols): game.py:18 and ghost.py:4 now import `GHOST_BOX_BOUNDS`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

BUG-01 is a local-only constant-collapse + import-repoint in the ghost-targeting code path. It introduces NO new input, network, auth, serialization, or data-handling surface — it changes one geometry constant's value at one function and renames it at another. The oracle/belt-check are test-only code deleted before the phase closes.

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary — ghost targeting reads in-process game state only; no external/untrusted input crosses any boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `game.py:get_targets` (the sanctioned behavior delta) | mitigate | The risk is an UNINTENDED behavior change outside the box ring. Mitigated by the maximum-paranoia stack: differential oracle proves the delta is ring-only (D-03), belt-check proves check_collisions byte-identical (D-04), teeth-check proves the oracle is not vacuously green (D-05), golden re-bless moves ONLY box-edge frames (D-06), 15 micro-tests stay green, plus the Claude playtest (D-07) + human GIF (D-08). |
| T-03-05 | Tampering | test-only oracle code (`tests/test_box_bounds_oracle.py`) | accept | Transient test code, never shipped, deleted in the fix commit (D-06). No production-runtime surface. No packages installed. Low risk. |

**Block-on:high gate: SATISFIED — no high-severity threats.** BUG-01 is a local refactor with no input/network/auth surface. The only meaningful risk (unintended behavior drift) is mitigated by the four-way verification bar; T-03-05 is test-only/transient. No blockers.
</threat_model>

<verification>
- Oracle + belt-check green (Task 1), then green against live repointed code (Task 2) — proving get_targets ring-only-diff AND check_collisions byte-identical across the enumerated input space.
- Teeth-check: out-of-ring perturbation makes the oracle RED, revert restores green (D-05) — the oracle has teeth.
- `pytest tests/test_ghost_micro.py` exits 0 — 15 micro-tests untouched and green (D-02 measuring instrument).
- Golden re-bless (Linux CI ONLY, never Windows): bless diff shows ONLY box_edge/box_exit box-edge frames change (per-frame `target` tuple); every other frame/scenario byte-identical (D-06).
- Oracle deleted in the fix commit (`test ! -f tests/test_box_bounds_oracle.py`) — re-blessed traces + micro-tests are the permanent guard.
- Claude adversarial playtest clean near the box (D-07); human before/after GIF confirms intended box-only change (D-08).
- CI (ubuntu, pinned, headless) green on the final commit.
</verification>

<success_criteria>
- The two inconsistent box definitions are unified into a single `GHOST_BOX_BOUNDS` used by both get_targets and check_collisions (BUG-01 SC#1).
- The behavior change is provably isolated to the box region: golden traces move ONLY at box edges, those frames re-blessed in Linux CI, before/after montages + GIF confirm visually, rest of every trace verified untouched (BUG-01 SC#2).
- get_targets is the only value change (8 sites); check_collisions is byte-identical by construction; 15 micro-tests stay green untouched (D-02).
- Oracle + belt-check + teeth-check prove isolation across the whole input space, then are deleted in the fix commit (D-03/D-04/D-05/D-06).
- This is the SINGLE trace-touching commit, landing after all four hygiene commits (D-09 — enforced by depends_on: 03-01).
</success_criteria>

<output>
Create `.planning/phases/03-box-bug-fix-hygiene/03-02-SUMMARY.md` when done.
</output>
