---
phase: 03-box-bug-fix-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - CLAUDE.md
  - menu.py
  - assets/ghost_images/
  - assets/player_images/
autonomous: false
requirements: [HYG-01, HYG-02, HYG-03, HYG-04]
user_setup: []

must_haves:
  truths:
    - "Client deps pygame and pyinstaller are pinned with == in requirements.txt; backend cloud_functions pins untouched"
    - ".claude/settings.local.json is no longer tracked by git; .gitignore has a single /.claude line and no CLAUDE.md line"
    - "CLAUDE.md is tracked by git and its Ghost Box Exit prose reads ~0.5 s / ~1 s for Pinky/Clyde"
    - "menu.py:run_main_menu docstring no longer references 'Change Initials'"
    - "assets/ghost_images/ and assets/player_images/ no longer exist; the rebuilt .exe still loads assets and runs"
    - "Golden traces stay byte-identical and CI is green after every hygiene commit (zero behavior change)"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned client dependency versions"
      contains: "pygame=="
    - path: ".gitignore"
      provides: "Reconciled ignore rules (single /.claude, no CLAUDE.md)"
    - path: "CLAUDE.md"
      provides: "Tracked project-instructions doc with corrected box-exit timing"
    - path: "menu.py"
      provides: "Corrected run_main_menu docstring"
  key_links:
    - from: "CLAUDE.md Ghost Box Exit prose"
      to: "settings.py BOX_EXIT_DELAY_PINKY=30 / CLYDE=60"
      via: "doc reconciliation (60fps frame->seconds conversion)"
      pattern: "0\\.5|~?0\\.5\\s*s"
    - from: "build.py --add-data=assets;assets"
      to: "assets/ (ghosts/, pacman/ only)"
      via: "PyInstaller bundle after dead-folder deletion"
      pattern: "add-data=assets"
---

<objective>
Land the four behavior-neutral hygiene items (HYG-01..04) as four ordered, atomic, independently-green commits — deps pin, untrack+gitignore, doc drift, dead assets — per D-09 (hygiene FIRST). Every commit MUST keep golden traces byte-identical and CI green. This plan touches NO gameplay logic, NO geometry constants, and NO golden traces; the box-fix (BUG-01) is isolated to Plan 02 and runs in a later wave.

Purpose: Clean the repo's dependency/tracking/doc/asset hygiene without any behavior change, so the Phase-3 PR reads "everything green and unchanged, then ONE commit that moves exactly the box-edge frames" (Plan 02). Sequencing the hygiene first and behavior-neutral is the Cardinal Rule applied at commit granularity (D-09).

Output: Pinned requirements.txt; reconciled .gitignore + untracked settings.local.json; tracked + corrected CLAUDE.md; corrected menu.py docstring; deleted dead asset folders; a human-verified .exe rebuild smoke-run (D-14).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md
@.planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin client dependencies in requirements.txt (HYG-01, D-10)</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current state: two unpinned lines `pygame`, `pyinstaller`)
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-10 — exact `==` pins, CI-green versions, backend untouched)
    - .planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md (requirements.txt section — pin both, source via pip freeze)
  </read_first>
  <action>
    Replace the two unpinned client lines in requirements.txt with exact `==` pins for `pygame` and `pyinstaller`. Per D-10, source the exact version strings from the CI-green environment: run `pip freeze` (or `pip show pygame pyinstaller`) in the active venv and pin to the resolved installed versions, e.g. `pygame==X.Y.Z` and `pyinstaller==X.Y.Z` where X.Y.Z is the actually-installed version (Claude's Discretion on the exact strings — they MUST match what the CI/dev env resolves). Pin pyinstaller too so the `.exe` bundle stays reproducible. Do NOT touch any `cloud_functions/*/requirements.txt` backend pins (`3.*`/`6.*`) — HYG-01 is client-scope only. Do NOT add, remove, or reorder any other lines. This is a config-only change with zero runtime/behavior effect. Commit as the first atomic hygiene commit (suggested message `chore(03): pin client deps in requirements.txt (HYG-01)`).
  </action>
  <verify>
    <automated>grep -Eq '^pygame==[0-9]' requirements.txt && grep -Eq '^pyinstaller==[0-9]' requirements.txt && pytest -q tests/test_golden_traces.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pygame==' requirements.txt` returns 1 and the pinned version is numeric (matches `^pygame==[0-9]`)
    - `grep -c '^pyinstaller==' requirements.txt` returns 1 and matches `^pyinstaller==[0-9]`
    - No `cloud_functions` requirements file is modified (`git status --porcelain cloud_functions/` returns empty)
    - `pytest tests/test_golden_traces.py` exits 0 (golden traces byte-identical — no behavior change)
  </acceptance_criteria>
  <done>requirements.txt has exact `==` pins for pygame and pyinstaller sourced from the CI-green env; backend pins untouched; golden traces unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack settings.local.json + reconcile .gitignore + track CLAUDE.md (HYG-02, D-11)</name>
  <files>.gitignore, .claude/settings.local.json, CLAUDE.md</files>
  <read_first>
    - .gitignore (current lines 23-26: `/.vscode`, duplicate `/.claude` ×2, `CLAUDE.md`)
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-11 — git rm --cached, dedupe /.claude, remove CLAUDE.md line, track CLAUDE.md)
    - .planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md (.gitignore section — exact lines 23-26)
  </read_first>
  <action>
    Three coupled edits, one atomic commit (D-11):
    (1) `git rm --cached .claude/settings.local.json` — stop tracking the local settings file (currently tracked; confirmed via `git ls-files`). The file stays on disk; only the index entry is removed.
    (2) In .gitignore, remove ONE of the two duplicate `/.claude` lines (keep exactly one `/.claude` so the rest of `.claude/` — local GSD tooling — stays ignored), and DELETE the `CLAUDE.md` line (currently line 26) so CLAUDE.md becomes trackable. Do not alter any other ignore rule.
    (3) `git add CLAUDE.md` so the project-instructions doc becomes tracked (it is the source of truth the HYG-03 doc fix in Task 3 edits — tracking it makes that fix durable and shared).
    Commit all three together (suggested message `chore(03): untrack settings.local.json, reconcile .gitignore, track CLAUDE.md (HYG-02)`). No code or behavior change — pure repo-tracking hygiene.
  </action>
  <verify>
    <automated>test -z "$(git ls-files .claude/settings.local.json)" && [ "$(grep -c '^/\.claude$' .gitignore)" -eq 1 ] && [ "$(grep -c '^CLAUDE\.md$' .gitignore)" -eq 0 ] && git ls-files --error-unmatch CLAUDE.md >/dev/null 2>&1 && pytest -q tests/test_golden_traces.py</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files .claude/settings.local.json` returns empty (file untracked)
    - `.claude/settings.local.json` still exists on disk (`test -f .claude/settings.local.json`)
    - `grep -c '^/\.claude$' .gitignore` returns exactly 1 (duplicate deduped, ignore preserved)
    - `grep -c '^CLAUDE\.md$' .gitignore` returns 0 (CLAUDE.md line removed)
    - `git ls-files --error-unmatch CLAUDE.md` exits 0 (CLAUDE.md now tracked)
    - `pytest tests/test_golden_traces.py` exits 0
  </acceptance_criteria>
  <done>settings.local.json untracked but on disk; .gitignore has a single /.claude and no CLAUDE.md line; CLAUDE.md is tracked; golden traces unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Fix doc drift — CLAUDE.md box-exit timing + menu.py docstring (HYG-03, D-12)</name>
  <files>CLAUDE.md, menu.py</files>
  <read_first>
    - CLAUDE.md ("Ghost Box Exit" section — prose currently says "Pinky after ~2 sec, Clyde after ~4 sec")
    - settings.py:56-58 (source of truth: BOX_EXIT_DELAY_INKY=0, PINKY=30, CLYDE=60 — DO NOT change these)
    - menu.py:11-12 (run_main_menu docstring referencing the dead 'Change Initials' option)
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-12 — edit the DOC not the constants; remove dead docstring)
    - .planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md (CLAUDE.md + menu.py sections — exact target prose)
  </read_first>
  <action>
    Two documentation-only edits, one atomic commit (D-12):
    (1) In CLAUDE.md "Ghost Box Exit" section, reconcile the timing prose to match the constants. At 60 fps, BOX_EXIT_DELAY_PINKY=30 frames ≈ 0.5 s and BOX_EXIT_DELAY_CLYDE=60 frames ≈ 1 s. Change "Pinky after ~2 sec" to "Pinky after ~0.5 sec" and "Clyde after ~4 sec" to "Clyde after ~1 sec" (Inky=0 / "immediately" is already consistent — leave it). EDIT THE PROSE ONLY. Do NOT change settings.py constants — changing the delays would be a second, unsanctioned behavior change that moves golden traces.
    (2) In menu.py, remove the dead `'Change Initials'` reference from the run_main_menu docstring so it reads as Play / Leaderboard / Quit (matching MENU_OPTIONS = ["Play", "Leaderboard", "Quit"]). Docstring text only — no code path change.
    Commit both together (suggested message `docs(03): reconcile box-exit timing + drop dead Change Initials docstring (HYG-03)`).
  </action>
  <verify>
    <automated>grep -Eq 'Pinky after ~?0\.5' CLAUDE.md && grep -Eq 'Clyde after ~?1 ' CLAUDE.md && [ "$(grep -c 'Change Initials' menu.py)" -eq 0 ] && grep -Eq 'BOX_EXIT_DELAY_PINKY = 30' settings.py && grep -Eq 'BOX_EXIT_DELAY_CLYDE = 60' settings.py && pytest -q tests/test_golden_traces.py</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "Pinky after ~0.5" (matches `Pinky after ~?0\.5`) and "Clyde after ~1" (matches `Clyde after ~?1 `)
    - CLAUDE.md no longer contains "~2 sec" or "~4 sec" in the Ghost Box Exit section
    - `grep -c 'Change Initials' menu.py` returns 0
    - settings.py STILL contains `BOX_EXIT_DELAY_PINKY = 30` and `BOX_EXIT_DELAY_CLYDE = 60` (constants UNCHANGED)
    - `pytest tests/test_golden_traces.py` exits 0 (no behavior change)
  </acceptance_criteria>
  <done>CLAUDE.md box-exit prose reads ~0.5 s / ~1 s matching the constants; menu.py docstring drops 'Change Initials'; settings.py constants untouched; golden traces unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Delete dead asset folders + human .exe rebuild smoke-run (HYG-04, D-13/D-14)</name>
  <files>assets/ghost_images/, assets/player_images/</files>
  <read_first>
    - .planning/phases/03-box-bug-fix-hygiene/03-CONTEXT.md (D-13 delete byte-dupe folders; D-14 required .exe rebuild + smoke-run gate)
    - .planning/phases/03-box-bug-fix-hygiene/03-PATTERNS.md (assets section — only reference is the design spec; build.py:8 ships the folder)
    - build.py (line 8 `--add-data=assets;assets` — confirms the whole assets/ folder is bundled)
  </read_first>
  <what-built>
    The executor has (automated, before this checkpoint): confirmed assets/ghost_images/ and assets/player_images/ are byte-duplicates of the live assets/ghosts/ and assets/pacman/ folders, and that the ONLY code/config reference to the `*_images` folders anywhere in the repo is the design spec instructing their deletion (grep returns no live-code reference). Then deleted both folders with `git rm -r assets/ghost_images assets/player_images` and committed (suggested message `chore(03): delete dead duplicate asset folders (HYG-04)`). The full pytest suite + golden traces are green (the deleted folders are unreferenced by any code path).
  </what-built>
  <action>
    AUTOMATED portion (do before pausing for the human): (1) grep the whole repo for any reference to `ghost_images` or `player_images` outside docs/.planning — confirm the only hits are the design spec / planning docs. If ANY live-code reference exists, STOP and surface it (do not delete). (2) `git rm -r assets/ghost_images assets/player_images`. (3) Run `pytest` (full suite) and confirm green. (4) Commit. THEN pause for the human gate below — D-14 requires a real PyInstaller build + smoke-run that headless CI structurally cannot perform.
  </action>
  <how-to-verify>
    Human, perform the D-14 .exe rebuild + smoke-run gate (headless CI cannot build the PyInstaller bundle):
    1. From the repo root, run: `python build.py`
    2. Confirm it produces `dist/pacman/pacman.exe` with no asset-related build errors.
    3. Launch `dist/pacman/pacman.exe` and play a few frames (move Pac-Man, confirm ghosts and Pac-Man sprites render from the bundled `assets/ghosts/` and `assets/pacman/`).
    4. Confirm the bundle still loads all assets and runs (no missing-image crash, no broken sprite) — proving the deleted `*_images` folders were truly dead and the pyinstaller pin (Task 1) builds a working bundle.
  </how-to-verify>
  <verify>
    <automated>test ! -d assets/ghost_images && test ! -d assets/player_images && test -d assets/ghosts && test -d assets/pacman && pytest -q</automated>
    <human-check>python build.py produces a working dist/pacman/pacman.exe that launches and renders ghost + Pac-Man sprites from the bundled assets over a few frames</human-check>
  </verify>
  <acceptance_criteria>
    - `test ! -d assets/ghost_images` and `test ! -d assets/player_images` both succeed (folders deleted)
    - `test -d assets/ghosts` and `test -d assets/pacman` both succeed (live folders preserved)
    - No live-code reference to `ghost_images`/`player_images` outside docs/.planning (grep confirms)
    - `pytest` (full suite) exits 0 after deletion
    - Human confirms `python build.py` builds dist/pacman/pacman.exe and it launches + renders sprites over a few frames
  </acceptance_criteria>
  <resume-signal>Type "approved" once the rebuilt .exe launches and renders sprites correctly, or describe the build/run failure.</resume-signal>
  <done>Dead *_images asset folders deleted; live assets/ghosts/ + assets/pacman/ intact; full suite green; human-verified the rebuilt .exe loads assets and runs.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces NO new gameplay symbols or constants — it is behavior-neutral hygiene only. State changes:
- requirements.txt: `pygame==<ver>`, `pyinstaller==<ver>` (newly pinned strings)
- .gitignore: single `/.claude` line, `CLAUDE.md` line removed (newly trackable doc)
- CLAUDE.md: now git-tracked; "Ghost Box Exit" prose reads ~0.5 s / ~1 s
- menu.py: run_main_menu docstring no longer references 'Change Initials'
- Deleted: assets/ghost_images/, assets/player_images/ (dead byte-duplicate folders)
No new source symbols, functions, or constants are introduced. The unified `GHOST_BOX_BOUNDS` constant and the transient oracle test functions are produced by Plan 02, not here.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

This plan introduces NO new input, network, auth, or data-handling surface. All four items are local-only repo/config/doc/asset changes. The only boundary worth a sentence is the local-settings tracking surface.

| Boundary | Description |
|----------|-------------|
| git index ↔ working tree (.claude/settings.local.json) | A local machine-specific settings file was tracked; this plan stops tracking it (security-POSITIVE). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `.claude/settings.local.json` (was tracked) | mitigate | `git rm --cached` stops tracking the local settings file (Task 2). This is the security-POSITIVE core of HYG-02 — reduces the chance of machine-specific/local config leaking into the shared repo going forward. |
| T-03-02 | Information Disclosure | git HISTORY of settings.local.json | accept | INFORMATIONAL (not a blocker for this phase): untracking stops FUTURE tracking but does not purge prior commits. Confirm the file contains no committed secret (it is GSD local tooling config — machine paths/flags, no credentials). If a real secret were ever found in history, history-rewrite is a separate out-of-scope remediation. Low risk, no PII/credentials expected. |
| T-03-03 | Tampering | pip installs (pygame, pyinstaller pins) | accept | No NEW packages added — HYG-01 only pins ALREADY-installed deps to their resolved versions. No package-legitimacy gate needed (both are long-established, already in use). Pinning is itself supply-chain POSITIVE (reproducible builds). |

**Block-on:high gate: SATISFIED — no high-severity threats.** This phase's hygiene surface is minimal; HYG-02 is net security-positive. T-03-02 is an informational check, explicitly NOT a blocker for Phase 3.
</threat_model>

<verification>
After each of the four atomic commits:
- `pytest tests/test_golden_traces.py` exits 0 — golden traces BYTE-IDENTICAL (this plan changes zero behavior).
- After Task 4: `pytest` (full suite) exits 0.
- CI (ubuntu, pinned, headless) green on push for every commit (D-09 CI-green-at-every-step).
- Per-task `<acceptance_criteria>` all pass.
- D-14: human-verified .exe rebuild + smoke-run (the one verification CI structurally cannot perform).
</verification>

<success_criteria>
- requirements.txt has exact `==` pins for pygame + pyinstaller (backend untouched) — HYG-01.
- .claude/settings.local.json untracked; .gitignore single /.claude, no CLAUDE.md line; CLAUDE.md tracked — HYG-02.
- CLAUDE.md box-exit prose ~0.5 s / ~1 s matching constants; menu.py docstring drops 'Change Initials'; settings.py constants UNCHANGED — HYG-03.
- assets/ghost_images/ + assets/player_images/ deleted; live folders intact; rebuilt .exe loads assets and runs (human-verified) — HYG-04.
- Golden traces byte-identical and CI green after EVERY commit (D-09).
</success_criteria>

<output>
Create `.planning/phases/03-box-bug-fix-hygiene/03-01-SUMMARY.md` when done.
</output>
