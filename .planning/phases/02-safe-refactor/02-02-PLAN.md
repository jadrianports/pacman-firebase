---
phase: 02-safe-refactor
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ghost.py
  - tests/_legacy_movers.py
  - tests/test_mover_oracle.py
  - CLAUDE.md
autonomous: true
requirements: [REF-02]

must_haves:
  truths:
    - "The 4× ghost-movement duplication is collapsed into per-ghost priority DATA (the *_PROFILE structures) + one unified _move + thin per-ghost hooks for the structural quirks"
    - "move_blinky / move_inky / move_pinky / move_clyde remain as byte-identical thin zero-arg wrappers returning (x_pos, y_pos, direction) — so game.py:move_ghosts dispatch AND the 15 micro tests are UNTOUCHED"
    - "Any dead/in-box ghost can still reach the clyde profile (move_clyde is the fallback game.py:move_ghosts calls on dead/in-box ghosts)"
    - "The synthetic-exhaustive differential oracle proves unified-mover(ghost) == legacy move_<ghost> across the whole decision space, per ghost, before each ghost's commit"
    - "Each ghost lands as its own atomic green commit in the D-11 order: blinky → inky → pinky → clyde"
    - "The mutation canary demonstrates the oracle + golden traces go RED on a deliberate ladder perturbation, then green on revert (the harness has teeth)"
    - "After the green gate, the legacy movers (tests/_legacy_movers.py), the legacy check_collisions copy (tests/_legacy_geometry.py), and both differential oracle test files are DELETED — single source of truth remains"
    - "CLAUDE.md's Ghost System section describes the unified data-driven mover + per-ghost profiles + thin wrappers"
    - "All 9 golden traces and 15 micro tests stay byte-identical green at every commit"
  artifacts:
    - path: "ghost.py"
      provides: "DirectionRule schema + 4 *_PROFILE data tables + named quirk hooks + unified _move + 4 thin wrappers"
      contains: "def _move("
    - path: "tests/_legacy_movers.py"
      provides: "One-shot frozen copies of the OLD move_blinky/inky/pinky/clyde (DELETED after green per D-06)"
      contains: "def legacy_move_blinky("
    - path: "tests/test_mover_oracle.py"
      provides: "Synthetic-exhaustive differential oracle (DELETED after green per D-06)"
      contains: "itertools.product"
    - path: "CLAUDE.md"
      provides: "Updated Ghost System doc (D-17)"
      contains: "_move"
  key_links:
    - from: "ghost.py:move_blinky"
      to: "ghost.py:_move"
      via: "return self._move(BLINKY_PROFILE)"
      pattern: "_move\\(BLINKY_PROFILE\\)"
    - from: "ghost.py:_move"
      to: "ghost.py:*_PROFILE"
      via: "consumes the per-direction DirectionRule data"
      pattern: "DirectionRule"
    - from: "game.py:move_ghosts"
      to: "ghost.py:move_clyde"
      via: "calls .move_clyde() on dead/in-box ghosts (the clyde profile is the fallback)"
      pattern: "move_clyde"
    - from: "tests/test_mover_oracle.py"
      to: "tests/_legacy_movers.py"
      via: "asserts unified _move == legacy_move_<ghost> per case"
      pattern: "legacy_move_"
---

<objective>
Collapse the 4× ghost-movement duplication (REF-02) into per-ghost priority DATA + a single unified `_move` + thin per-ghost hooks for the genuine structural quirks (D-01/D-03), while keeping `move_blinky/inky/pinky/clyde` as byte-identical thin wrappers (D-02) so the `game.py:move_ghosts` dispatch and the 15 micro tests are never disturbed. Prove byte-identity with a synthetic-exhaustive differential oracle (D-04/D-05), land it as per-ghost atomic green commits (D-11: blinky → inky → pinky → clyde), run the mutation canary once (D-10), then one-shot-delete the legacy copies + oracles (D-06/D-07) and update CLAUDE.md's Ghost System doc (D-17).

This is the SECOND half of the Cardinal Rule's "byte-identical refactor" step. It depends on Plan 01 (geometry must be centralized + proven green FIRST — the D-11 geometry-first gate, enforced by `depends_on: [02-01]`).

Purpose: Kill ~90% of the mover duplication into a single source of truth with mathematical proof the precious hand-tuned ghost AI is byte-for-byte unchanged.
Output: the profile schema + unified `_move` + 4 wrappers in ghost.py; the frozen legacy movers + differential oracle (then deleted); the mutation-canary attestation; and the D-17 CLAUDE.md update.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-safe-refactor/02-CONTEXT.md
@.planning/phases/02-safe-refactor/02-RESEARCH.md
@.planning/phases/02-safe-refactor/02-PATTERNS.md

@ghost.py
@game.py
@tests/test_ghost_micro.py
@tests/test_golden_traces.py
@CLAUDE.md

# Plan 01 landed the geometry-first gate this plan depends on:
@.planning/phases/02-safe-refactor/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**New symbols Plan 02-02 creates** (the convergence/source-grounding pass MUST exclude these from drift verification — they are NEW, not pre-existing):

In `ghost.py` (module-level data block at top, after imports, before `class Ghost` — D-03):
- `DirectionRule` namedtuple with fields `("primary", "blocked_ladder", "forward_open")`
- The four per-ghost profile data structures: `BLINKY_PROFILE`, `INKY_PROFILE`, `PINKY_PROFILE`, `CLYDE_PROFILE` (each a `dict[int, DirectionRule]` keyed by direction 0-3)
- Named quirk-hook functions (the non-tabular control-flow branches), e.g. `straight`, `seek_perp_y` (Q-a), `turn_no_move_down` / `turn_and_move_down` (Q-b inky vs pinky), plus the literal `if…if…else` forward-open hooks (Q-c) — names at executor's discretion (CONTEXT Claude's Discretion), descriptive per the quirk
- The unified mover method `Ghost._move(self, profile)`
- The four thin wrappers `move_blinky/move_inky/move_pinky/move_clyde` (names + zero-arg signatures + `(x_pos, y_pos, direction)` return PRESERVED — these are NOT new symbols, they are rewritten bodies)

New test files / oracle symbols (all DELETED after green per D-06):
- `tests/_legacy_movers.py` (`legacy_move_blinky/inky/pinky/clyde` — verbatim frozen OLD movers)
- `tests/test_mover_oracle.py` (the synthetic-exhaustive differential oracle)

NOTE: This plan does NOT touch `check_collisions`, `settings.py`, `game.py`, or `player.py` logic (Plan 01 owns geometry). It does NOT change targeting (`get_targets`) or turn-preference ordering — those are THE SPEC, preserved byte-for-byte (Non-Goal). The two box constants stay distinct (Phase 3 owns unification).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Profile schema + unified _move + thin wrappers + frozen legacy movers (data-driven mover, D-01/D-02/D-03)</name>
  <files>ghost.py, tests/_legacy_movers.py</files>
  <read_first>
    - ghost.py (READ ALL FOUR movers verbatim before writing anything: move_clyde L117-254, move_blinky L256-360, move_inky L362-482, move_pinky L484-608; and the shared trailing wrap clamp L250-254/356-360/478-482/604-608)
    - tests/test_ghost_micro.py (the 15 micro tests call move_blinky/inky/pinky/clyde + move_clyde by name — the measuring instrument that MUST stay untouched; make_ghost harness)
    - game.py (move_ghosts L344-362 — the dispatch that calls .move_clyde() on dead/in-box ghosts; stays byte-identical)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-1: the EXACT per-(ghost×direction) orderings + the three quirk classes Q-a/Q-b/Q-c with verbatim line refs; FOCUS-2: wrapper signatures + clyde-as-fallback + the shared wrap clamp; Pitfalls 1/2/3)
    - .planning/phases/02-safe-refactor/02-PATTERNS.md (profile-data placement + wrapper byte-identity + the shared wrap clamp; Q-b/Q-c landmine box)
    - .planning/phases/02-safe-refactor/02-CONTEXT.md (D-01/D-02/D-03 + Claude's Discretion on schema/hook names)
  </read_first>
  <action>
    FIRST capture the frozen oracle (D-06): create tests/_legacy_movers.py with verbatim copies of the CURRENT move_blinky/inky/pinky/clyde bodies as standalone functions `legacy_move_blinky(g)`, `legacy_move_inky(g)`, `legacy_move_pinky(g)`, `legacy_move_clyde(g)` operating on a passed ghost-like object `g` (read/write g.x_pos/y_pos/direction/target/turns/speed exactly as the methods do; apply the trailing wrap clamp; return `(g.x_pos, g.y_pos, g.direction)`). These must be the PRE-refactor logic, self-contained (no import from ghost.py), captured in the same commit window as the new mover so the differential test can compare. DELETED in Task 3 after green.

    THEN build the data-driven mover in ghost.py per the RESEARCH FOCUS-1 schema. The unit is the (ghost × direction) CELL, NOT one priority list per ghost (Pitfall 1 — a naive single-list-per-ghost schema silently bricks ≥3 quirks). At module level (top of ghost.py, after imports, before class Ghost — D-03) define `DirectionRule = namedtuple("DirectionRule", ["primary", "blocked_ladder", "forward_open"])`. For each ghost build a `dict[int, DirectionRule]` profile keyed by direction 0-3:
    - `blocked_ladder` = the TABULAR ~90%: an ordered tuple of fall-through steps (the `elif not self.turns[dir]:` ladder). The dir-0/dir-1/dir-3 blocked ladders are IDENTICAL across all four ghosts (RESEARCH FOCUS-1 verbatim); dir-2 has TWO variants — blinky trailing any-open order is `t3,t0,t1`, the other three are `t1,t3,t0` (LANDMINE — encode per ghost). Each ladder step is `(want_dir, target_condition_or_None)`; the unified executor walks it, takes the first satisfied step, applies the canonical `direction = d; pos ±= speed`.
    - `primary` and `forward_open` = the HOOKS (control-flow, NOT data — D-01 explicitly rejected boolean-flag-everything): small named module-level functions taking `(ghost)` and returning a sentinel (e.g. MOVED / NOT_HANDLED) so `_move` knows whether to fall through. The per-cell quirks to encode as distinct hooks: Q-a (dir-0/dir-2/dir-3 forward-open) — blinky/pinky go STRAIGHT (`straight`), clyde/inky branch on the perpendicular target axis (`seek_perp_y`/equivalent); Q-b (dir-1 primary) — inky sets `direction=3` with NO move (`turn_no_move_down`), pinky sets direction AND moves (`turn_and_move_down`), blinky/clyde have NO such dir-1 primary (their first test is `tgt.x<←&t1`); plus the dir-2 PRIMARY split (blinky/inky test `tgt.y<↑&t2` first; clyde/pinky test `tgt.x<←&t1` FIRST). Q-c (the `if…if…else` non-elif override blocks, e.g. clyde forward-open dir-0 L142-150) MUST be transcribed as LITERAL Python inside the hook — NEVER collapsed into an elif ladder (Pitfall 2: the second `if` can override the first in the same frame; the final `else` binds only to the second `if`). The original personality comments become each profile's docstring (D-03).
    - Fold the shared trailing wrap clamp (`if self.x_pos < -30: self.x_pos = 900 elif > 900: -30`, identical across all four — RESEARCH FOCUS-2) into `_move` ONCE, then `return self.x_pos, self.y_pos, self.direction`.

    Replace the four mover method BODIES with thin wrappers (D-02 — names/zero-arg signatures/return shape PRESERVED so game.py:move_ghosts and the 15 micro tests are untouched): `def move_blinky(self): return self._move(BLINKY_PROFILE)` and likewise inky/pinky/clyde. CLYDE-AS-FALLBACK (D-02 locked): `_move` reads only `self.*` state, so any ghost (dead/in-box) reaching `move_clyde()` via game.py:move_ghosts gets the CLYDE_PROFILE with no special wiring — the micro test `test_clyde_dead_in_box_fallback_returns_to_target` pins this path.

    Both implementations coexist in this commit (the new `_move`/wrappers AND the frozen legacy copy) — proof preserved in git history (D-06). Do NOT delete yet (Task 3 deletes after all four prove green).
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "import ast; src=open('ghost.py').read(); assert 'DirectionRule' in src and 'def _move(' in src and '_move(BLINKY_PROFILE)' in src and '_move(INKY_PROFILE)' in src and '_move(PINKY_PROFILE)' in src and '_move(CLYDE_PROFILE)' in src, 'mover shape missing'; print('mover shape OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "src=open('tests/_legacy_movers.py').read(); assert all(f'def legacy_move_{g}(' in src for g in ('blinky','inky','pinky','clyde')), 'legacy movers missing'; print('legacy movers OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_ghost_micro.py tests/test_golden_traces.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ghost.py` contains `DirectionRule` (namedtuple with `primary`, `blocked_ladder`, `forward_open`), all four `*_PROFILE` dicts keyed by direction 0-3, `def _move(`, and the four thin wrappers each `return self._move(<GHOST>_PROFILE)`
    - The four `move_blinky/inky/pinky/clyde` keep zero-arg signatures and `return self.x_pos, self.y_pos, self.direction` (the wrap clamp is folded into `_move` once)
    - The dir-2 blocked ladder is encoded with blinky's trailing order `t3,t0,t1` distinct from the other three's `t1,t3,t0` (LANDMINE)
    - inky dir-1 primary hook does NOT advance position (Q-b: `turn_no_move_down`); pinky dir-1 primary hook DOES advance (`turn_and_move_down`) — two distinct hooks
    - The Q-c `if…if…else` forward-open branches are literal Python in a named hook, NOT an `elif` ladder
    - `tests/_legacy_movers.py` defines `legacy_move_blinky/inky/pinky/clyde` operating on a passed ghost, with the trailing wrap clamp, NOT importing ghost.py
    - `pytest tests/test_ghost_micro.py -q` exits 0 (all 15 micro tests pass — the measuring instrument was NOT disturbed)
    - `pytest tests/test_golden_traces.py -q` exits 0 (all 9 golden traces byte-identical)
    - `game.py:move_ghosts` is unchanged (still calls `.move_blinky()`/`.move_inky()`/`.move_pinky()`/`.move_clyde()` by name)
  </acceptance_criteria>
  <done>ghost.py has the DirectionRule schema, four per-ghost profile dicts, the named quirk hooks (Q-a/Q-b/Q-c), and the unified _move; the four movers are byte-identical thin wrappers; the frozen legacy movers are captured in tests/_legacy_movers.py; the 15 micro tests + 9 golden traces are green and game.py:move_ghosts is untouched.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Synthetic-exhaustive differential oracle + per-ghost atomic green commits (D-04/D-05/D-11)</name>
  <files>tests/test_mover_oracle.py, ghost.py</files>
  <read_first>
    - ghost.py (the NEW unified _move + wrappers from Task 1 — the implementation under test)
    - tests/_legacy_movers.py (the frozen OLD oracle from Task 1)
    - tests/test_ghost_micro.py (make_ghost L41-57 + screen fixture L27-38 — reuse to construct headless ghosts; Pitfall 4/5 deep-copy board per case)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-3: input space = direction(4) × 16 turns combos × target-sign-per-axis(3×3) × in_box(2) × dead(2) × speed{1,2,4} × 5 wrap-x positions = ~34,560/ghost; bypass check_collisions by clobbering g.turns/g.in_box AFTER construction; deep-clone per case; programmatic loops with first-divergence accumulator, NOT parametrize; A2 runtime < 10s)
    - .planning/phases/02-safe-refactor/02-PATTERNS.md (oracle analog = test_ghost_micro.py; exact-tuple compare; clobber g.turns/g.in_box)
    - .planning/phases/02-safe-refactor/02-CONTEXT.md (D-11 per-ghost atomic-commit sequence: blinky → inky → pinky → clyde)
  </read_first>
  <action>
    Create tests/test_mover_oracle.py reusing the test_ghost_micro.py `screen` fixture + `make_ghost`. Write ONE test function per ghost (`test_unified_mover_matches_legacy_blinky/inky/pinky/clyde`) that loops with `itertools.product` (NOT pytest.mark.parametrize — 138k IDs would bloat collection per RESEARCH FOCUS-3) over: direction ∈ range(4); turns_combo ∈ all 16 boolean 4-tuples; target-sign per axis realized by picking `tx ∈ {x-100, x, x+100}` and `ty ∈ {y-100, y, y+100}` relative to the case's x,y; in_box ∈ {False, True}; dead ∈ {False, True}; speed ∈ {1, 2, 4}; x ∈ 5 wrap representatives {-31, -30, 450, 900, 901}. For each case, construct TWO independent ghosts via make_ghost (deep-copied board, Pitfall 4/5), clobber `g.turns = list(turns_combo)` and `g.in_box = box` AFTER construction (bypasses check_collisions to control the mover's full input space — RESEARCH FOCUS-3), run `legacy_move_<ghost>(old_g)` and `new_g.move_<ghost>()`, and assert `(old_g.x_pos, old_g.y_pos, old_g.direction) == new_result` with EXACT-integer compare. Accumulate the FIRST ~5 divergences into a list and assert `not failures` with a readable first-divergence message reporting the offending `(direction, turns, target-signs, in_box, dead, speed, x, old, new)`.

    Run the per-ghost atomic sequence (D-11): prove `test_unified_mover_matches_legacy_blinky` green → that is blinky's atomic green commit boundary → then inky → pinky → clyde, each independently proven before stacking the next. The differential oracle runs per-ghost as you go; if any ghost goes red, it bisects to exactly one ghost/step (never stack an unproven change). At each ghost's green point the golden traces + 15 micro tests must ALSO stay green. (Note: do NOT delete the legacy/oracle here — Task 3 runs the canary against them first, then deletes.)
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "src=open('tests/test_mover_oracle.py').read(); assert 'itertools.product' in src and all(f'legacy_move_{g}' in src for g in ('blinky','inky','pinky','clyde')), 'oracle shape missing'; print('oracle shape OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_mover_oracle.py -q</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_ghost_micro.py tests/test_golden_traces.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_mover_oracle.py` has one test per ghost using `itertools.product` and compares `(x_pos, y_pos, direction)` with `==`
    - The enumeration covers direction × 16 turns-combos × 3×3 target-signs × in_box × dead × speed{1,2,4} × the 5 wrap-x representatives, clobbering `g.turns`/`g.in_box` after `make_ghost`
    - `pytest tests/test_mover_oracle.py::test_unified_mover_matches_legacy_blinky -q` exits 0 (blinky proven byte-identical)
    - `pytest tests/test_mover_oracle.py -q` exits 0 for ALL four ghosts (inky/pinky/clyde proven too) — unified _move == legacy across the whole synthetic decision space
    - At each ghost's proof point, `pytest tests/test_ghost_micro.py tests/test_golden_traces.py -q` exits 0 (net stays green)
    - The git history reflects per-ghost atomic commits in the D-11 order (geometry from Plan 01 → blinky → inky → pinky → clyde), each green
    - The oracle completes inside the CI budget (single invocation; RESEARCH A2 estimates < 10s for ~138k pure-arithmetic calls)
  </acceptance_criteria>
  <done>The synthetic-exhaustive differential oracle proves unified-mover == legacy for all four ghosts across the entire (direction × turns × target-sign × in_box × dead × speed × wrap-x) space; each ghost landed as its own atomic green commit (blinky → inky → pinky → clyde) with the golden traces + micro tests green throughout.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation canary (D-10) + one-shot deletion (D-06/D-07) + CLAUDE.md Ghost System doc (D-17)</name>
  <files>ghost.py, CLAUDE.md, tests/_legacy_movers.py, tests/_legacy_geometry.py, tests/test_mover_oracle.py, tests/test_check_collisions_oracle.py</files>
  <read_first>
    - ghost.py (the unified _move + profile data — the canary perturbs one blocked-ladder rung here)
    - tests/test_mover_oracle.py + tests/_legacy_movers.py + tests/_legacy_geometry.py + tests/test_check_collisions_oracle.py (the one-shot artifacts to delete AFTER the canary + after green)
    - tests/test_golden_traces.py + tests/test_ghost_micro.py (the permanent guards that MUST stay green and that the canary must drive RED on perturbation)
    - CLAUDE.md (the "### Ghost System" section L32-onward; specifically the bullet "Each ghost has its own AI method (`move_blinky`, `move_inky`, `move_pinky`, `move_clyde`) with distinct personality" L35, and the "`move_clyde` doubles as fallback" bullet to PRESERVE)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-6: canary procedure — swap two adjacent rungs, confirm oracle + golden go RED, revert, attest; run while the oracle still exists; A1/A2 notes)
    - .planning/phases/02-safe-refactor/02-CONTEXT.md (D-06 one-shot-then-delete; D-10 canary; D-17 doc update; D-19 end-of-phase human GIF gate)
  </read_first>
  <action>
    MUTATION CANARY (D-10), run ONCE while the oracle still exists (before deletion), attest each step in the SUMMARY: (1) deliberately perturb one BLOCKED-ladder rung in the unified _move data — e.g. in the dir-2 blocked ladder swap two adjacent any-open rungs so blinky's `t3,t0,t1` becomes `t0,t3,t1` (or swap two rungs in the ordered executor list). (2) Run `pytest tests/test_mover_oracle.py -q` AND `pytest tests/test_golden_traces.py -k baseline -q` AND `pytest tests/test_ghost_micro.py -q`; confirm AT LEAST the differential oracle AND the golden traces go RED (micro tests may or may not trip depending on whether a scenario visits that exact cell — oracle+golden red is the required signal). (3) Revert the perturbation; confirm all three green again. (4) Record in the SUMMARY: the exact mutation, which suites went red, and that revert restored green. This proves the verification has teeth (closes the false-green / blind-harness failure mode) BEFORE we trust the green and delete the oracle.

    ONE-SHOT DELETION (D-06/D-07): only AFTER the canary is attested AND all four ghosts are proven green AND the check_collisions oracle (Plan 01) is green, DELETE the one-shot artifacts: `tests/_legacy_movers.py`, `tests/_legacy_geometry.py` (the frozen check_collisions copy from Plan 01), `tests/test_mover_oracle.py`, and `tests/test_check_collisions_oracle.py`. The proof is preserved in git history; the permanent regression guard going forward is the golden traces + 15 micro tests (which carry forward untouched) + the PERMANENT frame-hash check from Plan 01. Do NOT delete tests/test_frame_hash.py or the frame_hashes.txt manifests (those are permanent, D-09). After deletion, run the full suite to confirm the permanent net is still green WITHOUT the oracles.

    CLAUDE.md DOC UPDATE (D-17): update the "### Ghost System" section. Replace the bullet "Each ghost has its own AI method (`move_blinky`, `move_inky`, `move_pinky`, `move_clyde`) with distinct personality" with a description of the NEW design: a single unified data-driven mover (`_move`) consuming per-ghost `*_PROFILE` data (per-direction turn-priority tables + named quirk hooks), with `move_blinky/inky/pinky/clyde` kept as thin delegating wrappers. PRESERVE the existing "`move_clyde` doubles as fallback movement for dead/in-box ghosts" bullet (still true — the clyde profile is the fallback). Do NOT touch the pre-existing box-exit-timing or "Change Initials" doc drift (that is HYG-03, Phase 3) — D-17 is ONLY the new drift this refactor creates.
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && test ! -f tests/_legacy_movers.py && test ! -f tests/_legacy_geometry.py && test ! -f tests/test_mover_oracle.py && test ! -f tests/test_check_collisions_oracle.py && echo "one-shot artifacts deleted OK"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "src=open('CLAUDE.md').read(); assert '_move' in src and 'PROFILE' in src.upper() and 'move_clyde' in src, 'D-17 doc update missing'; assert 'Each ghost has its own AI method' not in src, 'old Ghost System bullet still present'; print('D-17 doc OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The mutation canary was run once and attested in the SUMMARY: the exact perturbation, that `test_mover_oracle.py` AND `test_golden_traces.py` went RED, and that revert restored green (run BEFORE deletion, while the oracle existed)
    - `tests/_legacy_movers.py`, `tests/_legacy_geometry.py`, `tests/test_mover_oracle.py`, `tests/test_check_collisions_oracle.py` are DELETED (the four `test ! -f` checks pass)
    - `tests/test_frame_hash.py` and the `frame_hashes.txt` manifests are NOT deleted (permanent, D-09)
    - CLAUDE.md no longer contains "Each ghost has its own AI method"; it now describes the unified `_move` + per-ghost `*_PROFILE` + thin wrappers, and STILL contains the `move_clyde` fallback bullet
    - CLAUDE.md box-exit-timing and "Change Initials" drift is UNCHANGED (that is HYG-03 / Phase 3)
    - `pytest -q` exits 0 (the full permanent suite — golden traces + 15 micro tests + frame-hash — is green WITHOUT the deleted oracles)
  </acceptance_criteria>
  <done>The mutation canary demonstrated the oracle + golden traces have teeth (red on perturbation, green on revert) and is attested; the one-shot legacy copies + both differential oracle test files are deleted (single source of truth: the unified mover, guarded permanently by the golden traces + 15 micro tests + frame-hash); CLAUDE.md's Ghost System section is updated to the new data-driven design (D-17) while preserving the clyde-fallback note and leaving Phase 3's pre-existing drift alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production game logic ↔ test harness | The only boundary this plan touches. The differential oracle constructs synthetic ghost state in-process; no external/untrusted input crosses into the game. |

**Finding (per planning_context security note):** This plan is a **pure internal refactor** of the ghost-AI movement logic + test-only oracles. It introduces **NO new attack surface**: no network I/O, no untrusted input parsing, no auth/session/crypto, no new persisted user data, no new packages. The leaderboard/`api_service.py`/cloud-function code is NOT touched. No new trust boundary is created. The public method surface (`move_blinky` etc.) is deliberately preserved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (integrity of behavior) | unified `_move` + per-ghost profiles replacing the four movers | mitigate | The only meaningful "integrity" concern is byte-for-byte behavior preservation of the precious AI. Guarded by: the synthetic-exhaustive differential oracle (whole decision space), the mutation canary (proves the guards have teeth BEFORE trusting green), and the inherited 9 golden traces + 15 untouched micro tests — all green at every per-ghost atomic commit. This is verification, not an external attacker. |
| T-02-05 | Tampering | npm/pip/cargo installs | accept | No packages installed this plan (RESEARCH: "Package Legitimacy Gate: N/A — zero new packages"). namedtuple/itertools are stdlib; pygame/pytest already pinned. No supply-chain surface added. |
| T-02-06 | Information Disclosure / Spoofing / Repudiation / DoS / EoP | (whole plan) | accept | Not applicable — no untrusted input crosses any boundary this plan touches; local single-player game logic + test code only (RESEARCH Security Domain: V2-V6 all N/A at ASVS L1). |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist (zero installs) — no `gate="blocking-human"` checkpoint required. (Task 2 carries `gate="blocking"` for the per-ghost atomic-commit sequencing, not for package legitimacy.)
</threat_model>

<verification>
- `pytest tests/test_mover_oracle.py -q` exits 0 BEFORE deletion (all four ghosts proven byte-identical across the synthetic-exhaustive space)
- The mutation canary drove `test_mover_oracle.py` + `test_golden_traces.py` RED on a deliberate ladder swap and green on revert (attested in SUMMARY) — the harness has teeth
- After one-shot deletion, `pytest -q` exits 0 (the permanent net — golden traces + 15 micro tests + frame-hash — is green without the oracles)
- `tests/_legacy_movers.py`, `tests/_legacy_geometry.py`, `tests/test_mover_oracle.py`, `tests/test_check_collisions_oracle.py` no longer exist
- CLAUDE.md Ghost System section describes the unified data-driven mover + profiles + thin wrappers (D-17), preserves the clyde-fallback bullet, and leaves Phase 3's pre-existing drift alone
- `game.py:move_ghosts` is byte-identical (still dispatches by the four wrapper names; clyde-fallback intact)
- Targeting (`get_targets`) and turn-preference ordering are unchanged (Non-Goal honored)
</verification>

<success_criteria>
ROADMAP Phase 2 SC#2 + SC#3 are satisfied: the 4× ghost movement duplication is collapsed into one data-driven turn-priority table (per-ghost profiles + unified `_move` + thin wrappers), every golden-master trace stays byte-identical, and the refactor lands with the differential oracle (mathematical proof) plus the inherited net green — ready for the D-19 end-of-phase human before/after GIF gate. REF-02 is fully implemented and proven; the per-ghost atomic-commit sequence (D-11) bisects any drift to one ghost; the one-shot oracle lifecycle (D-06/D-07) leaves a single source of truth; and the D-17 doc drift is fixed in-phase.
</success_criteria>

<output>
Create `.planning/phases/02-safe-refactor/02-02-SUMMARY.md` when done. Record: the final profile schema shape + the named quirk hooks chosen (Q-a/Q-b/Q-c mapping); the oracle case-count + runtime per ghost; the per-ghost atomic-commit order actually used; the FULL mutation-canary attestation (exact mutation, which suites went red, revert-to-green); confirmation the four one-shot files are deleted and the permanent net is green; and the D-17 CLAUDE.md change. Note that the D-19 human before/after GIF gate (reuse harness/capture.py build_gif on a canonical playthrough) is the end-of-phase verification owned by the phase orchestrator, not this plan.
</output>
