---
phase: 02-safe-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
  - geometry.py
  - ghost.py
  - game.py
  - player.py
  - tests/_legacy_geometry.py
  - tests/test_check_collisions_oracle.py
  - tests/test_frame_hash.py
  - tests/golden/box_exit/frame_hashes.txt
  - tests/golden/power_chase/frame_hashes.txt
  - tests/golden/ghost_eat/frame_hashes.txt
  - tests/golden/death/frame_hashes.txt
  - tests/golden/win/frame_hashes.txt
  - tests/golden/box_edge/frame_hashes.txt
  - tests/golden/tunnel_wrap/frame_hashes.txt
  - tests/golden/eyes_return/frame_hashes.txt
  - tests/golden/claude_session_01/frame_hashes.txt
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "The inline tile-math literals (num1=(HEIGHT-50)//32, num2=WIDTH//30, num3=15) no longer appear in game.py, ghost.py, or player.py — they read centralized named constants instead"
    - "geometry.py exposes tile_at, is_walkable, and in_box helpers imported by game/ghost/player"
    - "The two inconsistent box rectangles exist as TWO distinct named constants (GHOST_BOX_BOUNDS_COLLISION and GHOST_BOX_BOUNDS_TARGET) — NOT unified (unification is Phase 3/BUG-01)"
    - "All 9 golden-master traces (test_golden_traces.py) still replay byte-for-byte identical after the substitution"
    - "All 15 micro per-ghost tests (test_ghost_micro.py) still pass untouched"
    - "The check_collisions differential oracle proves OLD == NEW (turns, in_box) across the enumerated board/direction/in_box/dead space"
    - "The frame-hash check confirms rendered frames are byte-identical to the committed Linux-blessed manifest for the golden scenarios"
  artifacts:
    - path: "settings.py"
      provides: "Derived tile constants BOARD_ROWS/BOARD_COLS/HUD_HEIGHT/TILE_HEIGHT/TILE_WIDTH/HALF_TILE + named wrap/scatter constants"
      contains: "TILE_HEIGHT = (HEIGHT - HUD_HEIGHT) // BOARD_ROWS"
    - path: "geometry.py"
      provides: "Shared helpers tile_at/is_walkable/in_box + the two distinct box-bounds constants"
      contains: "def in_box("
    - path: "tests/test_check_collisions_oracle.py"
      provides: "Synthetic differential oracle proving geometry centralization preserves (turns, in_box)"
      contains: "def test_check_collisions"
    - path: "tests/test_frame_hash.py"
      provides: "Deterministic pixel-hash assertion vs committed manifest (sha256 over pygame.image.tobytes RGB)"
      contains: "hashlib.sha256"
    - path: "tests/golden/box_exit/frame_hashes.txt"
      provides: "Committed Linux-blessed frame-hash manifest (one representative golden scenario)"
  key_links:
    - from: "geometry.py"
      to: "settings.py"
      via: "from settings import TILE_HEIGHT, TILE_WIDTH"
      pattern: "from settings import"
    - from: "ghost.py:check_collisions"
      to: "geometry.in_box"
      via: "in_box(self.x_pos, self.y_pos, GHOST_BOX_BOUNDS_COLLISION)"
      pattern: "in_box\\("
    - from: "game.py:get_targets"
      to: "geometry.in_box"
      via: "in_box(self.<ghost>_x, self.<ghost>_y, GHOST_BOX_BOUNDS_TARGET)"
      pattern: "in_box\\("
    - from: "tests/test_frame_hash.py"
      to: "tests/golden/<scenario>/frame_hashes.txt"
      via: "read committed manifest, assert replayed frame hashes match"
      pattern: "frame_hashes"
---

<objective>
Centralize tile/board geometry and remove the inline `num1/num2/num3` magic numbers (REF-01), replacing them with named derived constants and a small `geometry.py` helper module — proven behavior-preserving by a `check_collisions` differential oracle (logic) and a deterministic frame-hash check (pixels), with all 9 golden traces and 15 micro tests staying green.

This is the FIRST half of the Cardinal Rule's "byte-identical refactor" step, and it MUST land + prove green BEFORE the mover refactor (Plan 02 / REF-02) — the D-11 geometry-first gate is enforced by this plan boundary (Plan 02 `depends_on: [02-01]`).

Purpose: Kill the highest-density magic-number duplication and stand up the geometry vocabulary (the two distinct box constants + `in_box` predicate) that Phase 3's BUG-01 unification will later collapse in one line.
Output: `settings.py` constants, new `geometry.py`, literal substitutions in game/ghost/player, the `check_collisions` differential oracle, and the frame-hash check + committed manifest.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-safe-refactor/02-CONTEXT.md
@.planning/phases/02-safe-refactor/02-RESEARCH.md
@.planning/phases/02-safe-refactor/02-PATTERNS.md

@settings.py
@ghost.py
@game.py
@player.py
@tests/test_ghost_micro.py
@tests/test_golden_traces.py
@tests/conftest.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
**New symbols Plan 02-01 creates** (the convergence/source-grounding pass MUST exclude these from drift verification — they are NEW, not pre-existing):

New constants in `settings.py`:
- `BOARD_ROWS = 32`, `BOARD_COLS = 30`, `HUD_HEIGHT = 50`
- `TILE_HEIGHT = (HEIGHT - HUD_HEIGHT) // BOARD_ROWS` (= 28, was `num1`)
- `TILE_WIDTH = WIDTH // BOARD_COLS` (= 30, was `num2`)
- `HALF_TILE = 15` (look-ahead offset, was `num3`)
- Wrap-edge constants (kept deliberately distinct per D-13): `GHOST_WRAP_LEFT = -30`, `GHOST_WRAP_RIGHT = 900`, `PLAYER_WRAP_RIGHT_EDGE = 900`, `PLAYER_WRAP_RIGHT_TO = -47`, `PLAYER_WRAP_LEFT_EDGE = -50`, `PLAYER_WRAP_LEFT_TO = 897`
- Scatter/fixed target constants (D-13): `SCATTER_RETURN_TARGET = (380, 400)`, `SCATTER_EATEN_TARGET = (400, 100)`, `SCATTER_CLYDE_TARGET = (450, 450)`

New module `geometry.py` and its functions:
- `tile_at(center_x, center_y, level)`
- `is_walkable(tile_code)`
- `in_box(x, y, bounds)`
- The two distinct box-bounds constants: `GHOST_BOX_BOUNDS_COLLISION = (350, 550, 360, 480)`, `GHOST_BOX_BOUNDS_TARGET = (340, 560, 340, 500)`

New test files / oracle symbols:
- `tests/_legacy_geometry.py` (one-shot frozen copy of OLD `check_collisions` — DELETED in Plan 02)
- `tests/test_check_collisions_oracle.py` (differential oracle — DELETED in Plan 02 after green)
- `tests/test_frame_hash.py` (PERMANENT)
- `tests/golden/<scenario>/frame_hashes.txt` × 9 (PERMANENT, Linux-blessed TEXT manifest)

NOTE: This plan does NOT unify the two box constants (Phase 3 / BUG-01) and does NOT touch the four `move_*` mover methods (Plan 02 / REF-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Centralize tile geometry — settings.py constants, geometry.py helpers, literal substitution in game/ghost/player</name>
  <files>settings.py, geometry.py, ghost.py, game.py, player.py</files>
  <read_first>
    - settings.py (the constant home; mirror its grouped UPPER_SNAKE_CASE blocks)
    - ghost.py (check_collisions L43-115 holds num1/num2/num3 L45-47 + box literal L111; _tile L38-41 is the tile_at analog)
    - game.py (draw_board L130-131, has_dot_nearby L196-197, check_collisions/eat-dot L209-210, get_targets 8 box checks L240/249/258/267/275/282/289/296, scatter targets L235/265)
    - player.py (check_position num1/num2/num3 L41-43, guard `centerx // 30 < 29` L46, band offsets L60-81)
    - tests/test_golden_traces.py (NUM1/NUM2 mirror L40-43; the byte-identity bar)
    - tests/test_ghost_micro.py (the 15 micro tests that MUST stay untouched + green)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-7 names + byte-identity proofs; FOCUS-8 helper bodies; Pitfall 6 do-not-over-share)
    - .planning/phases/02-safe-refactor/02-PATTERNS.md (constant idiom; geometry.py import-by-name idiom)
  </read_first>
  <action>
    Add to settings.py (grouped `# Tile geometry`, `# Wrap edges`, `# Scatter targets` blocks, matching the existing house grouping): `BOARD_ROWS = 32`, `BOARD_COLS = 30`, `HUD_HEIGHT = 50`, then `TILE_HEIGHT = (HEIGHT - HUD_HEIGHT) // BOARD_ROWS` (must equal 28), `TILE_WIDTH = WIDTH // BOARD_COLS` (must equal 30), `HALF_TILE = 15`. Per D-12 derive from named BOARD_ROWS/BOARD_COLS/HUD_HEIGHT (documents the mysterious 32/30/50). NAMING LANDMINE (RESEARCH FOCUS-7): num1 is tile HEIGHT (rows), num2 is tile WIDTH (cols) — do NOT transpose. Also add the distinct wrap-edge constants (GHOST_WRAP_LEFT=-30, GHOST_WRAP_RIGHT=900, PLAYER_WRAP_RIGHT_EDGE=900, PLAYER_WRAP_RIGHT_TO=-47, PLAYER_WRAP_LEFT_EDGE=-50, PLAYER_WRAP_LEFT_TO=897) and scatter targets (SCATTER_RETURN_TARGET=(380,400), SCATTER_EATEN_TARGET=(400,100), SCATTER_CLYDE_TARGET=(450,450)) kept as SEPARATE named constants per D-13 — naming the look-alikes explicitly guards against accidental future unification.

    Create geometry.py (new module, D-16) with `from settings import TILE_HEIGHT, TILE_WIDTH` and the two distinct box-bounds constants GHOST_BOX_BOUNDS_COLLISION=(350,550,360,480) and GHOST_BOX_BOUNDS_TARGET=(340,560,340,500) (kept distinct per D-13/D-14 — DO NOT unify; that is Phase 3/BUG-01). Define three helpers with one-line docstrings (house style): `tile_at(center_x, center_y, level)` returns `level[center_y // TILE_HEIGHT][center_x // TILE_WIDTH]`; `is_walkable(tile_code)` returns `tile_code < 3` (the project-wide walkability idiom); `in_box(x, y, bounds)` unpacks `x_lo, x_hi, y_lo, y_hi = bounds` and returns `x_lo < x < x_hi and y_lo < y < y_hi`.

    Substitute literals (REF-01) — replace num1->TILE_HEIGHT, num2->TILE_WIDTH, num3->HALF_TILE everywhere they are recomputed inline: ghost.py check_collisions L45-47, game.py draw_board L130-131, game.py has_dot_nearby L196-197, game.py check_collisions L209-210, player.py check_position L41-43. In ghost.py:111 replace `350 < self.x_pos < 550 and 360 < self.y_pos < 480` with `in_box(self.x_pos, self.y_pos, GHOST_BOX_BOUNDS_COLLISION)`. In each of the 8 game.py get_targets box checks replace `340 < self.<ghost>_x < 560 and 340 < self.<ghost>_y < 500` with `in_box(self.<ghost>_x, self.<ghost>_y, GHOST_BOX_BOUNDS_TARGET)`. Replace ghost.py guard `0 < self.center_x // 30 < 29` with `0 < self.center_x // TILE_WIDTH < BOARD_COLS - 1` and player.py guard `centerx // 30 < 29` with `centerx // TILE_WIDTH < BOARD_COLS - 1` (byte-identical: TILE_WIDTH==30, BOARD_COLS-1==29). Use geometry.tile_at / is_walkable ONLY where the read is the trivially-atomic `level[cy//num1][cx//num2]` / `< 3` form. LANDMINE D-15/Pitfall 6: do NOT factor the alignment-band/guard logic of check_collisions or check_position into a shared function — they are structurally divergent (different offsets/guards/gate-9 logic). Share ONLY tile_at/is_walkable/in_box. LANDMINE D-13: LEAVE the draw_board cosmetic literals inline (circle radii 4/10 L135-137, PI fractions, line width 3, the 0.5*/0.4* arc offsets L144-161) — they are rendering, not geometry. Add the geometry imports to game.py/ghost.py/player.py by top-level name (`from geometry import in_box, tile_at, is_walkable, GHOST_BOX_BOUNDS_COLLISION, GHOST_BOX_BOUNDS_TARGET`); avoid any player->ghost import (D-16). Scatter-target literals in get_targets/return_target may be replaced with the SCATTER_* names (caught by the golden trace `target` field, Phase 1 D-03).
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "import settings; assert settings.TILE_HEIGHT==28 and settings.TILE_WIDTH==30 and settings.HALF_TILE==15, (settings.TILE_HEIGHT, settings.TILE_WIDTH, settings.HALF_TILE); import geometry; assert geometry.GHOST_BOX_BOUNDS_COLLISION==(350,550,360,480) and geometry.GHOST_BOX_BOUNDS_TARGET==(340,560,340,500); assert geometry.in_box(400,420,geometry.GHOST_BOX_BOUNDS_COLLISION) is True and geometry.in_box(0,0,geometry.GHOST_BOX_BOUNDS_COLLISION) is False; print('OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_ghost_micro.py tests/test_golden_traces.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import settings; print(settings.TILE_HEIGHT, settings.TILE_WIDTH, settings.HALF_TILE)"` prints `28 30 15`
    - `settings.py` contains the literal `TILE_HEIGHT = (HEIGHT - HUD_HEIGHT) // BOARD_ROWS`
    - `geometry.py` contains `def in_box(`, `def tile_at(`, `def is_walkable(` and both `GHOST_BOX_BOUNDS_COLLISION` and `GHOST_BOX_BOUNDS_TARGET` as DISTINct tuples `(350, 550, 360, 480)` and `(340, 560, 340, 500)`
    - `grep -nv '^[[:space:]]*#' ghost.py game.py player.py | grep -c 'num1\|num2\|num3'` returns 0 (no inline tile-math literals remain in non-comment lines) EXCEPT the deliberate `# was num1` style comments
    - `pytest tests/test_ghost_micro.py -q` exits 0 (all 15 micro tests pass, untouched)
    - `pytest tests/test_golden_traces.py -q` exits 0 (all 9 golden traces replay byte-identical + invariants green)
    - `grep -c 'GHOST_BOX_BOUNDS' geometry.py` shows the two bounds are NOT collapsed into one constant (Phase 3 owns unification)
    - draw_board cosmetic literals (`4`, `10`, `0.5 *`, `0.4 *`, `, 3)`) remain inline in game.py (NOT centralized)
  </acceptance_criteria>
  <done>settings.py has the named tile/wrap/scatter constants; geometry.py exists with the three helpers + two distinct box constants; all num1/num2/num3 inline recomputations are replaced by the centralized names across game/ghost/player; the 9 golden traces and 15 micro tests are byte-identical green; the two box rectangles remain TWO separate constants.</done>
</task>

<task type="auto">
  <name>Task 2: check_collisions differential oracle (D-07) — frozen OLD copy + synthetic exhaustive proof</name>
  <files>tests/_legacy_geometry.py, tests/test_check_collisions_oracle.py</files>
  <read_first>
    - ghost.py (the NOW-CENTRALIZED check_collisions L43-115 — the NEW implementation under test)
    - tests/test_ghost_micro.py (make_ghost L41-57 + screen fixture L27-38 — the exact headless harness to reuse; Pitfall 4 deep-copy)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-4: enumeration = board positions × direction × in_box × dead, ~46k cases; tile-center + half-tile + alignment-band positions; one-shot-then-delete lifecycle; programmatic loops not parametrize)
    - .planning/phases/02-safe-refactor/02-PATTERNS.md (test_mover/check_collisions oracle analog = test_ghost_micro.py; exact-tuple compare style)
  </read_first>
  <action>
    BEFORE this oracle can compare, the frozen OLD must exist: create tests/_legacy_geometry.py containing a verbatim copy of the PRE-REF-01 `check_collisions` body (the original with inline `num1=(HEIGHT-50)//32`, `num2=WIDTH//30`, `num3=15` and the literal box test `350 < x_pos < 550 and 360 < y_pos < 480`) as a standalone function `legacy_check_collisions(g)` that operates on a passed ghost-like object `g` (reads g.center_x/center_y/level/direction/in_box/dead/x_pos/y_pos, returns `(turns, in_box)`). This is the frozen oracle the design's one-shot-gate requires — capture it in the SAME commit as the oracle test (D-07). It is DELETED in Plan 02 after green; keep it self-contained (no import from ghost.py so it cannot drift with the refactored code).

    Create tests/test_check_collisions_oracle.py reusing the test_ghost_micro.py `screen` fixture and `make_ghost` helper (import or re-declare per house style). Enumerate the input space with programmatic `itertools.product` loops (NOT pytest.mark.parametrize — ~46k cases would bloat collection per RESEARCH FOCUS-3/4): for each board tile (row, col) over BOARD_ROWS×BOARD_COLS, test x/y at the tile origin AND at origin+HALF_TILE AND at the alignment-band position where `center % TILE_WIDTH == 15` / `center % TILE_HEIGHT == 15` (the precedence-sensitive `12 <= center % num <= 18` branches), crossed with direction(0-3) × in_box(False,True) × dead(False,True). For each case construct a ghost via make_ghost, run BOTH `legacy_check_collisions(clone)` and `g.check_collisions()`, and assert the returned `(turns, in_box)` tuples are EXACTLY equal (exact-integer/bool compare, mirroring the micro-test style). Accumulate the FIRST few divergences into a list and assert `not failures` with a readable first-divergence message (RESEARCH FOCUS-3 pattern). Operate each case on a fresh make_ghost (deep-copied board per Pitfall 4/5) so no board mutation leaks between cases.
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_check_collisions_oracle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/_legacy_geometry.py` contains `def legacy_check_collisions(` and inlines the ORIGINAL literals (`(HEIGHT - 50) // 32`, `WIDTH // 30`, `350 < `, `360 < `) — it does NOT import the refactored ghost.check_collisions
    - `tests/test_check_collisions_oracle.py` uses `itertools.product` (not `@pytest.mark.parametrize` over the full space) and compares `(turns, in_box)` with `==`
    - `pytest tests/test_check_collisions_oracle.py -q` exits 0 (OLD == NEW for every enumerated case → centralization is behavior-preserving for turn-legality + the in_box flag)
    - The enumeration crosses direction × in_box × dead × board positions including the alignment-band offset (`% TILE_WIDTH == 15`) cases
    - The oracle runs in well under the CI budget (single `pytest` invocation completes; RESEARCH estimates < a few seconds for ~46k pure-arithmetic cases)
  </acceptance_criteria>
  <done>The frozen legacy check_collisions is captured standalone in tests/_legacy_geometry.py; the differential oracle enumerates the board/direction/in_box/dead space exhaustively and proves OLD == NEW for `(turns, in_box)`, locking the geometry centralization (and the in_box flag Phase 3 later edits) as byte-identical. (Deletion of the legacy copy + this oracle happens in Plan 02 after all proofs are green — D-06/D-07 lifecycle.)</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic frame-hash check (D-08/D-09) — sha256 over tobytes + committed manifest, wired to --bless</name>
  <files>tests/test_frame_hash.py, tests/golden/box_exit/frame_hashes.txt, tests/golden/power_chase/frame_hashes.txt, tests/golden/ghost_eat/frame_hashes.txt, tests/golden/death/frame_hashes.txt, tests/golden/win/frame_hashes.txt, tests/golden/box_edge/frame_hashes.txt, tests/golden/tunnel_wrap/frame_hashes.txt, tests/golden/eyes_return/frame_hashes.txt, tests/golden/claude_session_01/frame_hashes.txt</files>
  <read_first>
    - tests/test_golden_traces.py (the --bless branch L195-205; replay_scenario L88-94; MANIFEST L51; the _trace_path / golden dir layout L58-59)
    - tests/conftest.py (--bless registration L59-66; _ensure_default_font L31-56 — why the font is byte-stable in the pinned env)
    - harness/capture.py (save_png uses pygame.image.save; the artifacts dir is gitignored — frame_hashes.txt is COMMITTED text, NOT under tests/artifacts/)
    - tests/golden/manifest.json (the 9 scenarios + frame_cap; death=4200 frames → sample, do not hash every frame)
    - .planning/phases/02-safe-refactor/02-RESEARCH.md (FOCUS-5: hashlib.sha256(pygame.image.tobytes(surface,"RGB")).hexdigest(); RGB not RGBA; pinned-env-only assertion; sample every Nth frame + terminal; A1 risk note — geometry bugs shift ALL frames so any sample catches them)
    - .planning/phases/02-safe-refactor/02-PATTERNS.md (test_frame_hash analog = test_golden_traces.py --bless idiom + tile-math mirror; Pitfall 4 pinned-env-only)
  </read_first>
  <action>
    Create tests/test_frame_hash.py (separate file for clean bless-write logic, per RESEARCH FOCUS-5 recommendation + Open Question 1). Define a helper `frame_hash(pygame, surface)` returning `hashlib.sha256(pygame.image.tobytes(surface, "RGB")).hexdigest()` — use format "RGB" (the screen is opaque, alpha carries no info) and stdlib hashlib (zero new dep — do NOT add numpy/Pillow for hashing). Replay each golden scenario via the existing replay machinery (reuse test_golden_traces.py's replay model: run the per-frame body, but capture `frame_hash(pygame, game.screen)` for a SAMPLED subset of frames — every 20th frame plus the terminal frame — to keep the manifest small for the 4200-frame death scenario; record `frame_index<space>sha256hex` lines). Write each scenario's hashes to a COMMITTED text manifest at `tests/golden/<scenario>/frame_hashes.txt` (alongside the existing trace.jsonl — NOT under the gitignored tests/artifacts/). Wire into the existing `--bless` flow: when `request.config.getoption("--bless")` is set, (re)write frame_hashes.txt and return WITHOUT asserting (mirror test_golden_traces.py L195-205); otherwise read the committed manifest and assert every sampled replayed frame hash equals the committed hash.

    PLATFORM-STABILITY GUARD (D-09 / Pitfall 4): the frame-hash is byte-stable ONLY in the Linux pinned bless env (font rasterization of freesansbold.ttf differs across SDL builds; conftest._ensure_default_font guarantees the bundled font is present, but pixel rasterization is platform-bound). The committed manifest is the Linux-blessed truth. Make the assertion robust to non-pinned dev runs: gate the strict equality assertion to run only when an env marker indicating the pinned CI/bless env is set (e.g. honor a `GSD_FRAME_HASH_ENV` or check `os.environ.get("CI")`), and `pytest.skip("frame-hash asserted only in the Linux pinned bless env (D-09); regenerate locally via --bless")` otherwise — so a Windows dev `pytest` run does NOT false-red, while CI (the bless env) DOES assert. Document this contract in the module docstring. To produce the initial committed manifest, the executor runs the bless flow IN the pinned env (or notes the manifest must be blessed in CI); since this is authored on Windows, the executor MUST generate the manifest by running the bless flow under the same SDL-dummy + bundled-font conditions and clearly flag in the SUMMARY that the committed hashes are CI-blessed truth and CI is the assertion authority.
  </action>
  <verify>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -c "import hashlib, pygame; print('hashlib+pygame OK')" && python -c "import ast,sys; src=open('tests/test_frame_hash.py').read(); assert 'sha256' in src and 'tobytes' in src and 'RGB' in src and 'frame_hashes' in src, 'missing required tokens'; print('frame_hash test shape OK')"</automated>
    <automated>cd "C:/Users/James/desktop/projects/pacman-firebase" && python -m pytest tests/test_frame_hash.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_frame_hash.py` contains `hashlib.sha256(`, `pygame.image.tobytes(`, the literal `"RGB"`, and writes/reads `frame_hashes.txt`
    - The bless branch mirrors test_golden_traces.py: under `--bless` it (re)writes the manifest and returns without asserting; otherwise it reads and asserts equality
    - The strict frame-hash equality assertion is gated to the pinned/CI env and `pytest.skip(...)`s on non-pinned dev (no false-red on Windows) — documented in the module docstring
    - Committed manifests `tests/golden/<scenario>/frame_hashes.txt` exist for the golden scenarios and contain `<int> <64-hex>` lines, are NOT under the gitignored tests/artifacts/, and ARE tracked by git
    - `pytest tests/test_frame_hash.py -q` exits 0 (asserts in CI; skips-clean on dev)
    - No new package added (hashlib stdlib, pygame already pinned) — `requirements-dev.txt` unchanged
  </acceptance_criteria>
  <done>A deterministic frame-hash check exists (sha256 over pygame.image.tobytes RGB), wired into the existing --bless flow, with a committed Linux-blessed TEXT manifest per golden scenario; it asserts pixel byte-identity in the pinned CI env and skips cleanly on non-pinned dev — closing the "geometry bug shifts rendering while the state trace stays byte-identical" gap (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production game logic ↔ test harness | The only boundary this plan touches. The harness reads game state; it never feeds untrusted external input into the game. |

**Finding (per planning_context security note):** This plan is a **pure internal refactor** — centralizing geometry constants and adding test-only oracles/hashing. It introduces **NO new attack surface**: no network I/O, no untrusted input parsing, no auth/session/crypto, no new persisted user data, no new packages. The cloud-function validators (initials regex, score range) and `api_service.py` are NOT touched. No new trust boundary is created.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (integrity of behavior) | geometry centralization in game/ghost/player | mitigate | The ONLY meaningful "integrity" concern is behavior-preservation. Guarded three ways: the `check_collisions` differential oracle (logic), the frame-hash manifest (pixels), and the inherited 9 golden traces + 15 micro tests — all must stay green. This is verification, not an external attacker. |
| T-02-02 | Tampering | npm/pip/cargo installs | accept | No packages installed this plan (RESEARCH: "Package Legitimacy Gate: N/A — zero new packages"). hashlib is stdlib; pygame/pytest already pinned. No supply-chain surface added. |
| T-02-03 | Information Disclosure / Spoofing / Repudiation / DoS / EoP | (whole plan) | accept | Not applicable — no untrusted input crosses any boundary this plan touches; local single-player game logic + test code only (RESEARCH Security Domain: V2-V6 all N/A at ASVS L1). |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist (zero installs) — no `gate="blocking-human"` checkpoint required.
</threat_model>

<verification>
- `pytest tests/test_ghost_micro.py tests/test_golden_traces.py -q` exits 0 (the inherited net stays byte-identical green AFTER substitution)
- `pytest tests/test_check_collisions_oracle.py -q` exits 0 (geometry centralization proven behavior-preserving for `(turns, in_box)` exhaustively)
- `pytest tests/test_frame_hash.py -q` exits 0 (rendering proven byte-identical in the pinned env; skips-clean on dev)
- `python -c "import settings, geometry"` succeeds and the asserted constant values hold (TILE_HEIGHT=28, TILE_WIDTH=30, HALF_TILE=15; two distinct box constants)
- No inline `num1/num2/num3` recomputation remains in non-comment lines of game/ghost/player
- The two box rectangles remain TWO distinct named constants (NOT unified — Phase 3 owns BUG-01)
</verification>

<success_criteria>
ROADMAP Phase 2 SC#1 is satisfied for this plan: tile/board geometry is centralized, inline magic numbers are removed, the full golden-master trace replays byte-for-byte unchanged, AND the two inconsistent box-region definitions are preserved as two separate named constants (unifying them is Phase 3). REF-01 is fully implemented and proven by two complementary modalities (logic oracle + pixel hash) on top of the inherited net. The geometry-first D-11 gate is satisfied so Plan 02 (the mover refactor) may begin.
</success_criteria>

<output>
Create `.planning/phases/02-safe-refactor/02-01-SUMMARY.md` when done. Record: the exact constant names/values added; the geometry.py helper signatures; confirmation the two box constants stay distinct; the oracle case-count + runtime; the frame-hash manifest scenarios + the CI-is-assertion-authority note; and that the legacy geometry copy + oracle are scheduled for deletion in Plan 02 (D-06/D-07 one-shot lifecycle).
</output>
