---
phase: 01-test-safety-net
plan: 05
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - tests/test_ghost_micro.py
autonomous: true
requirements: [TST-02]
user_setup: []

must_haves:
  truths:
    - "Ghost objects are constructed headlessly against an SDL-dummy surface with no change to ghost.py (D-14)"
    - "check_collisions and each of move_blinky/move_inky/move_pinky/move_clyde are asserted at hand-curated decisive board states (multi-way intersection, tunnel mouth, box edge, flee-vs-chase) (D-13)"
    - "Each assertion pins the EXACT (x, y, dir) the move_* method returns, so a failure names the exact ghost and situation"
    - "Tests deep-copy board.boards so dots/state never leak between cases (Pitfall 4)"
  artifacts:
    - path: "tests/test_ghost_micro.py"
      provides: "Headless Ghost construction helper + decisive-state characterization tests for check_collisions + the four move_* methods"
      min_lines: 60
  key_links:
    - from: "tests/test_ghost_micro.py"
      to: "ghost.Ghost.move_blinky/move_inky/move_pinky/move_clyde/check_collisions"
      via: "construct Ghost headless, call the method, assert the exact return"
      pattern: "move_(blinky|inky|pinky|clyde)"
    - from: "tests/test_ghost_micro.py"
      to: "copy.deepcopy(board.boards)"
      via: "per-case board to avoid dot leakage"
      pattern: "deepcopy"
---

<objective>
Lock in the per-ghost decision behavior with micro characterization tests (TST-02). Construct each
`Ghost` headlessly against the SDL-dummy surface (D-14, NO change to `ghost.py`) and assert the EXACT
turn (`check_collisions` and each `move_*` return) at hand-curated decisive board states (D-13): a
multi-way intersection, a tunnel mouth, a box edge, and a flee-vs-chase target — one decisive case per
ghost minimum, plus `check_collisions`. A failure here names the exact ghost and the exact situation.

Purpose: This is the "micro per-decision" leg of the maximum-paranoia bar — when Phase 2 collapses the
4x movement duplication, these tests pin the per-ghost return values so any drift points at the specific
ghost + state, complementing the whole-trace golden diff.
Output: tests/test_ghost_micro.py.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-safety-net/01-RESEARCH.md
@.planning/phases/01-test-safety-net/01-PATTERNS.md
@.planning/phases/01-test-safety-net/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (exclude from source-drift checks):
- `tests/test_ghost_micro.py` (NEW): `make_ghost(...)` headless-construction helper + `test_*` cases for check_collisions and each move_* method.
Pre-existing symbols READ (do NOT modify): `ghost.Ghost.__init__`, `Ghost.check_collisions`, `Ghost.move_blinky/move_inky/move_pinky/move_clyde`, `board.boards`, `harness.headless.init_headless`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Headless Ghost construction helper + check_collisions characterization</name>
  <read_first>
    - ghost.py (lines 1-50 — Ghost.__init__ signature: x_coord, y_coord, target, speed, img, direct, dead, box, id, screen, powerup, eaten_ghost, spooked_img, dead_img, level; __init__ calls check_collisions() and draw(); check_collisions tile math around line 43; the box-bound check 350<x<550 & 360<y<480 around line 111)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 7 — make_ghost helper with dummy Surface((45,45)) images and a deep-copied real board; D-14 headless construction; Construction notes)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (tests/test_ghost_micro.py — fixture style from test_local_storage.py; the exact Ghost signature; Pitfall 4 deep-copy board)
    - board.py (the boards grid + tile codes 0 empty,1 dot,2 big dot,3 vertical wall,4 horizontal wall,5-8 corners,9 gate — to choose decisive tile positions)
    - tests/test_local_storage.py (the plain-pytest + fixture house style to mirror)
    - harness/headless.py (init_headless — provides the SDL-dummy screen for construction)
  </read_first>
  <action>
    Write tests/test_ghost_micro.py. Add a module-level headless screen (via harness.headless.init_headless
    or a local SDL-dummy init mirroring it) and a make_ghost(...) helper that builds a Ghost from the full
    __init__ signature (ghost.py:6-7), passing dummy pygame.Surface((45,45)) objects for img/spooked_img/
    dead_img, an eaten_ghost list [False,False,False,False], and a copy.deepcopy(board.boards) as level
    (Pitfall 4 — never pass the shared module list). Add characterization tests for check_collisions:
    construct a ghost at a chosen tile and assert the EXACT turn-availability / box flags it computes
    (assert against the ghost's actual current returned values — read ghost.py to capture them, then pin
    them as the expected literals). Do NOT modify ghost.py. If a method returns a tuple, assert the full
    tuple of integers; if it sets attributes (box flag), assert those attributes. Capture the CURRENT
    behavior as the expected values (characterization, not correction).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ghost_micro.py -q -k collisions</automated>
  </verify>
  <acceptance_criteria>
    - make_ghost constructs a Ghost headless with no display error and no change to ghost.py (git diff shows ghost.py unmodified).
    - make_ghost passes a copy.deepcopy(board.boards) as level (grep: deepcopy present; the shared board.boards is never passed by reference).
    - At least one check_collisions test asserts the EXACT turn-availability/box-flag values the current ghost.py produces (literal integers/bools, not a range or truthiness).
    - pytest tests/test_ghost_micro.py -k collisions exits 0.
  </acceptance_criteria>
  <done>Ghosts construct headless with no ghost.py change; check_collisions behavior is pinned at decisive states with exact expected values.</done>
</task>

<task type="auto">
  <name>Task 2: Decisive-state characterization for move_blinky/inky/pinky/clyde</name>
  <read_first>
    - ghost.py (move_blinky around line 256, move_inky around line 362, move_pinky around line 484, move_clyde around line 117 — read each method to capture the EXACT (x,y,dir) it returns at the chosen states; move_clyde also serves as the dead/in-box fallback per CLAUDE.md; tunnel-wrap handling around lines 250-253)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 7 — the four decisive board states per ghost: multi-way intersection, tunnel mouth, box edge, flee-vs-chase using runaway_x/y vs the get_targets target; assert the exact (x,y,dir))
    - .planning/phases/01-test-safety-net/01-CONTEXT.md (D-13 — hand-curate decisive states per ghost informed by states the long golden session visits; assert exact turn; NOT auto-sampled from traces)
    - tests/test_ghost_micro.py (Task 1 — reuse make_ghost and the headless screen)
    - settings.py (ghost speeds set in update_ghost_speeds are in {1,2,4}; pick a speed consistent with the state)
  </read_first>
  <action>
    Extend tests/test_ghost_micro.py with decisive-state tests for each of move_blinky, move_inky,
    move_pinky, move_clyde (at minimum one decisive case each; add the four state archetypes —
    multi-way intersection, tunnel mouth, box edge, flee-vs-chase — across the suite per D-13). For each:
    build the ghost via make_ghost at the curated x/y/direction/target/speed/dead/box/powerup, call the
    move_* method, and assert the EXACT returned (x, y, dir) tuple of integers the CURRENT ghost.py
    produces (run the method first to read the value, then pin it as the expected literal — this is
    characterization of today's behavior, the SPEC). Name tests so failures self-describe the ghost +
    situation (e.g. test_pinky_tunnel_mouth_wraps, test_clyde_flees_when_close). Include a flee-vs-chase
    case that toggles powerup so the ghost targets runaway_x/runaway_y vs the Pac-Man target. Deep-copy
    the board per case. Do NOT modify ghost.py; do NOT assert "improved"/arcade-accurate behavior — pin
    whatever the code does now.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ghost_micro.py -q</automated>
  </verify>
  <acceptance_criteria>
    - There is at least one passing characterization test for each of move_blinky, move_inky, move_pinky, move_clyde, each asserting an exact (x, y, dir) integer tuple.
    - The four decisive state archetypes (multi-way intersection, tunnel mouth, box edge, flee-vs-chase) each appear in at least one test (verified by test names / inline comments referencing the archetype).
    - At least one flee-vs-chase test sets powerup True and asserts the ghost moves toward the runaway target (not the chase target).
    - ghost.py is unmodified (git diff clean for ghost.py).
    - pytest tests/test_ghost_micro.py exits 0.
  </acceptance_criteria>
  <done>Each move_* method is pinned at a decisive board state with the exact (x,y,dir) it returns today; failures name the precise ghost + situation; ghost.py untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Tests construct in-process Ghost objects against a dummy surface; no network, no untrusted input, no disk writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T | Tampering | a test accidentally mutates the shared board.boards, leaking state and masking a real regression | mitigate | Every case deep-copies board.boards (Pitfall 4); the shared module list is never passed by reference |

No new runtime attack surface — tests/test_ghost_micro.py is a dev/test-only file that reads ghost.py
behavior without modifying it; it does not ship in the .exe.
</threat_model>

<verification>
- Ghost constructs headless with no ghost.py change.
- check_collisions + all four move_* methods are pinned at decisive states with exact expected values.
- Flee-vs-chase, tunnel-mouth, box-edge, and intersection archetypes are covered.
- Board is deep-copied per case; pytest tests/test_ghost_micro.py exits 0.
</verification>

<success_criteria>
- TST-02: micro characterization tests assert current per-ghost decisions (check_collisions, move_blinky/inky/pinky/clyde) at hand-curated decisive states; a failure names the exact ghost + situation; ghost.py is unmodified (D-14).
</success_criteria>

<output>
Create `.planning/phases/01-test-safety-net/01-05-SUMMARY.md` when done.
</output>
