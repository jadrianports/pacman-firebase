---
phase: 01-test-safety-net
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - game.py
  - harness/trace.py
  - harness/replay.py
autonomous: true
requirements: [HRN-01, HRN-02]
user_setup: []

must_haves:
  truths:
    - "The body of Game.run()'s while-loop (game.py lines 464-552) is extracted into a tick() method, leaving the while/return_to_menu/timer.tick(FPS) scaffolding in run()"
    - "tick() never calls self.timer.tick (the throttle stays only in run()), so the harness is sleep-independent"
    - "The extracted tick() reproduces a baseline trace captured from the UNMODIFIED run() loop byte-for-byte"
    - "capture_state(game) reads only existing Game/Ghost state and produces the D-03 per-frame snapshot (frame, pacman, 4 ghosts incl. target, score, lives, powerup, dots_remaining, game_over, game_won)"
    - "run_scenario injects sparse {frame,key} events via pygame.event.post and replays a scenario to a natural terminal state bounded by a frame cap that fails loudly on soft-lock"
  artifacts:
    - path: "game.py"
      provides: "tick() method (one frame of update) + slimmed run() that only owns the loop/throttle"
      contains: "def tick"
    - path: "harness/trace.py"
      provides: "capture_state(game)->dict, write_jsonl, read_jsonl, traces_equal/diff helpers"
      min_lines: 30
    - path: "harness/replay.py"
      provides: "load_events + run_scenario(game, pygame, input_path, frame_cap, capture_state) driver"
      min_lines: 25
  key_links:
    - from: "game.py run()"
      to: "game.py tick()"
      via: "run() calls self.tick() inside the while loop after timer.tick(FPS)"
      pattern: "self\\.tick\\(\\)"
    - from: "harness/replay.py"
      to: "game.tick()"
      via: "driver calls game.tick() each frame after posting events"
      pattern: "\\.tick\\(\\)"
    - from: "harness/replay.py"
      to: "pygame.event.post"
      via: "sparse {frame,key} events injected into the real input path"
      pattern: "event\\.post"
    - from: "harness/trace.py"
      to: "game.targets / get_targets()"
      via: "capture_state records each ghost's target tuple"
      pattern: "target"
---

<objective>
Perform the single most important, most delicate change of the phase: extract a steppable `tick()`
from `Game.run()`'s while-body as a BEHAVIOR-PRESERVING change to `game.py` ONLY, validated
baseline-trace-first. Build the trace schema (`harness/trace.py`, a pure reader of existing Game/Ghost
state per D-03/D-04/D-05) and the record/replay driver (`harness/replay.py`, sparse `{frame,key}`
injection via `pygame.event.post`, frame-capped soft-lock backstop per D-18/D-19).

Purpose: `tick()` is the seam the entire harness drives; the trace schema is the golden-master format;
the replay driver is how every scenario (Plan 04) reproduces frozen behavior. Getting the extraction
byte-identical is what makes Phase 2's "mathematically unchanged" claim possible.
Output: `game.py` (modified — tick() extracted), `harness/trace.py`, `harness/replay.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-safety-net/01-RESEARCH.md
@.planning/phases/01-test-safety-net/01-PATTERNS.md
@.planning/phases/01-test-safety-net/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates/modifies these NEW symbols (exclude from source-drift checks — they do not exist before this plan):
- `Game.tick()` method on game.py (NEW — extracted from run())
- `Game._frame` attribute (NEW — set by the replay driver each frame, NOT added to game.py logic)
- `harness/trace.py` (NEW): `capture_state(game)`, `write_jsonl(trace, path)`, `read_jsonl(path)`, `traces_equal(a, b)`, `diff_traces(a, b)`
- `harness/replay.py` (NEW): `load_events(path)`, `KEYMAP`, `run_scenario(game, pygame, input_path, frame_cap, capture_state, on_frame=None)`
Pre-existing symbols this plan READS (do not modify): `Game.run`, `Game.get_targets`, `Game.handle_events`, ghost flat attrs (`*_x/*_y/*_direction/*_dead/*_box`), `Game.level`, `Game.player`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Capture a baseline trace from the UNMODIFIED run() loop, then build the trace schema</name>
  <read_first>
    - game.py (lines 456-555 — the FULL run() loop, in order: counter, eat-freeze, powerup timer, starting/dying phases, draw block, update_ghost_speeds, check_win, player_circle guard at 519-520, create_ghosts, get_targets at 531, box-exit timers, player.check_position/move, move_ghosts, check_collisions/check_ghost_collisions at 542-544, handle_events at 547, update_direction, wrap_around, check_ghost_in_box, display.flip at 552)
    - game.py (lines 1-100 — Game.__init__: copy.deepcopy(boards) at 23, flat ghost attrs at 38-57, fonts at 95-98)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 1 baseline-trace-first steps 1-5; Pattern 3 trace schema with verified field sources; Pitfall 4 deep-copy board)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (harness/trace.py section — every trace field's verified source; game.py:531 sets self.targets)
    - harness/headless.py (init_headless, from Plan 01 — used to build a headless Game in the baseline harness)
  </read_first>
  <action>
    Write harness/trace.py with: capture_state(g) returning the EXACT D-03 schema — keys: "frame"
    (reads g._frame, set by the driver), "pacman" with x/y/dir (g.player.x/y/direction),
    "ghosts" as a list of four dicts in fixed order blinky, inky, pinky, clyde each with
    name/x/y/dir/dead/box/target read from the flat Game attrs (g.{name}_x etc.) and list(g.targets[i])
    for target, "score", "lives", "powerup", "dots_remaining" (sum of tile codes 1 and 2 across g.level
    rows), "game_over", "game_won". Do NOT capture internal counters per D-04 (no box-exit timers, no
    power_counter, no eat_freeze/starting/dying/flicker/counter). Add write_jsonl(trace, path)
    (one json.dumps per frame, newline-delimited, sort_keys=True for stable diffs), read_jsonl(path),
    traces_equal(a, b) (exact list-of-dict equality per D-05), and diff_traces(a, b) returning a
    human-readable per-frame/per-field diff string (used by --bless in Plan 04). All values are
    integers/bools per D-05 — no float rounding.
    Then PROVE the schema against the unmodified loop: write a throwaway harness script that drives a
    headless Game through the UNMODIFIED run() flow with the box_exit input for a few hundred frames
    (instrument by temporarily appending a per-frame capture_state dump AFTER the existing display.flip
    in a copy of the flow, OR run run() with a transient monkeypatch shim) and write the result as a
    BASELINE trace under tests/artifacts/_baseline/. Keep this baseline file; Task 2 asserts the
    extracted tick() reproduces it. Do NOT leave any capture_state instrumentation inside game.py after
    this task.
  </action>
  <verify>
    <automated>python tests/scratch_trace_schema_check.py</automated>
  </verify>
  <acceptance_criteria>
    - capture_state returns exactly the 9 top-level keys (frame, pacman, ghosts, score, lives, powerup, dots_remaining, game_over, game_won); ghosts is a 4-element list ordered blinky,inky,pinky,clyde; each ghost dict has keys name,x,y,dir,dead,box,target.
    - No D-04 internal counter appears in the snapshot (power_counter, eat_freeze, starting, dying, flicker, counter, box-exit timers all absent).
    - dots_remaining equals the sum over g.level rows of row.count(1)+row.count(2).
    - A baseline trace file exists under tests/artifacts/_baseline/ produced from the UNMODIFIED run() flow.
    - game.py contains no leftover capture_state instrumentation (grep -c capture_state on game.py returns 0).
    - write_jsonl followed by read_jsonl round-trips equal under traces_equal.
  </acceptance_criteria>
  <done>harness/trace.py produces the D-03 schema as a pure reader; a baseline trace from the unmodified run() loop is captured and saved; game.py has no leftover instrumentation.</done>
</task>

<task type="auto">
  <name>Task 2: Extract tick() from run() (behavior-preserving) and prove byte-identical to baseline</name>
  <read_first>
    - game.py (lines 456-555 — re-read immediately before editing to see CURRENT state, not assumptions)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 1 the exact split: lines 464-552 verbatim become tick(); timer.tick(FPS) stays in run() only per D-17; handle_events return propagates as tick()'s return; Pitfall 1 do not reorder; Verified gotchas do not hoist player_circle)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (game.py tick() extraction — the exact tail lines 546-552 order; the player_circle guard at 519-520/539-544 to preserve)
    - harness/trace.py (capture_state — Task 1 — used to compare extracted-tick trace vs baseline)
    - tests/artifacts/_baseline/ (the baseline trace from Task 1 — the reference to reproduce)
  </read_first>
  <action>
    Split Game.run() (game.py:456-555). Move the loop body — game.py lines 464 through 552 VERBATIM,
    in the SAME statement order — into a new method def tick(self). The tick() method must:
    (a) NOT contain self.timer.tick(FPS) (that line stays in run() ONLY, per D-17 — tick() must be
    sleep-independent and must never read the throttle return value); (b) end with pygame.display.flip()
    as its last statement; (c) capture the result of running = self.handle_events() (was line 547) and
    return running so a pygame.QUIT propagates out (mirrors handle_events returning False on QUIT).
    Preserve the order-dependent player_circle guard EXACTLY: player_circle is defined only inside
    `if not self.eat_freeze:` and referenced only inside `if not self.dying and not self.eat_freeze and
    not self.starting:` — do NOT hoist or pre-initialize it.
    Rewrite run() to keep ONLY the scaffolding: running=True; the while loop; the return_to_menu early
    return; self.timer.tick(FPS); running = self.tick(); then self.sound.stop_all(); return None.
    Do NOT add self._frame inside tick() or game.py — the replay driver sets g._frame externally.
    Do NOT modify ghost.py, player.py, or any targeting/movement logic — game.py ONLY.
    Then replay the SAME box_exit input through tick() (the same headless flow used for the baseline)
    and assert the resulting trace == the Task-1 baseline, byte-for-byte (traces_equal). If they differ,
    the extraction reordered something — fix the ORDER until identical; do NOT re-bless the baseline to match.
  </action>
  <verify>
    <automated>python tests/scratch_tick_baseline_check.py</automated>
  </verify>
  <acceptance_criteria>
    - game.Game.tick exists and its source does NOT contain timer.tick (D-17 satisfied by construction).
    - The last executable statement of tick() is pygame.display.flip() immediately followed by return running.
    - game.Game.run source contains both self.timer.tick( (throttle stays) and self.tick() (delegates the body).
    - tick() source contains the player_circle guard unchanged (the if not self.eat_freeze block defines player_circle; the check_ghost_collisions(player_circle) call is inside the not dying / not eat_freeze / not starting guard).
    - Replaying the box_exit input through tick() yields a trace EQUAL to the Task-1 baseline (traces_equal returns True; diff_traces is empty).
    - ghost.py and player.py are unmodified (git diff shows source changes ONLY in game.py).
  </acceptance_criteria>
  <done>tick() is the verbatim loop body minus the throttle, returns running, and reproduces the baseline trace byte-for-byte; run() keeps only the loop + timer.tick(FPS); no AI file touched.</done>
</task>

<task type="auto">
  <name>Task 3: Build the record/replay driver (sparse event injection + soft-lock-capped loop)</name>
  <read_first>
    - game.py (handle_events around lines 420-443 — pygame.event.get() drains the queue; key reads event.key == pygame.K_RIGHT etc.; KEYUP resets direction_command to player.direction)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 4 full run_scenario driver; KEYMAP; the for-range(frame_cap)/else soft-lock backstop; frame_cap default 28800; KEYUP nuance and optional type field; Pitfall 5 soft-lock threshold context)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (harness/replay.py section — same loop shape as run() minus throttle, event injection before game.tick(), capture_state after)
    - harness/trace.py (capture_state — recorded each frame by the driver)
    - harness/headless.py (init_headless — the driver runs against a headless Game)
  </read_first>
  <action>
    Write harness/replay.py per RESEARCH Pattern 4. Implement: load_events(input_path) parsing sparse
    JSONL records of shape frame:int, key:str, type:down|up (type defaults to down) into a dict keyed
    by frame; KEYMAP mapping logical names RIGHT/LEFT/UP/DOWN/SPACE to pygame K_* attribute names; and
    run_scenario(game, pygame, input_path, frame_cap, capture_state, on_frame=None) that, for frame in
    range(frame_cap): sets game._frame = frame; posts each scheduled event via
    pygame.event.post(pygame.event.Event(KEYDOWN or KEYUP, key=getattr(pygame, K_name))) BEFORE the
    step (the real input path consumed by handle_events); calls game.tick(); appends capture_state(game)
    to the trace list; calls on_frame(frame, game) if provided (Plan 03 uses this to save PNGs); breaks
    on game.game_over or game.game_won (D-19 natural terminal). The for-loop's else clause (reached only
    if the cap is hit without terminating) MUST raise AssertionError naming the frame_cap — the soft-lock
    backstop that fails loudly instead of hanging CI. Default frame_cap to 28800 but accept a per-call
    override (the manifest sets it per scenario in Plan 04). Inject via event.post ONLY — do NOT mutate
    player.direction_command directly (D-18: exercise the real handle_events path including KEYUP).
  </action>
  <verify>
    <automated>python tests/scratch_replay_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - load_events parses a sparse JSONL file into a frame-keyed dict; a record without a type field defaults to a KEYDOWN.
    - run_scenario returns a list of snapshots; snapshot[0]['frame'] == 0 and every snapshot contains a 'ghosts' list.
    - Events are injected only via pygame.event.post (grep shows event.post present and no direct assignment to player.direction_command in replay.py).
    - A scenario that never terminates within frame_cap raises AssertionError mentioning the cap (soft-lock backstop), verified by running run_scenario with an empty input and a tiny frame_cap on a non-terminating Game.
    - Replaying the box_exit input reproduces the same trace as Task 2 (deterministic — two runs are traces_equal).
  </acceptance_criteria>
  <done>harness/replay.py injects sparse {frame,key} events via event.post, steps tick() each frame, captures the trace, terminates on natural game end, and raises loudly on a frame-cap soft-lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replay driver → Game | Scripted input events cross into the real handle_events path (test-only, no external network/disk trust boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T | Tampering | tick() extraction silently reorders frame effects (a brick disguised as a refactor) | mitigate | Baseline-trace-first: capture the trace from the UNMODIFIED run() before extracting, then assert the extracted tick() reproduces it byte-for-byte; do not modify ghost.py/player.py |
| T-02-D | Denial of Service | a non-terminating scenario hangs CI | mitigate | run_scenario is bounded by frame_cap; the for/else raises AssertionError instead of looping forever |

No new runtime attack surface ships in the .exe — harness/trace.py and harness/replay.py are dev/test-only;
the game.py change is an internal behavior-preserving refactor with no new input surface.
</threat_model>

<verification>
- Baseline trace captured from the unmodified run() loop exists.
- tick() reproduces the baseline byte-for-byte (traces_equal True).
- tick() contains no timer.tick; run() retains the throttle and calls self.tick().
- capture_state produces the D-03 schema; no D-04 internals leak.
- run_scenario injects via event.post, terminates naturally, and raises on a frame-cap soft-lock.
- Among source files, only game.py changed (ghost.py/player.py untouched).
</verification>

<success_criteria>
- HRN-01: game.py runs headless and is steppable via tick(); the extracted loop reproduces the baseline trace byte-for-byte.
- HRN-02: capture_state records the full D-03 per-frame trace and run_scenario replays a scenario deterministically.
- The throttle stays in run() only (D-17); the trace schema honors D-03/D-04/D-05; injection honors D-18; termination/soft-lock honor D-19.
</success_criteria>

<output>
Create `.planning/phases/01-test-safety-net/01-02-SUMMARY.md` when done.
</output>
