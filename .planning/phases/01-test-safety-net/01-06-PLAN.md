---
phase: 01-test-safety-net
plan: 06
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - tests/test_submit_score.py
  - tests/test_get_leaderboard.py
autonomous: true
requirements: [TST-03]
user_setup: []

must_haves:
  truths:
    - "submit_score validator tests assert 400 for bad initials (not ^[A-Z]{3}$), score > MAX_SCORE (500000), non-int score, and missing machine_id"
    - "The is_new_best upsert decision is tested against a mocked transaction/doc: new-best when higher, not-new-best when lower-or-equal"
    - "get_leaderboard returns {entries:[{initials,score}]} from a mocked query.stream()"
    - "Tests import the WORKING-TREE cloud_functions/*/main.py with firebase-admin mocked before import (via the conftest fixtures), never standing up the Firestore emulator (D-15, D-16)"
    - "The @firestore.transactional spike (A2) is resolved: either the decorator works under the mock, or main.firestore.transactional is patched to a pass-through, documented in the SUMMARY"
  artifacts:
    - path: "tests/test_submit_score.py"
      provides: "Validator 400 tests + is_new_best upsert tests via the HTTP entrypoint with mocked firebase"
      min_lines: 50
    - path: "tests/test_get_leaderboard.py"
      provides: "Leaderboard success/empty tests via the HTTP entrypoint with a mocked query.stream()"
      min_lines: 25
  key_links:
    - from: "tests/test_submit_score.py"
      to: "cloud_functions.submit_score.main.submit_score"
      via: "build a flask.Request, call the entrypoint, assert (body, status, headers)"
      pattern: "submit_score\\("
    - from: "tests/test_submit_score.py"
      to: "mocked db.collection().document().get()"
      via: "drive doc.exists / doc.to_dict() for is_new_best"
      pattern: "to_dict"
---

<objective>
Lock in the cloud-function input validators and the best-score upsert as tests (TST-03), using the
firebase-admin pre-import mock fixtures from Plan 01's conftest (D-15) against the WORKING-TREE
`cloud_functions/*/main.py` (D-16). Cover the four 400 rejections (bad initials, over-MAX score,
non-int score, missing machine_id), the `is_new_best` upsert decision via a mocked transaction/doc,
and the `get_leaderboard` success/empty paths via a mocked `query.stream()`. Resolve the
`@firestore.transactional` spike (A2 / Pitfall 2) at the start.

Purpose: These validators are the existing security control surface (V5 Input Validation). Freezing them
as tests is security-positive and cheap; it also documents the contract the More Competitive milestone's
anti-cheat work will build on. No source refactor, no emulator.
Output: tests/test_submit_score.py, tests/test_get_leaderboard.py.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-safety-net/01-RESEARCH.md
@.planning/phases/01-test-safety-net/01-PATTERNS.md
@.planning/phases/01-test-safety-net/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (exclude from source-drift checks):
- `tests/test_submit_score.py` (NEW): make_request helper + validator/upsert tests
- `tests/test_get_leaderboard.py` (NEW): leaderboard success/empty tests
Pre-existing symbols READ (do NOT modify): `cloud_functions.submit_score.main.submit_score` / `_update_score`, `cloud_functions.get_leaderboard.main.get_leaderboard`, conftest fixtures `submit_module` / `leaderboard_module`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the @firestore.transactional spike, then write the submit_score validator + upsert tests</name>
  <read_first>
    - cloud_functions/submit_score/main.py (the WHOLE file — the four validators at lines 47-52: missing machine_id, ^[A-Z]{3}$ initials, isinstance(score,int)/0<=score<=MAX_SCORE=500000; _update_score at 14-25 decorated @firestore.transactional; the 200 return shape {success:True, is_new_best} at 58; the Invalid JSON 400 at 40-41)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 8 — make_request via werkzeug EnvironBuilder + flask.Request; the four 400 tests; the is_new_best mocked-transaction tests; Pitfall 2 / A2 — patch cloud_functions.submit_score.main.firestore.transactional to lambda f: f if the decorator fights the mock; Open Question 1 — 30-min spike, default to patching)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (tests/test_submit_score.py — exact house style of test_api_service.py: call the function, assert the returned tuple; the validators and return shape excerpts)
    - tests/conftest.py (the submit_module fixture from Plan 01 — imports the working-tree module with firebase mocked, exposing _mock_client; this task drives db.collection().document().get())
    - tests/test_api_service.py (the call-and-assert-the-dict house style to mirror)
  </read_first>
  <action>
    SPIKE FIRST (A2): with the submit_module fixture active, attempt an is_new_best test WITHOUT patching
    the decorator. If @firestore.transactional drives the mocked transaction cleanly, keep it. If it
    raises/odd-calls (Pitfall 2 warning signs), patch cloud_functions.submit_score.main.firestore.transactional
    to a pass-through (lambda f: f) inside the submit_module fixture path (or a local patch in the upsert
    tests) so _update_score runs directly against the mocked transaction/doc. Record which path was taken
    in the SUMMARY.
    Then write tests/test_submit_score.py. Add make_request(body, method="POST") building a flask.Request
    via werkzeug.test.EnvironBuilder(method=, json=body) -> flask.Request(builder.get_environ())
    (functions-framework brings Flask/werkzeug). Write the four 400 tests using submit_module.submit_score(req):
    bad initials "ab" -> 400 "Invalid initials"; score 500001 -> 400 "Invalid score"; score "100" (str)
    -> 400 "Invalid score"; missing machine_id -> 400 "Missing machine_id". Add an Invalid JSON test
    (empty/None body -> 400 "Invalid JSON"). Then the is_new_best upsert tests: drive
    submit_module._mock_client.return_value -> db.collection().document().get() returning a snap with
    .exists=True and .to_dict.return_value={"score": N}: higher submitted score -> 200 and is_new_best True;
    lower-or-equal -> is_new_best False; and a no-existing-doc case (.exists=False) -> is_new_best True.
    Assert on the FIRST tuple element (the dict) and the status, mirroring the house style. Do NOT modify
    cloud_functions/*/main.py (D-16: test the working-tree code as-is).
  </action>
  <verify>
    <automated>python -m pytest tests/test_submit_score.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_submit_score.py exits 0.
    - There are passing tests asserting status 400 with error "Invalid initials" (initials "ab"), "Invalid score" (score 500001 and score "100"), and "Missing machine_id" (no machine_id), plus "Invalid JSON" for an empty body.
    - is_new_best is True when the submitted score exceeds the stored score and when no doc exists; False when the submitted score is <= the stored score (three passing upsert tests driving the mocked doc.exists / doc.to_dict()).
    - The @firestore.transactional handling taken (decorator-works vs patched-to-passthrough) is recorded in the SUMMARY; if patched, the patch targets cloud_functions.submit_score.main.firestore.transactional.
    - cloud_functions/submit_score/main.py is unmodified (git diff clean).
  </acceptance_criteria>
  <done>The transactional spike is resolved and documented; all four validator 400s plus Invalid JSON and the is_new_best upsert decision are pinned against the working-tree submit_score with firebase mocked.</done>
</task>

<task type="auto">
  <name>Task 2: get_leaderboard success/empty tests via a mocked query.stream()</name>
  <read_first>
    - cloud_functions/get_leaderboard/main.py (the WHOLE file — query = db.collection().order_by().limit(10); docs = query.stream(); entry shape {"initials": data["initials"], "score": data["score"]} at 30-33; the 200 return {"entries": entries}; the 500 fallback {"entries": [], "error": ...})
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 8 D-16 note — add get_leaderboard tests: success returns {entries:[...]} from a mocked query.stream(); entry shape {initials,score})
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (tests/test_get_leaderboard.py — exact style match of test_api_service.py; the get_leaderboard entry shape excerpt)
    - tests/conftest.py (the leaderboard_module fixture from Plan 01 — imports get_leaderboard.main with firebase mocked, exposing _mock_client)
    - tests/test_submit_score.py (Task 1 — reuse the make_request helper or mirror it for a GET request)
  </read_first>
  <action>
    Write tests/test_get_leaderboard.py. Build a GET flask.Request (make_request from Task 1 with
    method="GET", or a local equivalent). Drive leaderboard_module._mock_client.return_value so the
    db.collection().order_by().limit().stream() chain returns an iterable of mock docs, each with
    .to_dict.return_value = {"initials": "JAM", "score": 8000} etc. Test: a success case with two docs
    returns ({"entries": [{"initials":"JAM","score":8000}, {"initials":"BOB","score":5000}]}, 200,
    headers) — assert the entries list order and shape; an empty case (stream() returns []) returns
    ({"entries": []}, 200, headers). Assert on the first tuple element and the status, house style.
    Do NOT modify cloud_functions/get_leaderboard/main.py (D-16).
  </action>
  <verify>
    <automated>python -m pytest tests/test_get_leaderboard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_get_leaderboard.py exits 0.
    - The success test asserts a 200 response whose "entries" is a list of {"initials","score"} dicts in the order returned by the mocked stream(), each carrying only the two fields the source projects.
    - The empty test asserts ({"entries": []}, 200, ...).
    - cloud_functions/get_leaderboard/main.py is unmodified (git diff clean).
    - Running the full cloud-fn suite (pytest tests/test_submit_score.py tests/test_get_leaderboard.py) exits 0 and never reaches real Firestore (no credential/network error).
  </acceptance_criteria>
  <done>get_leaderboard success and empty paths are pinned against the working-tree code with a mocked query.stream(); the full cloud-fn suite passes with firebase fully mocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → submit_score / get_leaderboard HTTP entrypoint | Untrusted JSON crosses here; the validators are the defense these tests freeze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-T1 | Tampering | malformed/oversized/typed-wrong submit_score payload (bad initials, non-int/over-range score, missing machine_id, non-JSON body) | mitigate | Tests assert the existing validators reject each with 400 (initials ^[A-Z]{3}$, isinstance(score,int) + 0<=score<=500000, required machine_id, get_json(silent=True) guard) — locking the control in place |
| T-06-T2 | Tampering | score overflow above MAX_SCORE | mitigate | Test asserts score 500001 -> 400 "Invalid score" (the MAX_SCORE=500000 upper bound) |
| T-06-ID | Information Disclosure | real Firebase credentials / firebase-key.json loaded during tests | accept | firebase-admin.initialize_app is patched out before import (conftest); tests never touch real Firestore; firebase-key.json stays gitignored and unused |
| T-06-COMP | Tampering | forgeable scores (client can still submit any valid-shaped score) | accept | Deeper anti-cheat / server-side validation is COMP-01, explicitly deferred to the More Competitive milestone; this phase only freezes the EXISTING validators (no new control promised) |

No new runtime attack surface ships in the .exe — these are dev/test-only files that exercise the
existing cloud functions with firebase mocked; no production code path changes.
</threat_model>

<verification>
- The @firestore.transactional spike is resolved and documented.
- Four validator 400s + Invalid JSON + is_new_best upsert (higher/lower-equal/no-doc) pinned for submit_score.
- get_leaderboard success/empty pinned via a mocked query.stream().
- The full cloud-fn suite passes with firebase fully mocked; no real Firestore access; main.py files unmodified.
</verification>

<success_criteria>
- TST-03: cloud-function validator tests cover initials regex ^[A-Z]{3}$, score type/range (incl. MAX_SCORE=500000), and best-score upsert; tests run against the working-tree main.py with firebase mocked (D-15, D-16); no emulator.
</success_criteria>

<output>
Create `.planning/phases/01-test-safety-net/01-06-SUMMARY.md` when done.
</output>
