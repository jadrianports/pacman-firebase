---
phase: 01-test-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-dev.txt
  - .gitignore
  - harness/__init__.py
  - harness/headless.py
  - tests/conftest.py
autonomous: false
requirements: [HRN-01, TST-03, TST-01]
user_setup: []

must_haves:
  truths:
    - "A pinned requirements-dev.txt exists with test/harness deps that match the deployed cloud-function pins (firebase-admin==6.*, functions-framework==3.*)"
    - "A human confirmed the five pinned dev packages/versions before any pip install ran"
    - "pygame initializes headless with no window and no audio device under SDL_VIDEODRIVER=dummy / SDL_AUDIODRIVER=dummy"
    - "conftest.py prepends repo-root to sys.path, sets SDL dummy env before any pygame import, registers a --bless flag, and exposes firebase-admin-mocked import fixtures"
    - "tests/artifacts/ is gitignored (regenerated PNG/montage/GIF never committed)"
  artifacts:
    - path: "requirements-dev.txt"
      provides: "Pinned dev/test/harness dependencies (flat, one-per-line)"
      contains: "firebase-admin==6"
    - path: "harness/headless.py"
      provides: "init_headless() sets SDL dummy env before import pygame and returns (pygame, screen, clock)"
      min_lines: 12
    - path: "harness/__init__.py"
      provides: "Marks harness/ as an importable package"
    - path: "tests/conftest.py"
      provides: "sys.path repo-root insert, SDL dummy env, --bless pytest_addoption, firebase-admin pre-import patch fixtures"
      contains: "pytest_addoption"
    - path: ".gitignore"
      provides: "tests/artifacts/ ignore rule"
      contains: "tests/artifacts"
  key_links:
    - from: "tests/conftest.py"
      to: "firebase_admin.initialize_app / firebase_admin.firestore.client"
      via: "unittest.mock.patch before importlib.import_module of the cloud-fn module"
      pattern: "patch\\(.firebase_admin"
    - from: "harness/headless.py"
      to: "os.environ['SDL_VIDEODRIVER']"
      via: "set before import pygame"
      pattern: "SDL_VIDEODRIVER"
---

<objective>
Lay the test-and-harness foundation the rest of Phase 1 builds on: a pinned `requirements-dev.txt`
(installed behind a human-verify checkpoint because slopcheck could not run), the headless pygame
bootstrap (`harness/headless.py` + `harness/__init__.py`), and the repo's FIRST config file
`tests/conftest.py` (repo-root `sys.path`, SDL dummy env, the `--bless` pytest flag, and the
firebase-admin pre-import patch fixtures). Also gitignore `tests/artifacts/`.

Purpose: Every downstream plan imports pygame headless, every cloud-fn test imports a firebase-mocked
module via conftest, and every golden-bless run reads the `--bless` flag — all of which depend on the
artifacts created here.
Output: `requirements-dev.txt`, `.gitignore` (modified), `harness/__init__.py`, `harness/headless.py`,
`tests/conftest.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-safety-net/01-RESEARCH.md
@.planning/phases/01-test-safety-net/01-PATTERNS.md
@.planning/phases/01-test-safety-net/01-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols/files (exclude from any source-drift / "missing symbol" check —
they do not exist in the codebase before this plan runs):
- `requirements-dev.txt` (NEW file)
- `harness/` package: `harness/__init__.py`, `harness/headless.py` (NEW)
- `harness.headless.init_headless()` (NEW function)
- `tests/conftest.py` (NEW — the repo's first config file)
- conftest `--bless` CLI flag via `pytest_addoption` (NEW)
- conftest fixtures `submit_module` / `leaderboard_module` (NEW — firebase-mocked importers)
- `.gitignore` entry `tests/artifacts/` (NEW rule)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify pinned dev dependencies before install (slopcheck-degraded gate)</name>
  <read_first>
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Package Legitimacy Audit — slopcheck could not run; all five packages tagged [ASSUMED]; Standard Stack installation block)
    - cloud_functions/submit_score/requirements.txt and cloud_functions/get_leaderboard/requirements.txt (the deployed pins the test deps MUST match: functions-framework==3.*, firebase-admin==6.*)
  </read_first>
  <action>
    HALT and present the five pinned packages to the human for legitimacy verification BEFORE any pip
    install runs. Do NOT write requirements-dev.txt and do NOT install until the human replies "approved".
    This gate exists because slopcheck could not run (all five deps tagged [ASSUMED] in RESEARCH); it is
    never auto-approved regardless of workflow.auto_advance.
  </action>
  <what-built>
    The planned contents of requirements-dev.txt (Task 2 writes it after this gate). The five pinned
    packages are: pytest (8.4.x), pygame (2.6.1), Pillow (11.0.x), firebase-admin (6.x), and
    functions-framework (3.x). Four of five already appear in the repo's manifests; Pillow is the only
    genuinely new package (canonical PIL fork, ~100M+ weekly downloads).
  </what-built>
  <how-to-verify>
    1. Confirm each package name + pinned major is legitimate by visiting:
       - https://pypi.org/project/pytest/
       - https://pypi.org/project/pygame/
       - https://pypi.org/project/Pillow/
       - https://pypi.org/project/firebase-admin/  (confirm a 6.x release exists)
       - https://pypi.org/project/functions-framework/  (confirm a 3.x release exists)
    2. Confirm firebase-admin is pinned to the 6.x major and functions-framework to the 3.x major
       (NOT firebase-admin 7.x / functions-framework 3.10.x) so the test env matches what the cloud
       functions actually deploy with.
    3. Reply "approved" to allow Task 2 to write the file and run pip install, or name any package/
       version to change.
  </how-to-verify>
  <verify>
    <human-check>Human confirms each package on pypi.org and that firebase-admin/functions-framework are pinned to the deployed 6.*/3.* majors, then replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - This task halts execution until the human replies (gate="blocking-human"; never auto-approved).
    - The five package names and their pinned majors are stated to the human exactly as above.
    - The firebase-admin==6.* / functions-framework==3.* deploy-pin constraint is called out explicitly.
  </acceptance_criteria>
  <done>The human has confirmed all five pinned dev packages are legitimate and correctly pinned (firebase-admin major 6, functions-framework major 3); Task 2 may now write and install.</done>
  <resume-signal>Type "approved" or name the package/version to change.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write pinned requirements-dev.txt, gitignore artifacts, then install</name>
  <files>requirements-dev.txt, .gitignore</files>
  <read_first>
    - requirements.txt (the existing flat, unpinned shape — mirror this layout but pinned)
    - .gitignore (current contents — append tests/artifacts/, do not remove existing rules)
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Standard Stack installation block; Runtime State Inventory — add tests/artifacts/ to .gitignore)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (requirements-dev.txt — flat-but-pinned)
  </read_first>
  <action>
    Create requirements-dev.txt as a flat, one-package-per-line file (mirroring requirements.txt
    shape) with these pinned entries: pytest==8.4.2, pygame==2.6.1, Pillow==11.0.0, firebase-admin==6.5.0,
    functions-framework==3.8.1. The exact patch versions are chosen here; the HARD constraint is
    firebase-admin major == 6 and functions-framework major == 3 (must match the deployed
    cloud_functions/*/requirements.txt pins — do NOT use firebase-admin 7.x or functions-framework
    3.10.x). If pip reports any pinned patch version is yanked/unavailable, substitute the nearest
    available patch WITHIN the same major and note the substitution in the SUMMARY.
    Append a tests/artifacts/ rule to .gitignore (this directory holds regenerated PNG/montage/GIF
    per D-06 and must never be committed). Do not touch other .gitignore rules.
    Then install: pip install -r requirements.txt followed by pip install -r requirements-dev.txt.
    Do NOT pin or modify the client requirements.txt — pinning client deps is Phase 3 (HYG-01).
  </action>
  <verify>
    <automated>python tests/scratch_deps_check.py</automated>
  </verify>
  <acceptance_criteria>
    - requirements-dev.txt has a line matching firebase-admin==6. (firebase-admin pinned to major 6).
    - requirements-dev.txt has a line matching functions-framework==3. (functions-framework pinned to major 3).
    - requirements-dev.txt has pinned lines for pytest, pygame, and Pillow (three ==-pinned entries).
    - .gitignore contains a tests/artifacts rule.
    - All five packages import and firebase_admin major is 6 (the verify script exits 0).
    - requirements.txt (client) is unchanged (git diff shows no modification to requirements.txt).
  </acceptance_criteria>
  <done>requirements-dev.txt exists with the five pinned deps (firebase-admin major 6, functions-framework major 3); tests/artifacts/ is gitignored; all five packages import; client requirements.txt untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create harness package, headless bootstrap, and the repo's first conftest.py</name>
  <files>harness/__init__.py, harness/headless.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 2 Headless execution — exact init_headless recipe; Pattern 8 — conftest pre-import firebase patch + sys.path; Don't Hand-Roll — pytest_addoption for --bless; Pitfall 3 — SDL env must be set before import pygame)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (tests/conftest.py — house mock-the-I/O fixture style from test_api_service.py:7-18 to extend)
    - tests/test_api_service.py (the house fixture + MagicMock style to mirror)
    - cloud_functions/submit_score/main.py (lines 1-9 — the import-time if not firebase_admin._apps: initialize_app() + db = firestore.client() that the conftest patch must neutralize BEFORE import)
    - game.py (lines 22, 95-98 — Game.__init__ builds Fonts; headless init must call pygame.font.init())
  </read_first>
  <action>
    Create harness/__init__.py (empty package marker). Create harness/headless.py with an
    init_headless(size=(900, 950)) function that, IN THIS ORDER: sets os.environ["SDL_VIDEODRIVER"]="dummy"
    and os.environ["SDL_AUDIODRIVER"]="dummy" BEFORE import pygame; then imports pygame; calls
    pygame.display.init(), pygame.font.init(), pygame.mixer.init(); creates screen via
    pygame.display.set_mode(size); creates clock via pygame.time.Clock(); returns the tuple
    (pygame, screen, clock). Source detail is in RESEARCH Pattern 2 — do not deviate from the
    set-env-before-import ordering.
    Create tests/conftest.py (the repo's first config file) that: (a) inserts the repo root at
    sys.path[0] via os.path.dirname(os.path.dirname(__file__)); (b) sets the two SDL_*=dummy env vars
    at module top before any test imports pygame; (c) registers a --bless flag via pytest_addoption
    (store_true; Plan 04 reads it via request.config.getoption("--bless")); (d) provides two fixtures —
    submit_module and leaderboard_module — that, under patch("firebase_admin.initialize_app"),
    patch("firebase_admin._apps", new=[]), and patch("firebase_admin.firestore.client"), pop any
    cached cloud-fn module from sys.modules and importlib.import_module the WORKING-TREE module
    (cloud_functions.submit_score.main / cloud_functions.get_leaderboard.main), exposing the mock
    client on the module as _mock_client for the tests to drive. Mirror the MagicMock house style
    from test_api_service.py. Full fixture shape is in RESEARCH Pattern 8 — replicate the patch targets
    exactly. Do NOT add a [tool.pytest.ini_options] section anywhere; keep repo-root import-by-name.
    Add cloud_functions/__init__.py and cloud_functions/submit_score/__init__.py /
    cloud_functions/get_leaderboard/__init__.py ONLY IF importlib.import_module of the dotted path fails
    without them (verify first; prefer the minimal change).
  </action>
  <verify>
    <automated>python tests/scratch_conftest_headless_check.py</automated>
  </verify>
  <acceptance_criteria>
    - harness/headless.py sets os.environ['SDL_VIDEODRIVER']='dummy' on a line BEFORE the line import pygame (the assignment line number is less than the import pygame line number).
    - init_headless() returns a screen 900px wide with no DISPLAY (runs under dummy driver, no "No available video device" error).
    - pytest --bless --collect-only does NOT emit "unrecognized arguments: --bless" (flag is registered).
    - tests/conftest.py contains pytest_addoption and patch("firebase_admin.firestore.client" (grep matches).
    - Importing the conftest does NOT execute the real cloud-fn initialize_app() (no firebase credential error during collection).
  </acceptance_criteria>
  <done>harness package + init_headless() work headless; conftest.py registers --bless and provides firebase-mocked submit_module/leaderboard_module fixtures without running real firebase init; pytest collection succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev machine → PyPI | Pinned dev/test deps are downloaded and installed locally and in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip install of requirements-dev.txt (5 deps, slopcheck unavailable) | mitigate | Task 1 is a blocking-human checkpoint verifying each package on pypi.org before install; all deps pinned to exact/major versions; firebase-admin/functions-framework pinned to the deployed 6.*/3.* majors |
| T-01-ID | Information Disclosure | conftest firebase mock | accept | No real Firebase credentials are loaded (initialize_app is patched out before import); firebase-key.json stays gitignored and untouched; tests never reach real Firestore |

No new runtime attack surface ships in the .exe — requirements-dev.txt, harness/, and conftest.py are
dev/test-only artifacts excluded from the PyInstaller build.
</threat_model>

<verification>
- pip install -r requirements-dev.txt succeeds after the human-verify gate.
- All five dev packages import; firebase_admin major is 6.
- pytest --collect-only succeeds (conftest does not trigger real firebase init).
- pytest --bless --collect-only does not error on the unknown flag.
- harness.headless.init_headless() returns a usable 900-wide screen with no display device.
</verification>

<success_criteria>
- requirements-dev.txt pinned (firebase-admin major 6, functions-framework major 3) and installed behind a human-verify gate.
- harness/headless.py initializes pygame headless (SDL env set before import pygame).
- tests/conftest.py provides repo-root sys.path, SDL dummy env, --bless flag, and firebase-mocked import fixtures.
- tests/artifacts/ gitignored; client requirements.txt untouched.
</success_criteria>

<output>
Create `.planning/phases/01-test-safety-net/01-01-SUMMARY.md` when done.
</output>
