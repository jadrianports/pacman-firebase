---
phase: 01-test-safety-net
plan: 03
type: execute
wave: 3
depends_on: ['01-02']
files_modified:
  - harness/capture.py
  - harness/play_loop.py
autonomous: true
requirements: [HRN-03, HRN-04]
user_setup: []

must_haves:
  truths:
    - "Rendered frames are saved to PNG via pygame.image.save"
    - "A montage (grid of scaled stills) is assembled by blitting thumbnails onto one surface using only pygame (zero new dep)"
    - "A GIF is assembled from PNG frames using Pillow only"
    - "All PNG/montage/GIF output writes under tests/artifacts/ (gitignored), never under tests/golden/"
    - "A play_turn driver implements observe (PNG+state) -> decide (a pluggable decide_fn returns a direction) -> act (event.post) -> step (tick), and serializes injected {frame,key} events to the same sparse JSONL format the replay driver consumes"
  artifacts:
    - path: "harness/capture.py"
      provides: "save_png, build_montage (blit), build_gif (Pillow)"
      min_lines: 20
    - path: "harness/play_loop.py"
      provides: "play_turn observe->decide->act driver + a serialize-inputs helper that emits sparse {frame,key} JSONL"
      min_lines: 20
  key_links:
    - from: "harness/capture.py"
      to: "pygame.image.save"
      via: "save_png writes the screen surface to a PNG path"
      pattern: "image\\.save"
    - from: "harness/capture.py"
      to: "PIL.Image.save"
      via: "build_gif assembles frames with save_all/append_images"
      pattern: "save_all"
    - from: "harness/play_loop.py"
      to: "pygame.event.post"
      via: "act phase injects the chosen direction via the real input path"
      pattern: "event\\.post"
---

<objective>
Add the visual-proof and Claude-drive capabilities: `harness/capture.py` (PNG per frame via
`pygame.image.save`, blit-based montages with ZERO new dep, GIF via Pillow ONLY — D-20) and
`harness/play_loop.py` (the observe -> decide -> act driver, HRN-04). The play-loop serializes its
injected inputs to the SAME sparse `{frame,key}` JSONL format the replay driver consumes, so a
Claude-played session becomes a deterministic, CI-replayable golden in Plan 04 (the D-10 capture/hunt split).

Purpose: HRN-03 gives Claude its own eyes (montages) and the human a GIF; HRN-04 lets Claude drive the
game to generate the long realistic golden session and to run the manual adversarial bug-hunt at phase
verification.
Output: `harness/capture.py`, `harness/play_loop.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-safety-net/01-RESEARCH.md
@.planning/phases/01-test-safety-net/01-PATTERNS.md
@.planning/phases/01-test-safety-net/01-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (exclude from source-drift checks):
- `harness/capture.py` (NEW): `save_png(pygame, screen, path)`, `build_montage(pygame, frame_surfaces, cols, cell, pad)`, `build_gif(png_paths, out_path, duration_ms, loop)`
- `harness/play_loop.py` (NEW): `play_turn(game, pygame, capture_state, decide_fn, png_path=None)`, `serialize_inputs(events, path)` (emits sparse {frame,key} JSONL)
Pre-existing symbols this plan READS: `harness.trace.capture_state`, `harness.replay` KEYMAP, `pygame.image.save`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Frame capture — PNG, blit montage, Pillow GIF</name>
  <read_first>
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 5 — save_png, build_montage via Surface.blit of scaled thumbnails, build_gif via PIL.Image.save(save_all, append_images, duration, loop); D-06 directory policy writes to tests/artifacts/ gitignored; D-20 Pillow only for GIF)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (harness/capture.py — no in-repo analog, use RESEARCH Pattern 5 verbatim)
    - harness/headless.py (init_headless — provides the pygame + screen used to render and save)
  </read_first>
  <action>
    Write harness/capture.py per RESEARCH Pattern 5. Implement: save_png(pygame, screen, path) calling
    pygame.image.save(screen, path) (PNG inferred from the .png extension), creating parent dirs under
    tests/artifacts/ if needed; build_montage(pygame, frame_surfaces, cols, cell=(180,190), pad=4)
    building one pygame.Surface sized to fit a cols-wide grid, filling black, blitting each
    pygame.transform.scale(surf, cell) thumbnail at its grid cell, and returning the sheet Surface
    (caller saves it with save_png) — pure pygame, ZERO new dependency; build_gif(png_paths, out_path,
    duration_ms=33, loop=0) importing PIL.Image INSIDE the function (Pillow is the ONLY new dep and is
    used ONLY here per D-20), opening each PNG and calling frames[0].save(out_path, save_all=True,
    append_images=frames[1:], duration=duration_ms, loop=loop). All outputs MUST be written under
    tests/artifacts/ (gitignored, D-06) — never under tests/golden/. Do NOT hash PNG pixels anywhere
    (pixels are for eyes only; the numeric trace is the stable golden — anti-pattern in RESEARCH).
  </action>
  <verify>
    <automated>python tests/scratch_capture_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - save_png writes a real .png file readable back by pygame.image.load (non-zero size).
    - build_montage returns a pygame.Surface whose width >= cols*cell_width (grid fits the requested columns).
    - build_gif writes a .gif whose first bytes are the GIF signature (file opens via PIL.Image.open and reports format == 'GIF').
    - PIL is imported only inside build_gif (grep: 'from PIL' / 'import PIL' appears only within build_gif's body, not at module top).
    - All smoke outputs land under tests/artifacts/ (no file written under tests/golden/).
  </acceptance_criteria>
  <done>capture.py saves PNGs, builds blit montages with zero new dep, and assembles a GIF via Pillow only; everything writes to gitignored tests/artifacts/.</done>
</task>

<task type="auto">
  <name>Task 2: Claude play-loop driver (observe -> decide -> act) with input serialization</name>
  <read_first>
    - .planning/phases/01-test-safety-net/01-RESEARCH.md (Pattern 6 play-loop — observe (save PNG + capture_state) -> decide (decide_fn returns RIGHT/LEFT/UP/DOWN or None) -> act (event.post) -> tick; D-10 split: serialize injected {frame,key} to the SAME sparse JSONL the replay driver reads so the session replays deterministically in CI; the live adversarial hunt stays a MANUAL gate, not a committed file)
    - .planning/phases/01-test-safety-net/01-PATTERNS.md (harness/play_loop.py — sibling of replay.py; role-match)
    - harness/replay.py (KEYMAP + the run_scenario injection pattern to mirror; load_events is the inverse of serialize_inputs)
    - harness/trace.py (capture_state — observe step)
    - harness/capture.py (save_png — observe step writes the montage frame)
  </read_first>
  <action>
    Write harness/play_loop.py per RESEARCH Pattern 6. Implement play_turn(game, pygame, capture_state,
    decide_fn, png_path=None): if png_path is given, save_png the current game.screen (OBSERVE); read
    state = capture_state(game); call key = decide_fn(state, png_path) where decide_fn is a pluggable
    callback returning a logical direction (RIGHT/LEFT/UP/DOWN/SPACE) or None (DECIDE — this is where
    Claude is in the loop); if key, pygame.event.post(pygame.event.Event(pygame.KEYDOWN,
    key=getattr(pygame, KEYMAP[key]))) (ACT, reusing replay.KEYMAP); then game.tick() to advance; return
    capture_state(game). Also implement serialize_inputs(events, path) that writes the list of injected
    (frame, key, type) events as sparse JSONL in the EXACT shape harness.replay.load_events parses, so a
    recorded session round-trips through run_scenario byte-for-byte. Reuse replay.KEYMAP — do not define
    a second mapping. The driver must inject ONLY via event.post (D-18). Do NOT make decide_fn depend on
    randomness or wall-clock (keeps recorded sessions deterministic).
  </action>
  <verify>
    <automated>python tests/scratch_playloop_roundtrip.py</automated>
  </verify>
  <acceptance_criteria>
    - play_turn advances the game by one tick and returns a capture_state snapshot dict.
    - play_turn injects the decided key via pygame.event.post (grep shows event.post; no direct player.direction_command mutation).
    - serialize_inputs writes JSONL that harness.replay.load_events parses back into the same frame->events mapping (round-trip equal).
    - A short scripted play session, serialized via serialize_inputs then replayed via harness.replay.run_scenario, produces a trace equal to the play session's own per-frame snapshots (deterministic replay — the D-10 recorded-golden property).
    - play_loop.py imports KEYMAP from harness.replay (no duplicate KEYMAP defined).
  </acceptance_criteria>
  <done>play_loop.py drives observe->decide->act, injects via event.post, and serializes inputs to the sparse JSONL the replay driver reads — so a Claude session becomes a deterministic CI-replayable golden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → filesystem (tests/artifacts/) | PNG/montage/GIF written to a gitignored scratch dir; no untrusted input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-I | Information Disclosure | regenerated PNG/montage/GIF artifacts accidentally committed | mitigate | All capture output writes under tests/artifacts/, which Plan 01 added to .gitignore; only JSONL goldens are committed (D-06) |
| T-03-R | Repudiation | a recorded Claude session is non-deterministic and cannot be replayed/audited | mitigate | decide_fn is forbidden from using randomness/wall-clock; injected inputs are serialized to the same sparse JSONL the replay driver replays byte-for-byte (D-10) |

No new runtime attack surface ships in the .exe — capture.py and play_loop.py are dev/test-only;
Pillow is a dev-only dependency excluded from the PyInstaller build.
</threat_model>

<verification>
- save_png/build_montage/build_gif produce valid files under tests/artifacts/ only.
- Pillow is used ONLY inside build_gif.
- play_turn injects via event.post and advances one tick.
- serialize_inputs output round-trips through replay.load_events; a serialized session replays deterministically.
</verification>

<success_criteria>
- HRN-03: frames captured to PNG, assembled into montages (zero new dep) and a GIF (Pillow only); outputs gitignored.
- HRN-04: Claude can drive the game observe->decide->act; injected inputs serialize to the deterministic, CI-replayable sparse JSONL format (D-10 capture half).
</success_criteria>

<output>
Create `.planning/phases/01-test-safety-net/01-03-SUMMARY.md` when done.
</output>
