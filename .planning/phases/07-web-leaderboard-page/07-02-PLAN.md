---
phase: 07-web-leaderboard-page
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/public/app.js
  - web/tests/app.test.mjs
autonomous: true
requirements: [WEB-01, WEB-03]
must_haves:
  truths:
    - "On load the page fetches and shows the All Time board (default view, D-08)."
    - "Tapping This Week lazily fetches the weekly board the first time, then caches it; the last-week champion subtitle appears when present and hides when absent."
    - "Tapping All Time shows the cached all-time board (no refetch)."
    - "On a failed fetch the active view shows 'Could not connect to leaderboard.'; the other cached view still renders."
    - "An empty board shows the view-correct 'Be the first!' line; rank 1 is visually distinct from ranks 2-10."
    - "The Refresh control re-pulls the currently active view and re-renders it."
  artifacts:
    - path: "web/public/app.js"
      provides: "ES module: LEADERBOARD_URL constant + fetch/cache/render/toggle/refresh logic against the Plan 01 DOM contract"
      min_lines: 70
      exports: ["buildLeaderboardUrl", "lastWeekInitials", "boardMarkup"]
    - path: "web/tests/app.test.mjs"
      provides: "node:test coverage of the pure data/render functions with a stubbed fetch"
  key_links:
    - from: "web/public/app.js"
      to: "https://get-leaderboard-991339031546.asia-southeast1.run.app"
      via: "fetch with ?scope=week|all|last_week"
      pattern: "get-leaderboard-991339031546"
    - from: "web/public/app.js"
      to: "web/public/index.html"
      via: "querySelector on #board / #tab-all / #tab-week / #subtitle / #refresh"
      pattern: "getElementById|querySelector"
---

<objective>
Implement `web/public/app.js` — the page's entire behavioral core as a no-dependency ES module that consumes the live `get_leaderboard` Cloud Function and renders the in-game This Week / All Time boards into the Plan 01 DOM contract. Mirrors `menu.py:run_leaderboard` logic and `api_service.py:get_leaderboard` fetch semantics exactly, translated to browser `fetch` + DOM.

Behavior locked by decisions: default view = All Time (D-08, deliberate divergence from the in-game This Week default — do NOT "fix"); per-view lazy fetch + cache (D-11, mirror in-game D-14); explicit Refresh re-pulls the active view (D-10); last-week champion subtitle via `scope=last_week`, This Week only, hidden when absent (D-09); verbatim loading/offline/empty copy (D-12); rank-1 distinct from 2-10. No self-row highlight — `machine_id` is stripped server-side (carry-over of Phase 6 D-19). No server/API change.

Purpose: Deliver WEB-01 (fetch from the existing API) and WEB-03 (both views mirrored) as testable browser logic.
Output: `web/public/app.js`, `web/tests/app.test.mjs`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-leaderboard-page/07-CONTEXT.md
@.planning/phases/07-web-leaderboard-page/07-PATTERNS.md
@web/public/index.html
</context>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols/files — exclude from drift verification)

This plan creates:
- `web/public/app.js` — ES module. New symbols: `LEADERBOARD_URL` (const, mirror of `settings.API_LEADERBOARD_URL`), `buildLeaderboardUrl(scope)`, `fetchEntries(scope)` (returns entries array or `null`), `lastWeekInitials(entries)`, `boardMarkup(entries, view)` (returns rank-row / state-message HTML for `#board`), `formatScore(n)`, a `views` cache object `{ all, week }` with an `UNFETCHED` sentinel, `activeView` state (defaults `"all"`), `loadView(scope)`, `renderActive()`, `onTabClick`, `onRefresh`, `init()` (DOM bootstrap, guarded for non-browser import).
- `web/tests/app.test.mjs` — node:test file importing the pure functions.

Consumes (already created, do NOT recreate): the Plan 01 DOM hooks `#tabbar`, `#tab-all`, `#tab-week`, `#subtitle`, `#board`, `#refresh`, classes `tab--active`, `rank-row`, `rank-row--first`, `state-msg`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data layer — URL build, fetch→entries|null, last-week champion, lazy cache</name>
  <files>web/public/app.js, web/tests/app.test.mjs</files>
  <read_first>
    - api_service.py lines 30-40 (get_leaderboard: urlencode scope, urlopen, `data.get("entries")`, broad `except → None` — the null=offline sentinel to mirror)
    - cloud_functions/get_leaderboard/main.py lines 24-94 (response shape `{ "entries": [ { "initials", "score" } ] }`, top-10, machine_id stripped, scope tolerant parse, 500 returns `{entries:[], error}` — treat any non-200 as offline)
    - menu.py lines 167-177 (per-view cache + sentinel truth table; last-week initials = `last_week[0]["initials"] if last_week else None`)
    - settings.py line 82 (API_LEADERBOARD_URL — the source-of-truth URL to mirror as a JS constant, D-03)
    - .planning/phases/07-web-leaderboard-page/07-PATTERNS.md (§ app.js Analog A + Analog B; § Shared Patterns data source/contract)
  </read_first>
  <behavior>
    - buildLeaderboardUrl("all") returns `https://get-leaderboard-991339031546.asia-southeast1.run.app?scope=all`; same for `week` and `last_week`; encodes the scope param.
    - fetchEntries(scope): when the stubbed fetch resolves `{ ok:true, json:()=>({entries:[...]}) }`, returns that entries array; when it resolves `{ ok:false }` (non-200) OR rejects/throws, returns `null` (the offline sentinel — never throws). When the body has no `entries` key, returns `null`.
    - lastWeekInitials([{initials:"BOB",score:9}]) returns "BOB"; lastWeekInitials([]) returns null; lastWeekInitials(null) returns null.
    - The `views` cache starts `{ all: UNFETCHED, week: UNFETCHED }` and `activeView` starts `"all"` (D-08).
  </behavior>
  <action>
    Create web/public/app.js as an ES module (the Plan 01 `web/package.json` `type:module` marker makes node import it as ESM; the browser loads it via `<script type="module">`).

    Define `export const LEADERBOARD_URL = "https://get-leaderboard-991339031546.asia-southeast1.run.app";` — a hardcoded mirror of settings.API_LEADERBOARD_URL (D-03; intentionally public, non-secret — there is no HMAC/secret on the read path).

    `export function buildLeaderboardUrl(scope)` — return `LEADERBOARD_URL` plus `?scope=<scope>` using `encodeURIComponent` / `URLSearchParams` (mirror api_service's `urlencode({'scope': scope})`). Valid scopes: `week`, `all`, `last_week`.

    `export async function fetchEntries(scope)` — `try { const r = await fetch(buildLeaderboardUrl(scope)); if (!r.ok) return null; const data = await r.json(); return Array.isArray(data?.entries) ? data.entries : null; } catch { return null; }`. This is the exact mirror of api_service.get_leaderboard's broad-except-→-None contract. Preserve `null` = offline as the single sentinel the render layer keys off (Task 2). Make `fetch` injectable for tests (read the global `fetch`, which node:test can stub via `globalThis.fetch`).

    `export function lastWeekInitials(entries)` — return `entries?.[0]?.initials ?? null` (mirror menu.py:176-177; subtitle source, This Week only).

    Define the cache + state primitives (exported so Task 2 + tests use them): a unique `UNFETCHED` sentinel (e.g. `Symbol("unfetched")` or a module const object), `const views = { all: UNFETCHED, week: UNFETCHED }`, `let activeView = "all"` (D-08), and a separate `let lastWeek = UNFETCHED` slot for the champion (independent of the toggle cache, mirror menu.py).

    `export async function loadView(scope)` — if `views[scope] === UNFETCHED`, fetch it via fetchEntries and store the result (entries array, `[]`, or `null`) in `views[scope]`; return the stored value. (Lazy per-view cache, D-11.) When `scope === "week"` is loaded for the first time, also fetch `lastWeek = await fetchEntries("last_week")` once (so This Week's subtitle is ready).

    Create web/tests/app.test.mjs (node:test + node:assert/strict) importing the above. Stub `globalThis.fetch` per test to return a resolved `{ ok, json }` or to reject; assert buildLeaderboardUrl scope encoding, fetchEntries success/non-ok/throw/missing-entries → array|null, lastWeekInitials cases, and that loadView caches (second call with a fetch that would throw still returns the cached value).
  </action>
  <verify>
    <automated>node --test web/tests/app.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/app.test.mjs` exits 0.
    - app.js contains the literal host `get-leaderboard-991339031546` in the LEADERBOARD_URL constant (mirror of settings.API_LEADERBOARD_URL).
    - fetchEntries returns `null` (never throws) on non-ok response, network throw, and missing `entries` key; returns the entries array on a 200 with `{entries:[...]}`.
    - lastWeekInitials returns `entries[0].initials` for a populated list and `null` for `[]`/`null`.
    - `activeView` initial value is `"all"` (D-08) and `views` starts both slots UNFETCHED.
    - loadView caches per scope (no second fetch for an already-loaded scope).
  </acceptance_criteria>
  <done>app.js data layer (URL/fetch/cache/last-week) implemented and green under node --test; null=offline sentinel preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render + interaction — state branches, rank rows, tab toggle, refresh, subtitle, DOM bootstrap</name>
  <files>web/public/app.js, web/tests/app.test.mjs</files>
  <read_first>
    - menu.py lines 184-263 (three-way render: `None`→offline line, `len==0`→empty line, else rank rows; rank-1 yellow vs 2-10 white at line 239; tab toggle + lazy-fetch at 256-261; subtitle show/hide at 214-220; rank row format at 232-238)
    - .planning/phases/07-web-leaderboard-page/07-UI-SPEC.md (Copywriting Contract — exact state strings; Rank row format `{rank}. {INITIALS}  {dot-leaders}  {score}`, rank-1 yellow; thousands separators are discretion)
    - .planning/phases/07-web-leaderboard-page/07-PATTERNS.md (§ Verbatim copy strings table; § app.js Refresh control D-10)
    - web/public/index.html (the DOM hooks: #board, #tab-all, #tab-week, #subtitle, #refresh, classes tab--active / rank-row / rank-row--first / state-msg)
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-09 subtitle hide-when-none, D-10 refresh, D-12 wording + independent degrade, D-19 no self-row highlight)
  </read_first>
  <behavior>
    - boardMarkup(null, "all") and boardMarkup(null, "week") both render a `state-msg` element with exactly `Could not connect to leaderboard.`
    - boardMarkup([], "week") renders `state-msg` `No scores yet this week. Be the first!`; boardMarkup([], "all") renders `No scores yet. Be the first!`.
    - boardMarkup([{initials:"BOB",score:12345},{initials:"AMY",score:9000}], "all") renders two `rank-row` items; the first carries `rank-row--first`; each row contains the rank number, the initials, and the formatted score (e.g. `12,345` if thousands separators chosen).
    - Initials and score are inserted via textContent / escaped — a hostile `initials` value like `<img>` must NOT produce a live element (XSS defense-in-depth; server already enforces `^[A-Z]{3}$`).
  </behavior>
  <action>
    Extend web/public/app.js with rendering + interaction, all targeting the Plan 01 DOM contract.

    `export function boardMarkup(entries, view)` — the three-way branch mirroring menu.py:222-242. Return DOM nodes (preferred) or an HTML string built with ESCAPED values:
    - `entries === null` → one `<li class="state-msg">` (or `<p>` inside #board) with exactly `Could not connect to leaderboard.` (D-12, verbatim).
    - `entries.length === 0` → one `state-msg` with the view-specific empty text: `week` → `No scores yet this week. Be the first!`, `all` → `No scores yet. Be the first!` (D-12, verbatim).
    - else → one `<li class="rank-row">` per entry; rank-1 gets the extra class `rank-row--first` (the positional yellow highlight — rank-1 vs 2-10, mirror menu.py:239; NOT a self-row highlight — machine_id is stripped, D-19). Each row holds rank `${i+1}.`, the initials, a dot-leader span (`.dots`, rendered via CSS in Plan 03 — emit the span, do not pad with literal `.`), and the score. SECURITY: build rows with `textContent` / `document.createElement` / `createTextNode` (or escape via a helper) for `initials` and `score` — never `innerHTML` of raw API data (T-07-01 XSS mitigation).

    `export function formatScore(n)` — Claude's discretion (D): apply `Number(n).toLocaleString("en-US")` thousands separators; tolerate string scores.

    `renderActive()` — read `views[activeView]`, set `#board` content to `boardMarkup(views[activeView], activeView)`; toggle `tab--active` between `#tab-all`/`#tab-week`; and the subtitle: show `#subtitle` (remove `hidden`) with `Last week: ${initials}` ONLY when `activeView === "week"` AND `lastWeekInitials(lastWeek)` is non-null, else set `hidden` (D-09).

    `onTabClick(scope)` — set `activeView = scope`; if `views[scope] === UNFETCHED` paint a `state-msg` `Loading...` into #board then `await loadView(scope)`; then `renderActive()`. (Lazy toggle, D-11.)

    `onRefresh()` — force-refetch the ACTIVE view only: set `views[activeView] = UNFETCHED` (and if active is week, also reset `lastWeek = UNFETCHED`), paint `Loading...`, `await loadView(activeView)`, `renderActive()`. Overwrites just the active cache slot, leaving the other view's cache intact (D-10, independent degrade).

    `init()` — DOM bootstrap, GUARDED so a non-browser import is inert: `if (typeof document === "undefined") { /* test/import context */ }` else wire `#tab-all`/`#tab-week` click→`onTabClick(data-scope)`, `#refresh` click→`onRefresh`, then initial load: `await loadView("all")` and `renderActive()` (default All Time on open, D-08). Call `init()` at module end (it self-guards).

    Extend web/tests/app.test.mjs: assert boardMarkup's three branches produce the exact verbatim strings and the `state-msg` class; assert a populated list yields N `rank-row` entries with `rank-row--first` only on index 0; assert a hostile `initials` of `<img src=x>` does NOT appear as raw HTML (escaped/textContent — search the produced markup/string for the literal `<img` and assert it is encoded, not live).
  </action>
  <verify>
    <automated>node --test web/tests/app.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/app.test.mjs` exits 0 with the render + XSS assertions passing.
    - The three state strings are byte-for-byte verbatim: `Could not connect to leaderboard.`, `No scores yet this week. Be the first!`, `No scores yet. Be the first!`, plus `Loading...` (D-12).
    - rank-1 row carries `rank-row--first`; rows 2-N do not.
    - A `<img src=x>` initials value is rendered escaped/inert (no live element) — proves the T-07-01 XSS mitigation.
    - `init()` is guarded: importing app.js in node (no `document`) does not throw and does not call fetch at import time.
    - app.js wires `#refresh` to re-pull only `activeView`; the inactive view's cache is untouched.
  </acceptance_criteria>
  <done>app.js renders all three states + rank rows + subtitle, toggles + refreshes against the DOM contract, escapes API data; node --test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → get_leaderboard (Cloud Run) | Page fetches public JSON over HTTPS; CORS `*` already set server-side; no auth, no secret on the read path |
| API response → DOM | Server-returned `initials`/`score` are written into `#board` (treat as untrusted content even though the server validates initials) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering (XSS) | boardMarkup rendering entries into #board | mitigate | Render `initials`/`score` via textContent / createTextNode (or HTML-escape); never `innerHTML` of raw API data. Server already enforces `^[A-Z]{3}$` on initials (defense in depth) |
| T-07-02 | Info disclosure | LEADERBOARD_URL constant in app.js | accept | Intentionally public, non-secret mirror of settings.API_LEADERBOARD_URL; the read endpoint requires no key/signature |
| T-07-03 | Denial of service | Refresh re-pulling the public API | accept | No auto-poll (D-10) — Refresh is user-initiated only; function `max-instances=5` (Phase 4) caps blast radius |
| T-07-04 | Spoofing / Repudiation / Elevation | read-only page | accept | No auth, no writes, no privileged action surface |
</threat_model>

<verification>
- `node --test web/tests/app.test.mjs` passes (data layer + render branches + XSS escaping + cache/toggle/refresh logic).
- Live behavior (fetch against the deployed function, both views, refresh on a phone) is confirmed in Plan 04's human-verify checkpoint.
</verification>

<success_criteria>
- The page opens on All Time, lazily loads This Week (+ last-week subtitle) on first toggle, caches both, and Refresh re-pulls the active view — all mirroring the in-game board.
- All three render states use verbatim in-game copy; rank 1 is positionally distinct; API data is escaped.
- app.js is a single no-dependency ES module with no build step (D-01).
</success_criteria>

<output>
Create `.planning/phases/07-web-leaderboard-page/07-02-SUMMARY.md` when done.
</output>
