---
phase: 07-web-leaderboard-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/public/index.html
  - firebase.json
  - .firebaserc
  - web/package.json
  - web/tests/scaffold.test.mjs
autonomous: true
requirements: [WEB-01, WEB-03]
must_haves:
  truths:
    - "The page has a PAC-MAN wordmark, a This Week | All Time tab pair, a last-week subtitle slot, a board container, and a Refresh control (mirrors the in-game board order)."
    - "The browser tab title reads PAC-MAN — Leaderboard and a Pac-Man favicon is linked."
    - "Pasting the URL into a chat app unfurls a rich Open Graph / Twitter card (title, description, ~1200x630 image)."
    - "firebase deploy --only hosting targets the pacman-firebase project and serves the web/public directory."
  artifacts:
    - path: "web/public/index.html"
      provides: "Static markup contract + head (title, OG/Twitter meta, favicon, font, stylesheet + module script links)"
      min_lines: 60
      contains: "PAC-MAN — Leaderboard"
    - path: "firebase.json"
      provides: "Firebase Hosting config pointing at web/public"
      contains: "web/public"
    - path: ".firebaserc"
      provides: "Default Firebase project = pacman-firebase"
      contains: "pacman-firebase"
    - path: "web/package.json"
      provides: "ESM type marker so app.js is importable by node --test (no deps, no build step)"
      contains: "module"
    - path: "web/tests/scaffold.test.mjs"
      provides: "node:test assertions for the markup + hosting config"
  key_links:
    - from: "web/public/index.html"
      to: "web/public/app.js"
      via: "<script type=\"module\" src=\"app.js\">"
      pattern: "type=\"module\""
    - from: "web/public/index.html"
      to: "web/public/styles.css"
      via: "<link rel=\"stylesheet\" href=\"styles.css\">"
      pattern: "styles\\.css"
    - from: "firebase.json"
      to: "web/public"
      via: "hosting.public"
      pattern: "\"public\""
---

<objective>
Scaffold the static page's markup contract and Firebase Hosting config — the foundation both downstream plans build against. Create `web/public/index.html` as a full semantic skeleton (wordmark, This Week | All Time tab bar, last-week subtitle slot, board container, Refresh control, hint) with a complete `<head>` (title, Open Graph + Twitter Card meta, favicon link, pixel-font preload, stylesheet + module-script links), plus root `firebase.json` + `.firebaserc` hosting config and a `web/package.json` ESM marker.

This is plain static HTML/CSS/JS with no framework and no build step (D-01), living in a new `web/` folder in this repo (D-02), to be hosted on the existing Firebase project `pacman-firebase` on its default `.web.app` domain (D-03/D-15). No server/API change — the page is a pure consumer of the finished `get_leaderboard` function.

Purpose: Establish the exact DOM hooks (stable IDs / data-attributes / classes) that Plan 02 (`app.js` logic) and Plan 03 (`styles.css` + assets) attach to independently, so wave 2 can run in parallel. All visible copy mirrors the in-game board verbatim (D-12).
Output: `web/public/index.html`, `firebase.json`, `.firebaserc`, `web/package.json`, `web/tests/scaffold.test.mjs`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-leaderboard-page/07-CONTEXT.md
@.planning/phases/07-web-leaderboard-page/07-UI-SPEC.md
@.planning/phases/07-web-leaderboard-page/07-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols/files — exclude from drift verification)

This plan creates:
- `web/public/index.html` — static markup + head meta.
- `firebase.json` — Firebase Hosting config (repo root).
- `.firebaserc` — default project marker (`pacman-firebase`).
- `web/package.json` — `{ "type": "module" }` ESM marker (no dependencies; not a build step).
- `web/tests/scaffold.test.mjs` — node:test assertions.

DOM contract (stable hooks introduced here; consumed by Plan 02 `app.js` and Plan 03 `styles.css`):
- `.wordmark` (text `PAC-MAN`)
- `nav#tabbar` containing `button#tab-week.tab[data-scope="week"]` (`This Week`) and `button#tab-all.tab[data-scope="all"]` (`All Time`); active tab carries `tab--active`
- `#subtitle.subtitle[hidden]` (last-week champion line)
- `ol#board.board` (rank rows + state-message region)
- `li.rank-row` rows with `.rank-row--first` on rank 1; `.state-msg` for loading/offline/empty lines
- `button#refresh.refresh` (text `Refresh`)
- `#hint.hint`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: index.html — semantic board skeleton + branding head</name>
  <files>web/public/index.html, web/tests/scaffold.test.mjs</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-UI-SPEC.md (Copywriting Contract — verbatim strings; Typography/Color tokens; the two-family font split)
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-01 no build, D-06 font, D-07 tabs, D-12 copy, D-13 OG, D-14 branding, D-15 domain)
    - .planning/phases/07-web-leaderboard-page/07-PATTERNS.md (§ index.html render order mirroring menu.py:run_leaderboard; § Verbatim copy strings table)
    - menu.py lines 145-263 (run_leaderboard render order: header, tab bar, subtitle, board, hint — the layout to replicate as DOM)
  </read_first>
  <action>
    Create web/public/index.html as a single static HTML5 document, mobile-first (include `<meta name="viewport" content="width=device-width, initial-scale=1">` and `<meta charset="utf-8">`). No inline scripts, no framework, no build step (D-01).

    HEAD (D-13/D-14):
    - `<title>PAC-MAN — Leaderboard</title>` (em dash U+2014, exact — D-14).
    - `<meta name="description">` with a short arcade description (e.g. "The PAC-MAN high-score leaderboard — This Week and All Time. Beat your friends.").
    - Open Graph tags: `og:type` = website, `og:title` = `PAC-MAN — Leaderboard`, `og:description` = the arcade description, `og:image` = the ABSOLUTE url `https://pacman-firebase.web.app/og-preview.png` (scrapers require absolute), `og:image:width` = 1200, `og:image:height` = 630, `og:url` = `https://pacman-firebase.web.app/`.
    - Twitter Card tags: `twitter:card` = `summary_large_image`, `twitter:title`, `twitter:description`, `twitter:image` = the same absolute og-preview.png url.
    - `<link rel="icon" type="image/svg+xml" href="favicon.svg">` (Pac-Man favicon, created in Plan 03).
    - Pixel webfont (D-06): load Press Start 2P for the wordmark + tab labels only. Prefer SELF-HOSTING the woff2 under `web/public/fonts/` via an `@font-face` declared in styles.css (honors D-16 zero-tracking — no third-party request); if instead linking Google Fonts, add `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`. The actual `@font-face`/font wiring lives in styles.css (Plan 03) — here only add the preconnect/preload hint if the Google-Fonts path is chosen. Do not block render on the font.
    - `<link rel="stylesheet" href="styles.css">` (created in Plan 03 — reference it now).
    - `<script type="module" src="app.js"></script>` with `defer` semantics implied by module (created in Plan 02 — reference it now).

    BODY — replicate the in-game run_leaderboard render order (PATTERNS § index.html) as DOM with these EXACT stable hooks (the DOM contract Plans 02/03 depend on):
    - `<h1 class="wordmark">PAC-MAN</h1>` (arcade-yellow wordmark per D-14; color applied in CSS).
    - `<nav id="tabbar" class="tabbar" role="tablist">` containing two real `<button>` elements (44px touch targets, styled in Plan 03): `<button id="tab-all" class="tab tab--active" data-scope="all">All Time</button>` and `<button id="tab-week" class="tab" data-scope="week">This Week</button>`. Default-active = All Time (D-08) — `tab--active` starts on `#tab-all`. Tab label text EXACTLY `All Time` and `This Week` (D-12).
    - `<p id="subtitle" class="subtitle" hidden></p>` — last-week champion slot, `hidden` by default (D-09; populated by app.js as `Last week: {INITIALS}`, This Week view only).
    - `<ol id="board" class="board"></ol>` — empty rank-row / state-message region (app.js fills it). Seed it with one `<li class="state-msg">Loading...</li>` as the initial paint before app.js runs (verbatim `Loading...`, D-12).
    - `<button id="refresh" class="refresh">Refresh</button>` — the only added affordance vs in-game (D-10); 44px touch target.
    - `<p id="hint" class="hint">Tap a tab to switch boards.</p>` (web-appropriate hint; the in-game LEFT/RIGHT/ESC hint does not apply on touch).

    Use `textContent`-safe static markup only; NO server data is inlined (entries come from app.js at runtime). Keep all copy byte-for-byte from the UI-SPEC Copywriting Contract.

    Then create web/tests/scaffold.test.mjs (node:test + node:assert/strict) that readFileSync('web/public/index.html','utf8') and asserts: contains `<title>PAC-MAN — Leaderboard</title>`; contains `class="wordmark"` and text `PAC-MAN`; contains `id="tabbar"`, `id="tab-all"`, `id="tab-week"`, `data-scope="all"`, `data-scope="week"`, label text `All Time` and `This Week`; `tab--active` appears on the All Time tab (assert the `id="tab-all"` button's class list includes `tab--active`); contains `id="subtitle"` with the `hidden` attribute; contains `id="board"`; contains the seed `Loading...`; contains `id="refresh"` with text `Refresh`; contains `og:image` with the absolute `https://pacman-firebase.web.app/og-preview.png`; contains `twitter:card` `summary_large_image`; references `styles.css`, `app.js` (`type="module"`), and `favicon.svg`.
  </action>
  <verify>
    <automated>node --test web/tests/scaffold.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/scaffold.test.mjs` exits 0 with the index.html assertions passing.
    - index.html `<title>` is exactly `PAC-MAN — Leaderboard` (em dash).
    - The All Time tab carries `tab--active` on load (D-08 default), not This Week.
    - All visible copy (`All Time`, `This Week`, `Loading...`, `Refresh`) matches the UI-SPEC Copywriting Contract verbatim.
    - `og:image` and `twitter:image` are the absolute `https://pacman-firebase.web.app/og-preview.png` url; `og:image:width`/`og:image:height` are 1200/630.
    - index.html references `styles.css`, `app.js` (as `type="module"`), and `favicon.svg` (not-yet-created files are referenced intentionally).
  </acceptance_criteria>
  <done>web/public/index.html exists with the full DOM contract + branding head; scaffold.test.mjs passes its index.html assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Firebase Hosting config + ESM marker</name>
  <files>firebase.json, .firebaserc, web/package.json, web/tests/scaffold.test.mjs</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-02 web/ folder layout discretion, D-03 same Firebase project + manual deploy, D-15 default domain)
    - .planning/phases/07-web-leaderboard-page/07-PATTERNS.md (§ firebase.json — manual operator deploy convention)
    - firebase.json hosting schema via Context7 (/llmstxt/firebase_google_llms_txt) if any key is uncertain — `public` is the only required attribute; `ignore` + `headers` + `cleanUrls` are optional.
    - web/tests/scaffold.test.mjs (extend the file created in Task 1)
  </read_first>
  <action>
    Create firebase.json at the REPO ROOT (D-02 chosen layout: single root config, static files under web/public). Hosting config:
    - `hosting.public` = `web/public` (the only required key; this is what the deploy publishes — root files like firebase-key.json are NOT under web/public and are never served).
    - `hosting.ignore` = `["firebase.json", "**/.*", "**/node_modules/**"]` (standard; also excludes the dotfiles and any stray node_modules).
    - `hosting.cleanUrls` = true (serve `/` → index.html cleanly).
    - `hosting.headers` = one rule for `**/*.@(js|css|svg|png|woff2)` setting `Cache-Control: max-age=3600` so the static assets cache for an hour but updates propagate quickly.
    - NO rewrites (single page; no SPA routing), NO functions/run blocks (the page calls the already-deployed get_leaderboard directly over its public URL — D-03; this config is hosting-only).

    Create .firebaserc at the REPO ROOT with `projects.default` = `pacman-firebase` (the Firebase PROJECT ID from firebase-key.json — distinct from the Cloud Run host number 991339031546). This makes the operator deploy `firebase deploy --only hosting` work with no `--project` flag and pins the default `.web.app` domain to `pacman-firebase.web.app` (D-15).

    Create web/package.json with exactly `{ "type": "module" }` (no name/version/deps/scripts required beyond what node tolerates; add `"private": true` to silence npm warnings). Purpose: it makes node treat `web/public/app.js` as an ES module so `node --test` in Plan 02 can `import` the page's pure functions. It is NOT a dependency manifest, has no `node_modules`, and introduces no build step (honors D-01). It sits at `web/` (outside `web/public`) so it is never deployed.

    Extend web/tests/scaffold.test.mjs with config assertions: JSON.parse(firebase.json).hosting.public === `web/public`; hosting.ignore includes `firebase.json`; JSON.parse(.firebaserc).projects.default === `pacman-firebase`; JSON.parse(web/package.json).type === `module`.
  </action>
  <verify>
    <automated>node --test web/tests/scaffold.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/scaffold.test.mjs` exits 0 with BOTH the index.html (Task 1) and config (Task 2) assertions passing.
    - firebase.json is valid JSON; `hosting.public` === `web/public`; `ignore` excludes `firebase.json`.
    - .firebaserc `projects.default` === `pacman-firebase`.
    - web/package.json `type` === `module` and contains no `dependencies` block.
    - firebase.json contains no `functions`/`rewrites`-to-backend blocks (hosting-only — no server change this phase).
  </acceptance_criteria>
  <done>Root firebase.json + .firebaserc + web/package.json exist and validate; scaffold.test.mjs fully passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Firebase Hosting | Manual `firebase deploy --only hosting` publishes the static assets under web/public to the public `.web.app` domain |
| browser → get_leaderboard (Cloud Run) | The page (downstream plans) reads public JSON over HTTPS; CORS `*` already set server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Info disclosure | hosting config / served files | mitigate | `hosting.public` scoped to `web/public` only — root `firebase-key.json` (admin SA key) is NOT under web/public and is never deployed; `ignore` also drops dotfiles |
| T-07-04 | Spoofing/Repudiation/Elevation | static page | accept | Read-only page: no auth, no writes, no privileged actions exist to spoof or repudiate |
| T-07-SC | Tampering (supply chain) | web/package.json | accept | `{ "type":"module" }` marker only — no dependencies, no node_modules, no npm/pip/cargo install (D-01 no build step); nothing executable is fetched at deploy |
</threat_model>

<verification>
- `node --test web/tests/scaffold.test.mjs` passes (index.html structure + branding head + hosting config).
- Manual sanity: `firebase deploy --only hosting --dry-run`-equivalent is deferred to Plan 04 (operator step); this plan's gate is the in-repo config validating.
</verification>

<success_criteria>
- index.html renders the full board skeleton (wordmark, tabs default-All-Time, subtitle slot, board, refresh, hint) with verbatim in-game copy and a complete branding head (title, OG/Twitter, favicon, font, stylesheet + module script).
- firebase.json + .firebaserc make `firebase deploy --only hosting` a one-command operator step targeting `pacman-firebase` → `pacman-firebase.web.app`, publishing `web/public`.
- The DOM contract (IDs/data-attrs/classes) is fixed so Plan 02 and Plan 03 attach independently.
</success_criteria>

<output>
Create `.planning/phases/07-web-leaderboard-page/07-01-SUMMARY.md` when done.
</output>
