---
phase: 07-web-leaderboard-page
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03]
must_haves:
  truths:
    - "Served locally, the page renders the full retro-arcade board, opens on All Time, toggles to This Week (with last-week subtitle when present), refreshes, and is readable on a phone-width viewport."
    - "The operator publishes the page with `firebase deploy --only hosting` to the existing pacman-firebase project."
    - "The live `https://pacman-firebase.web.app` page loads both boards by fetching the existing get_leaderboard API, with no server/API change."
  artifacts: []
  key_links:
    - from: "https://pacman-firebase.web.app"
      to: "https://get-leaderboard-991339031546.asia-southeast1.run.app"
      via: "browser fetch from the deployed page"
      pattern: "get-leaderboard-991339031546"
---

<objective>
Verify and ship the page. First a human-verify checkpoint confirming the arcade look + full board behavior on a phone-width viewport served locally (WEB-02 is inherently visual). Then the manual operator deploy — `firebase deploy --only hosting` — which is a deliberate MANUAL operator step (D-03), the same pattern as the Phase 4/6 manual function redeploys; the in-repo deliverable is the page + firebase.json, live publish is operator-run. Closes WEB-01/02/03 against the live default `.web.app` domain (D-15).

No server/API change — the live page is a pure consumer of the already-deployed `get_leaderboard` function.

Purpose: Prove the three success criteria end-to-end (public Firebase Hosting page; mobile-first arcade style; both This Week / All Time views) on the real domain.
Output: a published `https://pacman-firebase.web.app` leaderboard page (no new repo files).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-leaderboard-page/07-CONTEXT.md
@.planning/phases/07-web-leaderboard-page/07-UI-SPEC.md
@web/public/index.html
@firebase.json
</context>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols/files — exclude from drift verification)

This plan creates NO new repo files. It verifies the artifacts from Plans 01-03 and publishes them. The live URL it activates: `https://pacman-firebase.web.app` (default Firebase domain, D-15). The deploy command used: `firebase deploy --only hosting`.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Local visual + behavior verification on a phone-width viewport</name>
  <files>(verification only — no repo files modified; serves web/public/)</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-UI-SPEC.md (the visual/copy contract to judge against)
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-04 arcade look, D-07/D-08 tabs + All-Time default, D-09 subtitle, D-10 refresh, D-12 states)
    - web/public/index.html, web/public/app.js, web/public/styles.css (what is being served)
  </read_first>
  <action>
    Run the three automated node:test suites as the pre-checkpoint gate, then serve web/public locally and PAUSE for the operator to visually confirm the arcade look + full board behavior on a phone-width viewport (steps in how-to-verify). This is a blocking human-verify checkpoint because WEB-02 (mobile-first arcade style matching the game) is inherently visual and cannot be fully asserted by automation. Do not proceed to the deploy task until the operator approves.
  </action>
  <what-built>
    A complete static leaderboard page in web/public/ (index.html + app.js + styles.css + favicon.svg + og-preview.png) that fetches the live get_leaderboard API and mirrors the in-game This Week / All Time boards in full retro-arcade style. All automated gates from Plans 01-03 pass (`node --test web/tests/`).
  </what-built>
  <how-to-verify>
    1. Serve locally from the public dir: run `python -m http.server 5000` from `web/public/` (use the project `.venv` python). Leave it running.
    2. Open `http://localhost:5000` in a browser. Open dev tools and switch to a phone-width device emulation (e.g. iPhone / 390px wide) — or open it on your actual phone via your machine's LAN IP.
    3. Confirm the page OPENS ON ALL TIME (the default — D-08, deliberately different from the in-game This Week default), shows the yellow `PAC-MAN` wordmark, the `This Week | All Time` tab pair with All Time active (yellow), and a board of scores with rank 1 in yellow and ranks 2-10 in white, dot-leaders filling between initials and score.
    4. Tap `This Week`: it loads (brief `Loading...`), shows the weekly board, and — if last week had a champion — a gray `Last week: XXX` subtitle under the header (hidden if none). Tap back to `All Time`: it shows instantly from cache.
    5. Tap `Refresh`: the active view re-pulls and re-renders.
    6. Confirm mobile readability: no horizontal scroll at phone width, tab + Refresh are comfortable touch targets (~44px), numbers legible.
    7. (Optional) Confirm the browser tab title is `PAC-MAN — Leaderboard` and the Pac-Man favicon shows.
    8. (Offline check, optional) Temporarily go offline / block the request and confirm the board shows `Could not connect to leaderboard.` rather than a crash, and the other cached view still renders.
  </how-to-verify>
  <verify>
    <automated>node --test web/tests/scaffold.test.mjs web/tests/app.test.mjs web/tests/style.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All three node:test suites pass (re-run as the pre-checkpoint automated gate).
    - Operator confirms: arcade look matches the game; opens on All Time; This Week toggles + lazy-loads with the last-week subtitle behavior; Refresh works; mobile-readable with ~44px touch targets; verbatim state copy.
  </acceptance_criteria>
  <done>All three node:test suites pass and the operator has approved the page's arcade look + behavior on a phone-width viewport.</done>
  <resume-signal>Type "approved" if the page looks/behaves correct on a phone-width viewport, or describe what is off.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator deploy to Firebase Hosting + live smoke</name>
  <files>(no repo files modified — operator runs `firebase deploy --only hosting`)</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-03 manual operator deploy, same Firebase project; D-15 default domain)
    - cloud_functions/DEPLOY.md (the established manual operator-deploy pattern for Phases 4/6)
    - firebase.json, .firebaserc (the hosting config + default project pacman-firebase)
  </read_first>
  <action>
    PAUSE for the operator to publish the page. Deploying Firebase Hosting is intentionally a manual operator step (D-03), mirroring the Phase 4/6 manual function redeploys — the in-repo deliverable (web/public/* + root firebase.json + .firebaserc) is complete; the operator runs `firebase deploy --only hosting` from the repo root with their own authenticated Firebase CLI. After the operator reports success, run the automated live-API smoke (the curl below) and have the operator confirm `https://pacman-firebase.web.app` renders both boards. No server/API change is made.
  </action>
  <what-built>
    The in-repo hosting deliverable (web/public/* + root firebase.json + .firebaserc → project pacman-firebase). Publishing is intentionally a manual operator step (D-03), mirroring the Phase 4/6 manual function redeploys.
  </what-built>
  <how-to-verify>
    1. Ensure the Firebase CLI is installed and authenticated for the `pacman-firebase` project (`firebase login`; the CLI is the operator's, not baked into the repo).
    2. From the repo root, run: `firebase deploy --only hosting` (the root firebase.json + .firebaserc target project `pacman-firebase`, public dir `web/public`; no `--project` flag needed).
    3. After the deploy reports success, visit `https://pacman-firebase.web.app` on a phone. Confirm the board loads from the live API, both This Week and All Time work, and the OG card unfurls when the link is pasted into a chat app (optional).
  </how-to-verify>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "https://get-leaderboard-991339031546.asia-southeast1.run.app?scope=all"</automated>
  </verify>
  <acceptance_criteria>
    - Operator runs `firebase deploy --only hosting` and it reports success for project `pacman-firebase`.
    - `https://pacman-firebase.web.app` responds 200 and renders the leaderboard (operator-confirmed in a browser).
    - The live get_leaderboard API the page consumes returns 200 for `scope=all` (automated curl above) — proving the read endpoint the live page depends on is up; no server/API change was made.
    - Both This Week and All Time views render on the live page (operator-confirmed).
  </acceptance_criteria>
  <done>The operator has run `firebase deploy --only hosting` successfully and `https://pacman-firebase.web.app` shows both boards; the live get_leaderboard `scope=all` curl returns 200.</done>
  <resume-signal>Type "deployed" once `firebase deploy --only hosting` succeeds and `https://pacman-firebase.web.app` shows the boards, or describe the deploy error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Firebase Hosting | Manual `firebase deploy --only hosting` publishes web/public to the public `.web.app` domain |
| public browser → live page → get_leaderboard | Anyone with the link fetches public, read-only JSON over HTTPS (CORS `*`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Info disclosure | what gets deployed | mitigate | `hosting.public` = web/public only; root `firebase-key.json` (admin SA key) and dotfiles are outside web/public / ignored → never published. Operator verifies the deploy file list excludes secrets |
| T-07-03 | Denial of service | public live endpoint | accept | Read-only, no auto-poll (D-10); get_leaderboard `max-instances=5` (Phase 4) bounds cost/blast radius |
| T-07-04 | Spoofing / Repudiation / Elevation | live read-only page | accept | No auth, no writes, no privileged actions; nothing to spoof or escalate |
| T-07-SC | Tampering (supply chain) | Firebase CLI deploy | accept | Operator's own authenticated CLI; no npm/pip/cargo install added to the repo (D-01); deploy publishes only the static, author-controlled web/public assets |
</threat_model>

<verification>
- Pre-deploy: all three node:test suites pass; human-verify confirms the arcade look + behavior on a phone-width viewport.
- Post-deploy: `https://pacman-firebase.web.app` returns 200 and renders both boards (operator-confirmed); the live get_leaderboard `scope=all` curl returns 200.
</verification>

<success_criteria>
- WEB-01: a public web page on Firebase Hosting (`pacman-firebase.web.app`) displays the leaderboard, fetching the existing Cloud Functions API.
- WEB-02: the live page is mobile-first / readable on a phone in arcade style matching the game (human-verified).
- WEB-03: the live page mirrors the in-game boards — both This Week and All Time views.
</success_criteria>

<output>
Create `.planning/phases/07-web-leaderboard-page/07-04-SUMMARY.md` when done.
</output>
