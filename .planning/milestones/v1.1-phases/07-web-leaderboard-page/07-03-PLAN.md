---
phase: 07-web-leaderboard-page
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/public/styles.css
  - web/public/favicon.svg
  - web/public/og-preview.png
  - web/public/fonts/
  - web/tests/style.test.mjs
autonomous: true
requirements: [WEB-02, WEB-03]
must_haves:
  truths:
    - "The page is full retro arcade: black background, Pac-Man-yellow accents, deep-navy board panel, pixel-font wordmark + tabs, monospace rank rows."
    - "It is mobile-first and readable on a phone: tab and Refresh controls are >=44px touch targets and the layout fits a narrow viewport without horizontal scroll."
    - "Rank 1 is yellow; ranks 2-10 are white; dot-leaders fill the gap between initials and score via CSS (not literal dots)."
    - "A Pac-Man favicon shows in the browser tab and a ~1200x630 arcade OG preview image exists for link unfurls."
  artifacts:
    - path: "web/public/styles.css"
      provides: "Retro arcade stylesheet honoring the UI-SPEC tokens, built via the frontend-design skill"
      min_lines: 80
      contains: "#FFFF00"
    - path: "web/public/favicon.svg"
      provides: "Pac-Man vector favicon"
      contains: "<svg"
    - path: "web/public/og-preview.png"
      provides: "~1200x630 arcade OG/Twitter preview image"
    - path: "web/tests/style.test.mjs"
      provides: "node:test assertions for the CSS tokens, favicon root, and PNG dimensions"
  key_links:
    - from: "web/public/styles.css"
      to: "web/public/index.html"
      via: "selectors matching .wordmark / .tab / .tab--active / .board / .rank-row / .rank-row--first / .state-msg / .refresh / .subtitle"
      pattern: "rank-row--first"
    - from: "web/public/index.html"
      to: "web/public/og-preview.png"
      via: "og:image / twitter:image meta (absolute url)"
      pattern: "og-preview\\.png"
---

<objective>
Build the full retro-arcade visual layer and the share/branding assets for the leaderboard page, routed through the `frontend-design` skill (D-05, a hard user directive — not a templated dark theme). Deliver `web/public/styles.css` (honoring every UI-SPEC token), the Pac-Man `web/public/favicon.svg`, and the ~1200x630 `web/public/og-preview.png` for rich link unfurls (D-13/D-14).

Locked visual direction (D-04): black background, Pac-Man-yellow accents, deep-arcade-navy board panel, chunky rank list, arcade chrome. Mobile-first (WEB-02). Two-family type system (D-06): Press Start 2P pixel font for the `PAC-MAN` wordmark + tab labels only; a monospace stack for rank/score/body so the dot-leader column aligns and small numbers stay legible on a phone. Accent yellow (#FFFF00) is reserved for exactly four things (wordmark, active tab, rank-1 row, Refresh focus). No analytics / zero tracking (D-16) — prefer self-hosting the pixel font over a third-party request.

Purpose: Deliver WEB-02 (mobile-first arcade style matching the game) and the visual half of WEB-03 (boards that look like the in-game boards).
Output: `web/public/styles.css`, `web/public/favicon.svg`, `web/public/og-preview.png`, optional `web/public/fonts/` (self-hosted woff2), `web/tests/style.test.mjs`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-leaderboard-page/07-UI-SPEC.md
@.planning/phases/07-web-leaderboard-page/07-CONTEXT.md
@web/public/index.html
</context>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols/files — exclude from drift verification)

This plan creates:
- `web/public/styles.css` — retro-arcade stylesheet. New selectors (must match the Plan 01 DOM hooks): `body`, `.wordmark`, `.tabbar`, `.tab`, `.tab--active`, `.subtitle`, `.board`, `.rank-row`, `.rank-row--first`, `.rank-row .dots`, `.state-msg`, `.refresh`, `.hint`; CSS custom props for the tokens (e.g. `--bg`, `--panel`, `--accent`, `--white`, `--gray`); `@font-face` for the pixel font if self-hosted.
- `web/public/favicon.svg` — Pac-Man vector icon.
- `web/public/og-preview.png` — ~1200x630 arcade preview image.
- `web/public/fonts/` — optional self-hosted Press Start 2P woff2 (D-16).
- `web/tests/style.test.mjs` — node:test assertions.

Consumes (already created, do NOT recreate): the Plan 01 DOM hooks and the index.html head links to `styles.css` / `favicon.svg` / the absolute `og-preview.png` url.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: styles.css — retro arcade, mobile-first (frontend-design skill)</name>
  <files>web/public/styles.css, web/public/fonts/, web/tests/style.test.mjs</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-UI-SPEC.md (the FULL token contract: Design System, Spacing Scale 4px multiples + 44px touch exception, Typography table, Color 60/30/10 + accent-reserved list + supporting white/gray, Copywriting Contract)
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-04 full retro arcade, D-05 frontend-design directive, D-06 font split, D-16 zero tracking)
    - settings.py lines 61-63 (COLOR_YELLOW #FFFF00, COLOR_WHITE #FFFFFF, COLOR_GRAY #808080 — the in-game palette to echo)
    - web/public/index.html (the exact DOM hooks the CSS must select: .wordmark, #tabbar/.tab/.tab--active, #subtitle, #board/.rank-row/.rank-row--first/.dots/.state-msg, #refresh, .hint)
    - menu.py lines 186-247 (in-game board look: black fill, yellow header/rank-1, white rows, gray hints/subtitle — visual parity reference)
  </read_first>
  <action>
    INVOKE THE `frontend-design` SKILL to drive this stylesheet (D-05, hard directive). The skill produces the distinctive arcade chrome; the UI-SPEC tokens below are the non-negotiable contract it must honor (it may add detail WITHIN them).

    Create web/public/styles.css, mobile-first (base styles target a narrow phone viewport; layer desktop centering via `min-width` media query). Define CSS custom properties for the tokens: `--bg:#000000` (60% dominant page background), `--panel:#10102E` (30% deep-arcade-navy board/tab surface), `--accent:#FFFF00` (10% Pac-Man yellow), `--white:#FFFFFF`, `--gray:#808080`. Echoes settings.py COLOR_* exactly.

    Accent (#FFFF00) RESERVED for exactly four uses (UI-SPEC): (1) `.wordmark`, (2) the active tab `.tab--active` label/indicator, (3) the rank-1 row `.rank-row--first`, (4) the `.refresh` active/focus highlight. Nothing else is yellow. Supporting text: `.rank-row` (ranks 2-10) white; `.hint`, `.subtitle`, inactive `.tab`, `.state-msg`, and `.dots` gray.

    Typography (UI-SPEC table) — two families:
    - Press Start 2P (D-06) for `.wordmark` (28px display, line-height 1.2) and `.tab` labels (16px, 1.2) ONLY. Self-host the woff2 under web/public/fonts/ and declare `@font-face { font-family:"Press Start 2P"; src:url("fonts/<file>.woff2") format("woff2"); font-display:swap; }` to honor D-16 zero-tracking (no third-party request). If the woff2 cannot be obtained offline, fall back to the Google Fonts `<link>` (and add the preconnect in index.html) — but prefer self-host.
    - Monospace stack for `.rank-row`, `.subtitle`, `.state-msg`, `.hint`: `ui-monospace, "SF Mono", "Cascadia Mono", Menlo, Consolas, monospace` (UI-SPEC). Body/rank text 16px line-height 1.5; rank-1 row weight 700; labels 12px.

    Spacing: use the 4px-multiple scale (xs4/sm8/md16/lg24/xl32/2xl48). The ONE allowed deviation: `.tab` and `.refresh` must have `min-height:44px` (and adequate horizontal hit area) for mobile touch targets (UI-SPEC / WCAG 2.5.5) even if their text box is smaller. `.board` gets md(16px) side gutters on mobile; lg(24px) gaps between the header block, tab bar, and board; xl(32px) above/below the wordmark.

    Dot-leaders: render `.rank-row` as a flex row — rank+initials on the left, score on the right, and `.dots` as a flexible gray fill BETWEEN them via CSS (e.g. a repeating radial-gradient/leader-dots technique or a flex spacer with a dotted bottom border) — NOT literal `.` characters (app.js emits an empty `.dots` span; the dots are pure CSS). Ensure no horizontal scroll at 320px width.

    Create web/tests/style.test.mjs (node:test) reading styles.css as text: assert it contains `#FFFF00`, `#10102E`, `#000000`, `#FFFFFF`, `#808080`; contains selectors `.wordmark`, `.tab--active`, `.rank-row--first`, `.refresh`, `.board`, `.state-msg`, `.dots`; contains `Press Start 2P` and a `monospace` stack; contains `44px` (touch target) and at least one `@media` query (mobile-first/responsive); and that every `.tab`/`.refresh` rule region references `44px` min-height (assert the substring `min-height` co-occurs with `44px`).
  </action>
  <verify>
    <automated>node --test web/tests/style.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/style.test.mjs` exits 0.
    - styles.css contains all five token hex values (#000000, #10102E, #FFFF00, #FFFFFF, #808080) matching the UI-SPEC Color table and settings.py COLOR_*.
    - styles.css defines selectors for every Plan 01 DOM hook (`.wordmark`, `.tab`, `.tab--active`, `.subtitle`, `.board`, `.rank-row`, `.rank-row--first`, `.dots`, `.state-msg`, `.refresh`, `.hint`).
    - `.tab` and `.refresh` carry `min-height:44px` (touch targets); at least one `@media` query exists (responsive/mobile-first).
    - Press Start 2P is used for wordmark/tabs only; rank/body text uses the monospace stack (D-06).
    - The `frontend-design` skill was invoked for the visual build (noted in the SUMMARY).
  </acceptance_criteria>
  <done>styles.css delivers the full retro-arcade, mobile-first look against the DOM contract; style.test.mjs CSS assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Branding assets — Pac-Man favicon.svg + ~1200x630 og-preview.png</name>
  <files>web/public/favicon.svg, web/public/og-preview.png, web/tests/style.test.mjs</files>
  <read_first>
    - .planning/phases/07-web-leaderboard-page/07-CONTEXT.md (D-13 OG + preview image ~1200x630, D-14 Pac-Man favicon + branding, D-04 arcade palette)
    - .planning/phases/07-web-leaderboard-page/07-UI-SPEC.md (Color tokens for the artwork; OG/Twitter Card row — title `PAC-MAN — Leaderboard`, arcade description, ~1200x630 image)
    - web/public/index.html (the head already references `favicon.svg` and the absolute `https://pacman-firebase.web.app/og-preview.png` — these files must satisfy those references)
    - web/tests/style.test.mjs (extend the file created in Task 1)
  </read_first>
  <action>
    INVOKE THE `frontend-design` SKILL for the artwork (D-05).

    favicon.svg (D-14): hand-author a small, crisp Pac-Man vector — a yellow (#FFFF00) Pac-Man disc with a wedge mouth on a transparent or black background, root `<svg>` with a `viewBox` (e.g. `0 0 32 32`) so it scales in the tab. Save to web/public/favicon.svg. It must match the `<link rel="icon" type="image/svg+xml" href="favicon.svg">` already in index.html.

    og-preview.png (D-13): a ~1200x630 arcade share card — black (#000000) background, the yellow `PAC-MAN` wordmark, a hint of the leaderboard/Pac-Man motif, and the deep-navy (#10102E) panel accent; readable as a thumbnail. Produce a real PNG at 1200x630 (raster, not SVG — many unfurlers reject SVG og:images). Generation method is the executor's discretion: e.g. design an SVG/HTML card then rasterize via a headless-browser screenshot, or draw it with Python Pillow from the project `.venv`, or any tool that emits a valid 1200x630 PNG. Save to web/public/og-preview.png. It must satisfy the absolute `og:image`/`twitter:image` url already in index.html.

    Extend web/tests/style.test.mjs: assert favicon.svg exists and its text contains `<svg` and a `viewBox`; assert og-preview.png exists and parse its PNG IHDR (bytes: the 4-byte big-endian width at offset 16 and height at offset 20 of the file) to assert width === 1200 and height === 630 (allow this exact size; if a different 1.91:1 size is produced, assert width/height ratio within 1.90-1.92 and width >= 1200). Also assert index.html's `og:image` references `og-preview.png` (cross-check the reference resolves to the created file name).
  </action>
  <verify>
    <automated>node --test web/tests/style.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test web/tests/style.test.mjs` exits 0 (CSS + favicon + PNG assertions).
    - web/public/favicon.svg is a valid SVG (`<svg ... viewBox=...>`) depicting Pac-Man in arcade yellow.
    - web/public/og-preview.png is a real PNG whose IHDR dimensions are 1200x630 (or a 1.91:1 image >=1200px wide).
    - Both assets satisfy the references already present in index.html (`favicon.svg`, absolute `og-preview.png`).
    - Artwork uses the locked arcade palette (#000000 / #FFFF00 / #10102E).
  </acceptance_criteria>
  <done>favicon.svg + og-preview.png exist, match the head references, and pass dimension/format assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → font/asset source | If the pixel font is loaded from Google Fonts, the browser makes a third-party request; self-hosting removes it |
| operator → Firebase Hosting | The new static assets (css/svg/png/woff2) are published as-is on the public domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering / privacy | Press Start 2P webfont | mitigate | Self-host the woff2 under web/public/fonts/ so no third-party (Google) request is made (honors D-16 zero-tracking); if Google Fonts is used instead, load with `crossorigin` and no tracking scripts — the font is static, non-executable |
| T-07-01 | Tampering (XSS) | static CSS/SVG/PNG assets | accept | These are author-controlled static files with no dynamic/script content; the SVG favicon contains no `<script>`; the live XSS surface (API data) is handled in Plan 02 |
| T-07-SC | Tampering (supply chain) | asset generation tooling | accept | No npm/pip/cargo runtime install is added (D-01); any rasterization tool (headless browser / Pillow in `.venv`) is a build-time author tool, not a shipped dependency; output is a static PNG/SVG |
</threat_model>

<verification>
- `node --test web/tests/style.test.mjs` passes (CSS tokens + selectors + touch targets + responsive; favicon root; PNG 1200x630).
- Visual fidelity on a real phone (arcade look, mobile readability, dot-leaders) is confirmed in Plan 04's human-verify checkpoint (WEB-02 is inherently visual).
</verification>

<success_criteria>
- The page is unmistakably the Pac-Man arcade aesthetic, mobile-first, with the UI-SPEC tokens honored and accent yellow reserved to its four uses.
- A Pac-Man favicon and a 1200x630 arcade OG card exist and satisfy the index.html head references, making the shared link look like "something" before anyone taps it.
- The visual build went through the `frontend-design` skill (D-05).
</success_criteria>

<output>
Create `.planning/phases/07-web-leaderboard-page/07-03-SUMMARY.md` when done.
</output>
