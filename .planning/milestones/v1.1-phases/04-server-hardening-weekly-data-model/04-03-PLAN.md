---
phase: 04-server-hardening-weekly-data-model
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - cloud_functions/get_leaderboard/main.py
  - tests/test_get_leaderboard.py
autonomous: true
requirements: [BOARD-01, BOARD-02]
must_haves:
  truths:
    - "A GET with no scope param returns the current week's top scores (default = week)"
    - "A GET with ?scope=all returns the all-time top scores from the leaderboard collection"
    - "A GET with ?scope=week queries the weekly collection filtered to the current week_id"
    - "Every response (week or all) projects only {initials, score} — machine_id never leaks (D-10)"
    - "An unknown/garbage scope value falls back to week without a 400"
  artifacts:
    - path: "cloud_functions/get_leaderboard/main.py"
      provides: "Scope-aware leaderboard read (week|all), default week, machine_id-out projection preserved"
      contains: "scope"
    - path: "tests/test_get_leaderboard.py"
      provides: "Scope=all / scope=week / default / projection tests"
      contains: "scope=all"
  key_links:
    - from: "cloud_functions/get_leaderboard/main.py"
      to: "leaderboard_crypto.current_week_id"
      via: "import + call to build the weekly where-filter"
      pattern: "current_week_id"
    - from: "cloud_functions/get_leaderboard/main.py"
      to: "weekly collection filtered by week_id"
      via: "where(week_id ==) order_by(score desc) limit(10)"
      pattern: "where\\("
---

<objective>
Make `get_leaderboard` scope-aware (BOARD-02 / D-08). `?scope=all` keeps the existing all-time query untouched; `?scope=week` (and the param-less default) queries the new `weekly` collection filtered to the current week. The `{initials, score}` projection is preserved on every path so machine_id never leaks (D-10). The composite index the weekly query needs is created manually in Plan 04 (Console).

Per confirmed user decision: default scope = `week` (the already-shipped v1.0 exe will show the weekly board on redeploy, before the Phase 6 toggle — accepted). `is_new_best` is untouched here (read path).

Purpose: Satisfy BOARD-02 (scope-aware reader, all-time retained) and the read half of BOARD-01 (weekly view), while keeping CORS headers verbatim (Phase 7 dependency) and the D-10 projection intact.
Output: Scope-aware `get_leaderboard/main.py` + extended `tests/test_get_leaderboard.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/pacman-firebase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-server-hardening-weekly-data-model/04-CONTEXT.md
@.planning/phases/04-server-hardening-weekly-data-model/04-RESEARCH.md
@.planning/phases/04-server-hardening-weekly-data-model/04-PATTERNS.md
@cloud_functions/get_leaderboard/main.py
@tests/test_get_leaderboard.py
@tests/conftest.py
@.planning/phases/04-server-hardening-weekly-data-model/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates/introduces (treat as introduced-here for source-grounding):

- **Modified file:** `cloud_functions/get_leaderboard/main.py`
- **New request param accepted:** `?scope=week|all` (default `week`; unknown values fall back to `week`)
- **Consumed (from Plan 01):** `current_week_id` from `leaderboard_crypto`
- **Reads the new `weekly` collection** (created by Plan 02) filtered `where("week_id","==",current_week_id())` order_by score desc limit 10
- **New composite-index dependency:** weekly `week_id ASC, score DESC` (created manually in Plan 04 Console — not an in-repo artifact)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope-aware get_leaderboard (week|all, default week) with machine_id-out projection</name>
  <files>cloud_functions/get_leaderboard/main.py, tests/test_get_leaderboard.py</files>
  <read_first>
    - cloud_functions/get_leaderboard/main.py (the file being modified — current all-time-only query + {initials,score} projection)
    - cloud_functions/get_leaderboard/leaderboard_crypto.py (the Plan-01 helper copy this imports: current_week_id)
    - .planning/phases/04-server-hardening-weekly-data-model/04-RESEARCH.md (§ Scope Parameter — scope parsing, default week, weekly query shape, composite-index note, CORS-preservation landmine, backward-compat note; § Week-Bucket Storage weekly read query (a))
    - .planning/phases/04-server-hardening-weekly-data-model/04-PATTERNS.md (§ get_leaderboard query/projection/error pattern; § test doc+stream stub helpers, where-link wiring, query_string builder, projection-only test template)
    - tests/test_get_leaderboard.py (existing house style; param-less tests must stay green as default-week)
  </read_first>
  <behavior>
    - GET no scope param -> queries the weekly collection where week_id == current_week_id(), order_by score desc, limit 10
    - GET ?scope=all -> queries the leaderboard collection order_by score desc limit 10 (unchanged all-time path)
    - GET ?scope=week -> same as no-param (weekly current-week query)
    - GET ?scope=garbage -> falls back to weekly (no 400)
    - every response body is {"entries": [{"initials":..., "score":...}, ...]} — no machine_id, no week_id, no updated_at
    - OPTIONS preflight still returns 204 with Access-Control-Allow-Methods: GET
    - empty result -> {"entries": []} 200; query exception -> ({"entries": [], "error": ...}, 500, headers)
  </behavior>
  <action>
    Edit `get_leaderboard/main.py`. Add `from leaderboard_crypto import current_week_id` (helper lives in the same `get_leaderboard/` dir — Plan 01 copy). Keep the import/init shape and the OPTIONS preflight block verbatim (Access-Control-Allow-Methods: GET) and `headers = {"Access-Control-Allow-Origin": "*"}` (CORS preserved — Phase 7 dependency; RESEARCH § Scope Parameter CORS landmine).

    Parse scope tolerantly: `scope = (request.args.get("scope") or "week").lower()`; `if scope not in ("week", "all"): scope = "week"` (never 400 a reader — RESEARCH § Scope Parameter). Default + unknown → `week` (confirmed user decision: default week; accepts that the shipped exe shows the weekly board on redeploy).

    Branch the query: for `scope == "all"`, keep the EXISTING `db.collection("leaderboard").order_by("score", direction=firestore.Query.DESCENDING).limit(10)` query unchanged (D-08 all-time retained). For `week`, query `db.collection("weekly").where("week_id", "==", current_week_id()).order_by("score", direction=firestore.Query.DESCENDING).limit(10)` (RESEARCH § Week-Bucket Storage read query (a)). This equality+order query needs the composite index `week_id ASC, score DESC` — created manually in Plan 04; note it in a code comment.

    KEEP the projection loop exactly: `entries.append({"initials": data["initials"], "score": data["score"]})` — only initials+score ship, for BOTH scopes (D-10 machine_id-out-of-responses guarantee). Keep the try/except + `({"entries": [], "error": "Failed to fetch leaderboard"}, 500, headers)` shape and the `({"entries": entries}, 200, headers)` success shape.

    Tests in `tests/test_get_leaderboard.py`: keep the existing param-less tests green — they now exercise the default-week path, so re-wire `_stub_stream` (or add a weekly variant) to drive the `where(...).order_by(...).limit(...).stream()` chain when no scope is given. Add `test_scope_all_queries_leaderboard` (build request with `EnvironBuilder(method="GET", query_string="scope=all")`, assert the `leaderboard` collection chain is used / entries returned). Add `test_scope_week_queries_weekly` and `test_default_scope_is_week` asserting the `weekly` collection + a `where("week_id","==", ...)` link is exercised (pin current_week_id by injecting a known now, or assert the where filter argument). Add a weekly clone of `test_leaderboard_projects_only_initials_and_score` proving the weekly path also drops machine_id/week_id/updated_at. Use the where-link wiring and `query_string=` builder from PATTERNS §.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_get_leaderboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cloud_functions/get_leaderboard/main.py` parses `request.args.get("scope")` and defaults to `"week"`, with unknown values falling back to `"week"`
    - `cloud_functions/get_leaderboard/main.py` contains both `collection("leaderboard")` (scope=all) and `collection("weekly")` with a `where("week_id"` filter (scope=week)
    - `cloud_functions/get_leaderboard/main.py` references `current_week_id`
    - The response projection remains `{"initials": ..., "score": ...}` for both scopes (no machine_id/week_id/updated_at) — verified by a weekly projection test
    - The OPTIONS branch and `Access-Control-Allow-Methods: GET` header are unchanged
    - A GET with `?scope=all` exercises the leaderboard collection chain; no-param and `?scope=week` exercise the weekly chain; `?scope=garbage` does not 400
    - `.venv/Scripts/python.exe -m pytest tests/test_get_leaderboard.py` exits 0 (existing param-less tests stay green as default-week)
  </acceptance_criteria>
  <done>get_leaderboard is scope-aware (week default, all retained, garbage tolerated), the weekly path filters by current week_id, every response projects only {initials, score}, CORS headers and error contract preserved, all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/web/exe → get_leaderboard HTTP | Untrusted GET with optional ?scope param; reader endpoint, no auth (public board) |
| get_leaderboard → Firestore (leaderboard + weekly) | Read-only queries; response projected to {initials, score} only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | get_leaderboard response projection | mitigate | Projection loop emits only {initials, score} for BOTH scopes; machine_id/week_id/updated_at never serialized (D-10) — blocks machine_id harvest → impersonation |
| T-04-08 | Spoofing | weekly scope filter (week_id) | mitigate | week_id derived from current_week_id() server time only; client cannot request a forged week to read/fake last-week standings |
| T-04-12 | Denial of Service | tolerant scope parsing | mitigate | Unknown scope falls back to week (never 400 / never crashes the reader); endpoint flood bounded by max-instances cap (D-11, Plan 04) |
| T-04-13 | Tampering | CORS headers | accept | Access-Control-Allow-Origin: * preserved verbatim (public read board; Phase 7 web page depends on it) — open-read is intentional for a public leaderboard |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_get_leaderboard.py` exits 0
- `.venv/Scripts/python.exe -m pytest` full suite stays green (CI golden net unaffected — no ghost-AI code touched)
- Weekly projection test proves machine_id never leaks on the weekly path
</verification>

<success_criteria>
- BOARD-02: scope-aware reader — all-time retained, weekly added, default week (confirmed decision)
- BOARD-01 (read half): weekly view filters by current week_id from server time
- D-10: machine_id out of all responses on both scopes
- CORS + error-tuple contract preserved; param-less tests stay green
</success_criteria>

<output>
Create `.planning/phases/04-server-hardening-weekly-data-model/04-03-SUMMARY.md` when done
</output>
